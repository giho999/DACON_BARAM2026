# [opencode 작업지시] TASK-METRICW — 평가지표 정합 가중 재학습

> 작업 루트: `/home/gpu_04/DACON_baram2026/`
> TASK-G3TAIL과 **병렬** 진행. 우선순위는 G3TAIL이 위(제출 직결), 본 작업은
> (a) 원리적 개선 (b) 2차 평가 소명 자산 확보 두 목적.

---

## 0. 문제 정의 — 지금까지 학습 목적함수가 평가식과 어긋나 있었다

평가식은 다음 두 가지를 동시에 요구한다.

$$
\text{TOTAL}=\tfrac12\Big(1-\underbrace{\tfrac1n\sum_i \tfrac{|\hat y_i-y_i|}{C}}_{\text{NMAE, 균등가중}}\Big)
+\tfrac12\underbrace{\sum_i w_i\big[0.25\cdot\mathbb 1(e_i\le0.06)+0.75\cdot\mathbb 1(e_i\le0.08)\big]}_{\text{FICR, } w_i=y_i/\sum_j y_j}
$$

**둘 다 $y_i \ge 0.10\,C$ 인 행에서만 계산된다.**

### 0-1. 실측으로 확인된 어긋남 (train_labels.csv 2022–2024)

| 그룹 | `actual<0.10cap` 비율 | 전체 CF 평균 | 마스크 CF 평균 | FICR 가중 CF 평균 |
|---|---|---|---|---|
| G1 | **39.26%** | 0.3066 | 0.4890 (+0.182) | **0.6272 (+0.321)** |
| G2 | **39.35%** | 0.3276 | 0.5245 (+0.197) | **0.6659 (+0.338)** |
| G3 | **46.32%** | 0.2649 | 0.4754 (+0.210) | **0.6249 (+0.360)** |

- 평가 마스크는 **예측이 아니라 실측** 기준 → 실측이 낮은 시간대는 무엇을 써넣어도
  점수에 영향이 **정확히 0**이다. 전체의 39~46%가 그렇다.
- 그런데 현 base는 그 40%를 포함한 전체 분포의 조건부 중앙값을 학습한다.
  → **점수가 걸린 부분집합 위에서 보면 구조적 하향편향**이다.
  (마스크 기준 −0.18~0.21 cap, FICR 가중 기준 −0.32~0.36 cap)
- 인수인계서가 "G3 균일 음편향 −6.6~7.0%cap"이라 기록한 현상은 모델 결함이 아니라
  **이 마스킹 아티팩트**로 설명된다.

### 0-2. 올바른 표본가중

$$
w_i=\mathbb 1\!\left(y_i\ge 0.10\,C\right)\cdot\left(\frac{0.5}{n_{\text{eval}}}+\frac{0.5\,y_i}{\sum_{j\in\text{eval}} y_j}\right)
$$

- 앞항 = NMAE 몫 (마스크 내 균등)
- 뒷항 = FICR 몫 (발전량 비례)
- 마스크 밖 행은 가중 0

> **주의:** 인수인계서에 "power-weighted training 제거(성능 악화)"가 있으나
> 그건 **마스크 없이 발전량 가중만** 건 형태였다. 본 작업은 **마스크 + 0.5/0.5 혼합**이며
> 별개의 레버다. `§3 닫힌 레버 목록`에 없다.

### 0-3. 왜 이 레버가 8연속 실패와 다른가

실패한 8개는 전부 **2023/2024에서 적합한 상수(shift, 밴드, 캘리 파라미터)** 를 2025로
옮기다 깨졌다. **본 레버에는 연도 적합 상수가 0개다** — 목적함수 구조 변경뿐이다.
유일하게 전이에 성공한 CNN 편입도 순수 구조 변경이었다. 패턴이 일치한다.

---

## 1. 작업 내용

### 1-1. 학습 (필수)

- base GBDT 파이프라인(**`pipeline_v2` 계보 금지**, `work_a_package`/기존 GBDT 학습 경로 사용)에서
  **표본가중만 §0-2로 교체**하고 재학습. 피처·하이퍼파라미터·시드 **일절 변경 금지**.
- 그룹별 3개 모델. 시드 3개(42/1337/2024) → 3-seed 평균.
- 대조군: 동일 코드에서 가중만 균등(`w_i=1`)으로 학습한 **컨트롤**도 함께 산출.
  (가중 이외 요인 차단용)

### 1-2. 변형 3종만 비교 (그 이상 탐색 금지)

| ID | 가중 |
|---|---|
| `W0` (컨트롤) | 전 행 균등 |
| `W1` | 마스크만: $\mathbb 1(y\ge0.1C)$ |
| `W2` | **§0-2 정식** (마스크 + 0.5 균등 / 0.5 발전량) |

### 1-3. 평가 (하네스 규율)

- **평가 지표는 반드시 대회 평가식 원본**(`평가_산식_코드.ipynb`)을 그대로 써라.
  마스크·발전량가중을 뺀 자체 지표로 판단하면 D3와 같은 오류가 반복된다.
- 연도전이 홀드아웃 **2022–23 → 2024** 를 핵심 관문으로 사용.
- 3-seed 문턱: 평균 ΔTOTAL ≥ +0.0010 ∧ 2/3 seed 양수.
- **널대조 필수**: 가중을 무작위 셔플한 `W2-shuffle`을 함께 돌려, W2 이득이
  널을 유의하게 초과하는지 확인. 널 초과 못 하면 무판정.

### 1-4. 산출물 분포 진단 (이게 진짜 성공 신호)

TASK-G3TAIL의 §6-2와 **동일한 CF 구간별 질량비 표**를 W0/W1/W2 각각에 대해 산출.

성공 조건 = 스코어가 아니라 **분포 결손이 메워졌는가**:

| 구간 | 현 base 질량비(예측/실측) | 목표 |
|---|---|---|
| G1 `[0.95,1.10)` | 0.26 | ↑ |
| G2 `[0.95,1.10)` | 0.24 | ↑ |
| G3 `[0.85,0.95)` | **0.06** | ↑↑ |
| G3 `[0.95,1.10)` | **0.00** | ↑↑ |
| 전 그룹 `[0.10,0.30)` | 1.35~1.45 (과잉) | ↓ |

W2가 이 표를 올바른 방향으로 움직이면, 스코어 게이트가 애매하더라도
**TASK-G3TAIL의 λ를 원리로 대체할 수 있는 자산**이 된다.

---

## 2. 금지

- ❌ CNN 재학습·시드 탐색·블렌드 비율 스윕
- ❌ 피처 추가·전처리 변경 (0/56 기각 목록)
- ❌ `pipeline_v2` 계보
- ❌ W0/W1/W2 외 가중 변형 탐색
- ❌ 2024 성능으로 λ나 가중을 재조정하는 2차 튜닝
- ❌ 본 트랙 산출물의 **무단 LB 제출** — 프로브 예산은 G3TAIL이 우선 사용

---

## 3. 산출물

```
work_a_package/metricw/train_metricw.py        # W0/W1/W2 학습 (결정적, seed 고정)
work_a_package/metricw/eval_metricw.py         # 대회 평가식 원본 + 연도전이 홀드아웃
work_a_package/metricw/METRICW_report.md       # 아래 내용 포함
work_a_package/metricw/pred_{W0,W1,W2}_seed{42,1337,2024}.parquet
work_a_package/metricw/submission_metricw_W2.csv   # 제출 후보 (즉시 제출 금지)
work_a_package/PREREG_metricw_20260809.md
```

`METRICW_report.md` 필수 항목:
1. 표본가중 수식과 그룹별 실제 가중 통계 (마스크 제외율, 유효표본수)
2. W0/W1/W2 × 3seed 연도전이 홀드아웃 TOTAL/1-NMAE/FICR
3. `W2-shuffle` 널 결과와의 비교
4. **CF 구간별 질량비 표 (§1-4)** ← 가장 중요
5. 재현 커맨드, 환경(numpy 1.26.4 / pandas 1.5.3 / lightgbm 4.6.0 / catboost 1.2.10 / xgboost 2.1.4)
6. 결정성 확인 (동일 커맨드 2회 실행 시 md5 일치)

---

## 4. 2차 평가 소명 문장 (리포트에 그대로 포함)

> 대회 평가식은 `actual ≥ 0.10 × capacity` 인 행에서만 계산되며, FICR은 실측
> 발전량으로 가중된다. 그러나 표준 회귀 학습은 전체 분포(마스크 밖 39~46% 포함)의
> 조건부 중앙값을 추정하므로, 점수가 부여되는 부분집합 위에서 체계적 하향편향
> (마스크 기준 −0.18~0.21 cap, FICR 가중 기준 −0.32~0.36 cap)을 갖는다.
> 본 연구는 학습 표본가중을 평가식과 동일한 형태
> $w_i=\mathbb 1(y_i\ge0.1C)\,(0.5/n+0.5\,y_i/\sum y)$ 로 정합시켜 이 편향을 제거했다.
> 이 교정에는 연도 적합 상수가 포함되지 않으므로 연도전이 위험이 구조적으로 낮다.

---

## 5. 실행 순서

1. `PREREG_metricw_20260809.md` 선등록 (판정 문턱·널대조 명시) — **학습 전에**
2. W0/W1/W2 × 3seed 학습 (`nohup`, `OMP_NUM_THREADS=16`, `CUDA_VISIBLE_DEVICES=4`)
3. `W2-shuffle` 널 학습
4. 대회 평가식 원본으로 연도전이 홀드아웃 평가
5. CF 질량비 표 산출 (§1-4)
6. 리포트 작성 → `DACON_풍력_전체기록.md`에 TASK-METRICW 추가
7. **제출 여부는 헤드에이전트/사용자 판단 대기** — 자동 제출 금지

## 6. 완료 보고 형식

```
[TASK-METRICW 완료]
유효표본: G1 ..% / G2 ..% / G3 ..% (마스크 통과율)
연도전이 홀드아웃 ΔTOTAL (vs W0, 3seed 평균):
  W1 = ...  (seed 양수 ./3)
  W2 = ...  (seed 양수 ./3)
  W2-shuffle(널) = ...
CF 질량비 변화:
  G3 [0.85,0.95): 0.06 → W2 ...
  G3 [0.95,1.10): 0.00 → W2 ...
  G1 [0.95,1.10): 0.26 → W2 ...
  G2 [0.95,1.10): 0.24 → W2 ...
  [0.10,0.30) 과잉: 1.35~1.45 → W2 ...
결정성: md5 재현 PASS/FAIL
판정: 채택 / 무판정 / 기각
```
