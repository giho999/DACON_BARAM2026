# 실험 지시문 — 딥 시계열 모델 × 분포이동 보정 (v9 후보) · 클로드코드용

작성: 2026-07-21 (Cowork) · 실행: 서버 클로드코드(GPU6, PyTorch) · **판정 규칙 선등록·결과 보기 전 고정·변경 금지**

> 규율은 `DACON_풍력_전체기록.md` 7절, 특히 **13항(로컬 다중게이트 통과 ≠ 연도전이)** 을 최우선으로 반영한다. 이 문서 = 재현·판정 단일 기준.

---

## 0. 컨텍스트 (왜 이 실험인가)

- 우리 실패는 전부 한 원인으로 수렴: **2025 분포이동(adversarial AUC 0.965)** → 2024 로컬 좋음 → 2025 전이 실패(ECMWF·GEFS·SCADA·v8b).
- 지금까지는 *피처 추가*만 했고 전부 이동 벽에 부딪힘. 이번은 **접근 자체가 다름**:
  - **(A) 새 모델 클래스**: 우리 base는 트리+MLP뿐 = 시간별 독립예측 → **램프(ramp)를 못 봄**. 램프가 오차를 6/8% 밴드 밖으로 밀어 **FICR을 깎는 지점**. 시퀀스 모델이 램프·지속성을 잡고 트리와 탈상관.
  - **(B) 이동 직접 보정**: adversarial 분류기를 진단이 아니라 **importance weighting**에 사용 → 2025-닮은 훈련샘플 가중 → 전이 개선.
- A는 정밀도(FICR), B는 전이를 친다. 함께.

## 1. 목표·가설

- **가설**: 이동-보정 가중으로 학습한 시계열 모델을 새 멤버로 편입하면, (a) 램프 구간 오차↓→FICR↑, (b) 이동 강건성↑→2025 전이 성공, (c) 트리와 탈상관→앙상블 이득. TOTAL↑.

## 2. 모델 A — 딥 시계열 (PyTorch, GPU6)

- **입력 재구성**: cache 피처를 **예보일(day)별 24h 시퀀스**로 재배열 → (day, 24h, F). F = **cache_test에도 있는 NWP 피처만**(`c in cache_test.columns` 가드, train-only `clean_1/2`·타깃 제외).
- **아키텍처**(작게·강정규화): **BiGRU 1–2층(hidden 64–128)** over 24h → per-hour head → 3그룹 동시출력(공유 인코더). 대안 TCN. dropout 0.2–0.3, weight decay, **early stopping은 train 내부 홀드아웃(마지막 1–2개월)으로** — eval 절대 미사용(규율 8).
- **손실**: Huber 또는 L1(로버스트). **value-loss(FICR surrogate)는 금지** — v8b에서 전이 실패 실증(2024 밴드 과적합).
- **재현성**: torch/numpy/cuda seed 전부 고정. DL 비결정성은 GBDT보다 큼 → 2차 "오차범위 내 복원" 위해 seed·환경 기록 필수.

## 3. 이동 보정 B — Importance Weighting

- adversarial 분류기(train 2022–24 vs test 2025, **cache_test 피처만**) 학습 → $p_{\text{clf}}(x)$.
- 가중 $w_i=\mathrm{clip}\big(\frac{p_{\text{clf}}(x_i)}{1-p_{\text{clf}}(x_i)},\,[w_{lo},w_{hi}]\big)$ (예 [0.1,10]), 배치 self-normalize(분산 제어). 시계열 모델 손실에 sample weight로 적용.
- **★ Leakage 소명(필수 확인)**: 가중은 **피처만** 사용(라벨·미래정보 없음), test NWP는 예보라 예측기준시점 전 활용가능. transductive covariate-shift 보정 = 표준기법. assert: (a) 분류기 input ⊆ cache_test.columns, (b) test 라벨 미사용, (c) 가중 계산에 예측기준시점 이후 정보 없음. **소명 불확실하면 B 없이 A만 먼저 판정.**

## 4. 통합 (단일변수 원칙)

- 시계열 멤버 T 출력에 **v7 캘리 형태 강제 적용**(G1/G2 shift_only, G3 iso+shift, ±0.08 — 고정). 
- 블렌드에 T를 **사전 고정 가중으로 편입**: `ens_v9 = (3·base + 1·ms + 1·ts + 1·T)/6`, `final = (5·ens_v9 + 1·quant)/6`. 기존 GBDT base·ms·ts·quant·가중 구조 전부 v7 고정. **가중 스윕 금지.**

## 5. 평가 & ★★ 강화 전이 게이트 (v8b 교훈 핵심)

기준선 = **v7 레시피 rolling 12창 pooled TOTAL = 0.63477**. 3-seed{42,123,2024}.

**채택 = 다음 전부 (하나라도 미달 → 영구 기각):**
1. **주 게이트**: rolling 12창 pooled TOTAL diff ≥ +0.0015(단일 seed) 또는 3-seed 평균 ≥ +0.0010 ∧ 2/3 양수.
2. **★★ 연도전이 홀드아웃(v8b가 없어서 못 걸렀던 그것)**: 모델을 **2022–23만으로 학습**(B 가중은 2024를 target으로) → **2024 전체 홀드아웃**에서 이득 유지. 2024가 우리의 "2025 대리" — 여기서 무너지면 **기각**. B(이동보정)의 존재 이유가 바로 이 게이트 통과.
3. **겨울편향 게이트**: 월별 이득이 겨울(11–2월)에만 집중 아님(비겨울 2계절 이상 양수).
4. **큰 이득(+0.005급)이면 널테스트**: 셀프블렌드 diff≈0 ∧ 시간셔플 이득 소멸.
5. **leakage assert**(3절) 통과.

**통과 시**: v9 = v7 + T 멤버 → 프로덕션 빌드 → sanity(결측·over_cap·neg 0) → **제출 1회로 LB 확정**. 로컬≠LB, LB가 최종.

**정직한 사전확률**: v8b도 로컬 전 게이트를 통과하고 LB 반전했다. 이번엔 (a) 모델 클래스가 다르고 (b) 이동보정을 명시적으로 넣고 (c) 연도전이 홀드아웃을 추가했다 — **전이 강건성을 3중으로 설계**했지만 성공 보장은 없다. 실패해도 "다른 클래스+이동보정으로도 이동 벽을 못 넘는가"의 결정적 답을 얻는다.

## 6. 회신 형식 (Cowork로)

- 3-seed rolling 12창 pooled TOTAL + 1-NMAE/FICR 분해 + diff.
- **★ 연도전이 홀드아웃(2022-23→2024) 결과** — 이득 유지 여부.
- 월별(겨울편향), leakage assert 로그, 모델·가중 하이퍼 고정값, T의 base 대비 상관(탈상관 확인).
- 채택/기각 명시.

## 7. 가드레일 (건드리지 말 것)

- 캘리 형태·기존 멤버(GBDT/MLP/ms/ts/quant)·블렌드비율 전부 v7 고정. 이 실험 = T 멤버 1개 추가 단일변수.
- PyTorch는 **자체 학습**(사전학습 모델 아님) → 규칙 무관. API 추론 없음. UTF-8.
- 단계적 실행 권고: **먼저 A만(B 없이) 빠른 스모크** → 파이프라인·leakage 확인 → 그다음 A+B 정식 12창. 스모크는 버그확인용, 판정은 12창+연도전이 홀드아웃으로만.
- group 동시출력은 NN에선 자연스러우나(공유 인코더), GBDT group joint 기각(−0.0036)과 혼동 금지 — NN은 표현공유라 다름. 단 per-group도 비교해볼 것.
