# TASK1. CNN(v7+CNN w=0.20) 게이트 역검증 — 결과

작성: 2026-07-23 (클로드코드 실행) · 목적: 채택/기각 판정 아님 — 게이트 캘리브레이션(정확성 검증)

## 배경
팀원 로컬 실험(spatial_cnn_v7_top.py, 팀원 harness)에서 v7+CNN(w=0.20) 단일시드(42) diff=+0.00282,
LB 확인 diff=+0.00258(91% 전이, 우리 최초 전이 성공 사례). 코드 감사 결과 팀원 로컬 수치에 프로토콜
편향 2건 발견: (a) CNN 조기종료 검증셋=cal창(채점 대상)이라 CNN만 y_cal을 봄(v7은 안 봄, 비대칭),
(b) 캘리 적합·채점이 동일 cal창(in-sample, held-out 분리 없음). LB는 편향과 무관(진짜 held-out)하므로
레버 자체의 진위는 이미 확인됐다는 전제 하에, "우리 게이트가 이 확인된 레버를 정확히 감지하는가"를
검증하는 것이 이 TASK의 목적.

## 이식/제거
- 아키텍처: SpatialCNN dual-branch(LDAPS 4×4 + GFS 3×3) 팀원 원본과 100% 동일, w=0.20 고정(재탐색 없음).
- 편향(a) 제거: CNN 조기종료 검증셋을 train 구간(tm) 내부 시간순 마지막 10%로 교체(cal/eval 미사용).
- 편향(b) 제거: canonical `calibrate_total`(cal에서 적합 → eval에서만 채점, check_deepseq.py와 동일 패턴)
  사용. base(v7 GBDT 4모델)는 재학습 없이 기존 m1_base_preds_s{seed}_2024-*.parquet(M1에서 canonical
  0.63477 재현 검증된 예측) 재사용.
- 코드: `cnn_gate/cnn_common.py`(공용), `gate1_rolling.py`, `gate2_year_transfer.py`, `gate3_stress.py`,
  `gen_base_holdout_seed.py`(연도전이 3-seed 베이스 보강).

## 파이프라인 정합성 확인
seed42 gate1 base 예측(unweighted 12개월 평균) = **0.63477** — canonical(M1)과 소수 5자리 완전 일치.
격자 매칭률 전 구간(cal/eval/2024 전체) 100%. 버그 아님.

## 결과

| 게이트 | seed42 | seed123 | seed2024 | 3-seed 평균 |
|---|---|---|---|---|
| ① rolling 12창 pooled diff | -0.00599 (4/12+) | +0.00350 (10/12+) | +0.00055 (5/12+) | **-0.00065** |
| ② 연도전이 홀드아웃(2022-23→2024) diff | -0.00161 (4/11+) | +0.00289 (9/11+) | +0.00153 (7/11+) | **+0.00093** |
| ③ D2 스트레스셋(n=1756, 매칭 100%) diff | -0.00325 | +0.00520 | +0.00030 | **+0.00075** |

## 판정 (선등록 기준 대비)
③(스트레스) = +0.00075는 M1의 "사실상 0"(-0.0000078)과 달리 명목상 양수이며, ①(-0.00065)·②(+0.00093)와
같은 규모대(│diff│~0.0006~0.0009)에 있음 — 스트레스셋만 유독 붕괴하는 M1형 "거짓음성" 패턴은 **재현되지
않음**. 즉 게이트③ 자체의 정확성에 대한 반증은 없음(게이트 신뢰 유지 쪽).

다만 이 결과가 가리키는 더 근본적인 사실: **게이트①(가장 일반적인 검증)조차 3-seed 평균이 음수**이고,
세 게이트 모두 절대값이 게이트 판정 임계값(M1 기준 +0.0005~+0.0010대) 규모를 넘지 못하며 부호가 시드에
따라 뒤집힌다. 이는 "CNN 레버가 확인됐다"는 전제와 "우리 게이트가 그것을 못 잡는다"는 단순 구도가 아니라,
**편향 제거 시 로컬 신호 자체가 노이즈 수준으로 무너진다**는 뜻.

## 기준 외 관찰 (참고용, 판정에 미사용)
- **동일시드(42) 극적 반전**: 팀원 원본(편향 포함) seed42 로컬 diff=+0.00282(LB +0.00258로 확인). 아키텍처·
  w=0.20·데이터 전부 동일한 우리 seed42는 세 게이트 전부 음수(-0.00599/-0.00161/-0.00325). 바뀐 것은
  오직 두 편향 제거뿐인데 부호가 완전히 뒤집힘 — 팀원 로컬 수치가 두 편향에 의해 최소 부분적으로 부풀려졌을
  개연성이 높음을 시사(편향(a)와 (b) 각각의 기여도는 분리 측정하지 않았음).
- **시드간 분산이 게이트 임계값보다 훨씬 큼**: 게이트①범위 -0.00599~+0.00350(폭 0.0095). 단일시드(팀원의
  seed42) 판단이 구조적으로 불안정한 근거였음을 시사.
- LB(+0.00258)는 우리가 재현할 수 없는 진짜 held-out 신호이므로 이 결과가 LB 자체를 부정하지는 않음.
  다만 production `v7_cnn_submission.py`는 rolling 재학습이 아니라 2022~2024 전체 데이터 1회 학습
  방식이라, 우리 rolling(월별 재학습, 16k~24k행)·연도전이(2022-23만, 17k행) 실험과 학습 데이터
  규모/조건이 다름 — LB 성공이 "일반적으로 안정적인 이득"이 아니라 "특정 대규모 1회 학습 조건에서만
  나타나는 효과"일 가능성을 배제할 수 없음.

## 다음 액션
게이트③ 자체는 정확성 반증 없음(신뢰 유지). 그러나 CNN 레버의 로컬 신호가 편향 제거 시 시드에 따라
부호가 뒤집힐 만큼 약함이 확인됐으므로, 전략 리드/사용자에게 이 결과를 보고하고 다음 지시(예: 원 팀원
프로토콜을 seed123/2024로도 LB 재현성 확인, 또는 production 방식대로 재학습해 우리 harness로 재평가)를
대기.
