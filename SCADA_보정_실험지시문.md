# 실험 지시문 — SCADA 실측풍속 잔차보정 (v8 후보) · 클로드코드용

작성: 2026-07-21 (Cowork) · 실행: 서버 클로드코드(GPU6) · **판정 규칙 선등록, 결과 보기 전 고정, 변경 금지**

> 이 문서 하나로 실험을 재현·판정한다. 규율은 `DACON_풍력_전체기록.md` 7절을 그대로 따른다. 붙여넣고 그대로 실행.

---

## 0. 컨텍스트 (왜 이 실험인가)

- 현 best = v7 (LB 0.64926). 우리 base 짜내기 레버는 전부 닫힘(HPO·stacking·피처·캘리·ECMWF·ASOS·GEFS·v7a 블렌드비율).
- **프론티어 재진단(전체기록 3절)**: 상위권과의 격차 ~0.02 중 **60%가 FICR** = 고출력 시간대 시간별 정밀도. FICR은 재가중으로 안 짜이고(v7a 플랫 실증) **base 정밀도의 하류 보상**이다.
- 남은 유일한 in-hand 정밀도 레버 = **SCADA 실측풍속으로 NWP 풍속의 계통편차를 데이터드리븐 보정**. 이게 이 실험.

## 1. 목표·가설

- **가설**: NWP 풍속(LDAPS/GFS)은 현장 허브풍속 대비 방향·레짐 의존 계통편차가 있다. 이를 `meas_ws_gX`로 학습해 보정한 풍속 피처를 넣으면 고출력 구간 오차가 줄어 **FICR↑(+NMAE↑)**.
- **★ 정직한 사전확률(반드시 인지)**: 팀 피처에 **이미 `ws_corr_kpx_group_1/2/3`(SCADA 기반 풍속 보정)이 존재**하고 강한 기여자였다. 따라서 이 실험이 증명해야 할 것은 "풍속 보정이 유효하다"가 아니라 **기존 ws_corr_ 위에 증분이 있는가**다. 증분이 0이면(=ws_corr_가 이미 다 잡음) **기각이 정상 결과**다. 규율 있는 한 방일 뿐, 통과를 기대하지 말 것.

## 2. 데이터 & Leakage 설계 (실험의 핵심 — 여기가 틀리면 전부 무효)

- **타깃**: `outputs/scada_meas_hourly.parquet` = `t, meas_ws_g1, meas_ws_g2, meas_ws_g3` (그룹별 실측 허브풍속). 범위 **2022-01 ~ 2024-12**(마지막 행 2025-01-01 00:00), **2025 test와 겹침 0**. `meas_ws_g3`는 초기 NaN(G3 늦은 가동) — NaN 행은 학습 제외.
- **보정함수 INPUT**: **cache_test에도 존재하는 NWP 풍속계열 컬럼만** 사용(예: `gfs_ws_100m_mean_g*`, `gfs_ws_80m_*`, `ldaps_ws_mean/50m_g*`, `*_ws_sin/cos_*`(풍향), `*_gust_*`, `*_shear_*`, `atmos_stability`, hour). 전체 목록은 `[c for c in cache_test.columns if <풍속계열>]`로 뽑아 고정.
- **절대 금지(train-only, `c in cache_test.columns` 가드 필수)**: `kpx_group_1/2/3`(타깃), `clean_1`, `clean_2`. 그리고 **`meas_ws_gX`는 오직 학습 타깃 — 추론 피처로 절대 넣지 말 것.**
- **Leakage 논증(소명용)**: 보정함수 $g$는 ≤2024 SCADA로 **학습해 고정(freeze)**, 추론 시 2025 test의 **NWP 피처에만** 적용. 추론에 실시간/사후 SCADA·재분석 불사용. → 각 예측값의 예측기준시점 이전 정보만 사용. leakage assert로 강제: (a) $g$ input 컬럼 ⊆ cache_test.columns, (b) SCADA 파일 max(t) < 2025 학습컷, (c) 생성 피처가 test NWP만으로 계산됨.
- **외부데이터 아님**: SCADA는 주최측 제공 데이터(`외부데이터_정리.md` D절) → 외부데이터 소명 의무 없음. 단 train-only 격리는 필수.

## 3. Phase 1 — 보정함수 구축

1. NWP 풍속 피처 + hour + 풍향(sin/cos) + stability를 input으로, **그룹별** `meas_ws_gX`를 예측하는 회귀기 $g_{\text{group}}$ 학습(LightGBM 등, 얕게 — 과적합·연도전이 취약 회피). 
2. 산출 피처: `scada_corr_ws_g1/g2/g3`(예측 실측풍속) + 파생 `scada_corr_ws_cubed_gX`(3승법칙). test 구간은 2025 NWP에 $g$ 적용해 생성.
3. **차별화 포인트(기존 ws_corr_ 대비 증분을 노리는 지점)**:
   - **레짐 가중**: 고풍속(고출력) 구간 오차에 가중해 적합 — FICR이 값을 매기는 구간 집중.
   - **방향 섹터**: 풍향 sin/cos로 지형·후류 방향성 반영.
   - **그룹별 개별**: 특히 **G3**(다른 기종 U136, ms/ts가 끌어내리고 캘리도 iso+shift 필요했던 문제 그룹).

## 4. Phase 2 — 통합·평가 (rolling 12창, 프록시 금지)

- 기준선 = **v7 레시피 rolling 12창 pooled TOTAL = 0.63477**(전체기록). `rolling_eval.py`(12창, `--save_preds`) + v7 학습 스크립트(`train_msecalib_ensemble_teamfeat_v3b.py` 계열의 v7 사양) 사용.
- 처치 = v7 **전체 피처셋(ws_corr_ 포함) + `scada_corr_ws_*`** → 재학습 → rolling 12창 pooled.
- **증분 분해(필수)**: 아래 세 조건을 모두 돌려 "새 보정이 ws_corr_ 중복인지 진짜 증분인지" 판정:
  - (A) v7 그대로 (기준선)
  - (B) v7 + scada_corr (증분 여부)
  - (C) v7 − ws_corr_ + scada_corr (새 보정이 기존 걸 대체 가능한가)
- 캘리 형태·SHIFT_GRID·블렌드비율 **전부 v7 고정**(규율 4). 이 실험은 **피처 1~3개 추가만** 변경.

## 5. ★ 선등록 판정 게이트 (지금 고정 · 결과 보고 후 변경 금지)

**채택 = 다음 전부 충족:**

1. **주 게이트**: rolling 12창 pooled TOTAL diff(B vs A) ≥ **+0.0015**(단일 seed) 또는 3-seed{42,123,2024} 평균 ≥ **+0.0010** ∧ 2/3 seed 양수.
2. **★ 연도전이 게이트(ECMWF·GEFS를 죽인 그 함정)**: 보정함수를 **2022–2023만으로 학습**해 **2024 홀드아웃**에 적용했을 때도 rolling 이득이 **유지**돼야 함. 학습연도와 평가연도가 겹칠 때만 이득이 나면(=같은 해 패턴 암기) **기각**. 2024→2025 일반화의 대리 검증.
3. **증분 게이트**: (B) − (A) 이득의 상당분이 (C)에서도 재현 = 새 보정이 ws_corr_의 단순 중복이 아님. 만약 (B)≈(A)면 "ws_corr_가 이미 다 잡음" → 기각.
4. **큰 이득 시 널테스트(+0.005급이면 필수)**: 셀프블렌드 diff≈0.000000 ∧ 시간셔플 시 이득 소멸(진짜 시점정렬 신호 확인).
5. **leakage assert 통과**(2절 (a)(b)(c)).

**하나라도 미달 → 영구 기각·재시도 없음**(GEFS·v7a와 동일 원칙). 기준 +0.0015→+0.0010은 3-seed 평균의 √3 노이즈 감소 등가.

**채택 시**: v8 = v7 + `scada_corr_ws_*` → 프로덕션 빌드 → sanity(결측·over_cap·neg 0, 그룹 corr) → **제출 1회**. 그 제출 결과가 최종 판정(로컬≠LB, 규율 6).

## 6. 산출물·보고 형식 (Cowork로 회신)

- 3-seed × {A,B,C} rolling 12창 pooled TOTAL 표 + diff.
- 연도전이 게이트 결과(2022-23학습→2024홀드아웃 diff).
- 월별 diff 분해(양수 월 수) + 이득이 **어느 그룹·어느 출력구간(고/저)**에서 나오는지 — FICR 가설 검증용.
- leakage assert 로그.
- 판정: 위 게이트로 채택/기각 명시.

## 7. 가드레일 (건드리지 말 것)

- 캘리 형태(G1/G2 shift_only, G3 iso+shift, SHIFT_GRID ±0.08), GBDT 하이퍼파라미터, 블렌드비율(3:1:1·quant w=1) — **전부 v7 고정**. 이 실험은 피처 추가 단일 변수.
- 새 패키지 불필요(SCADA는 parquet, herbie 등 없음) → 프로덕션 venv 그대로.
- 4-9a의 **수동물리 보정(허브외삽·밀도 등가풍속)과 혼동 금지** — 그건 generic 공식이라 실패했고, 이건 **실측 SCADA로 학습한 site-specific 보정**이다.
- GBDT 비결정성: seed 고정, paired 비교(같은 seed 내 A/B/C).
- 프록시 검증 금지(규율 12): 단일창 스모크테스트는 **버그 확인용만**, 판정은 12창으로만.
