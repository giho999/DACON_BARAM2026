
# 실험 지시문 — Value-oriented FICR 정렬 손실 (v8b 후보) · 클로드코드용

작성: 2026-07-21 (Cowork) · 실행: 서버 클로드코드(GPU6) · **판정 규칙 선등록·결과 보기 전 고정·변경 금지**
문헌 근거: arXiv 2310.00571(value-oriented loss 유도), 2501.16086(정산 정렬 손실).

> 규율은 `DACON_풍력_전체기록.md` 7절을 그대로 따른다. 이 문서 = 재현·판정의 단일 기준.

---

## 0. 컨텍스트 (왜 이 실험인가)

- 우리 in-hand 데이터·피처 레버는 전부 닫힘. 프론티어 격차의 **60%가 FICR**(고출력 시간별 정밀도)인데, 우리 FICR은 0.428로 정체.
- 지금까지 FICR은 **간접**(quantile 멤버·FICR-max shift 캘리)으로만 공략 → 전부 정점/기각. **직접 학습손실로 FICR을 겨냥한 적은 없음.**
- 이 레버의 유일한 강점: **외부데이터도 연도전이도 필요 없음** → ECMWF·GEFS·SCADA를 죽인 "전이 벽"을 원천 우회. 병목(FICR)을 학습단에서 정면으로 친다.
- **정직한 사전확률**: FICR 레버는 우리 기록에서 반복적으로 신기루였다(quant w, 유효시간 가중 w=2 희석 +0.0003, C1 결정레이어 기각). *겨울-편향* 위험이 크다. 기각이 정상 결과일 수 있음. 규율 있는 한 방일 뿐.

## 1. 목표·가설

- **가설**: MSE/L1 대신 FICR의 정산구조(6%/8% 밴드 × 발측가중)에 정렬된 손실로 base 멤버를 하나 학습해 앙상블에 편입하면, 고출력 구간 오차가 6%cap 밑으로 더 눌려 **FICR↑**, TOTAL↑.

## 2. 손실 설계 (핵심)

FICR 시간별 가치 = $y_i\,u(e_i)$, $e_i=|\hat y_i-y_i|/C_g$, $u=4(e\le.06)/3(.06<e\le.08)/0$. 최소화 대상 = 손실정산 $L_i=y_i(4-u(e_i))$.

- **미분가능 surrogate**: $\tilde p(e)=\sigma\!\big(\tfrac{e-0.06}{\tau}\big)+3\,\sigma\!\big(\tfrac{e-0.08}{\tau}\big)$ (τ≈0.01), 손실 $\ell_i=w_i\,y_i\,\tilde p(e_i)$. `y_i` 곱 = FICR의 발측가중과 정합.
- **grad/hess** (LightGBM custom objective, $\hat y$ 기준): $\frac{de}{d\hat y}=\mathrm{sign}(\hat y-y)/C$, $\frac{d\tilde p}{de}=\tfrac1\tau[\sigma'(\tfrac{e-.06}\tau)+3\sigma'(\tfrac{e-.08}\tau)]$, $\sigma'(x)=\sigma(x)(1-\sigma(x))$. grad $=w_i y_i\,\tilde p'\,\tfrac{de}{d\hat y}$. hess는 Gauss-Newton 근사(작은 양수 하한으로 안정화).
- **유효구간 가중** $w_i$: 채점이 $y\ge0.1C$에서만이므로 $w_i=\mathbb 1[y_i\ge0.1C_g]$(또는 부드럽게). 저출력은 손실 0.

**두 변형(순차):**
- **B1(로버스트, 먼저)**: custom objective 없이 **sample_weight ∝ $y_i$**(유효구간)로 기존 L1 멤버 재학습 — 실측가중만으로 FICR 정렬. 저리스크·저비용. (주의: 유효시간 균일가중 w=2는 이미 희석 실증 — 이건 *고출력에 연속 비례* 가중이라 다름.)
- **B2(타깃, B1이 신호 보이면)**: 위 surrogate custom objective 멤버.

## 3. 통합 (단일변수 원칙)

- v7 파이프라인에 **value-loss GBDT 멤버 1개 추가**(L1·MLP를 붙였던 established 패턴과 동일). 기존 4-GBDT+MLP·캘리 형태·블렌드 구조·quant w=1 **전부 고정**.
- 멤버 가중은 **사전 고정**(예: GBDT 앙상블 내 동일가중, 또는 소가중 1개 값)으로 시작. 가중 스윕 금지(과적합).

## 4. 평가 (rolling 12창, 프록시 금지)

- 기준선 = **v7 레시피 rolling 12창 pooled TOTAL = 0.63477**. `rolling_eval.py`(12창) + v7 학습 스크립트.
- 3분해: A(v7) / B1(v7 + y가중 멤버) / B2(v7 + surrogate 멤버). 3-seed{42,123,2024} paired.
- **1-NMAE / FICR 분해 필수**: 이득이 FICR에서 오는지(가설), NMAE 희생은 없는지 확인. TOTAL 게이트가 tradeoff를 자동 반영.

## 5. ★ 선등록 판정 게이트 (지금 고정 · 변경 금지)

**채택 = 전부 충족:**
1. **주 게이트**: rolling 12창 pooled TOTAL diff(B−A) ≥ **+0.0015**(단일 seed) 또는 3-seed 평균 ≥ **+0.0010** ∧ 2/3 양수.
2. **★ 겨울편향 게이트(이 레버의 핵심 위험)**: 월별 분해에서 이득이 **겨울(11–2월)에 집중되지 않을 것** — 최소 비겨울 계절 중 2개 이상에서도 양수. 겨울에만 양수면 신기루로 판정, **기각**. (기록 7절 4항)
3. **큰 이득(+0.005급)이면 널테스트**: 셀프블렌드 diff≈0 ∧ 시간셔플 시 이득 소멸.

**하나라도 미달 → 영구 기각·재시도 없음**(GEFS·v7a·SCADA와 동일). B1 미달이면 B2도 실행 안 함(신호 없음).

**채택 시**: v8b = v7 + value 멤버 → 프로덕션 빌드 → sanity(결측·over_cap·neg 0) → **제출 1회**로 최종 확정(로컬≠LB).

## 6. 회신 형식 (Cowork로)

- 3-seed × {A,B1,(B2)} rolling 12창 pooled TOTAL + 1-NMAE/FICR 분해 + diff.
- 월별 분해(겨울편향 게이트용): 계절별 양수 여부.
- τ·가중 등 하이퍼 고정값 기록. 채택/기각 명시.

## 7. 가드레일 (건드리지 말 것)

- 캘리 형태(G1/G2 shift_only, G3 iso+shift, ±0.08)·기존 멤버 하이퍼·블렌드비율·quant w=1 **전부 v7 고정**. 이 실험 = value 멤버 1개 추가 단일변수.
- surrogate는 비볼록 → 학습 불안정 시 τ 키우거나(완만) B1(로버스트)만으로 판정. τ는 사전 1값 고정(사후 튜닝 금지).
- 새 데이터·패키지 불필요. seed 고정 paired. 단일창은 버그확인용, 판정은 12창.
- 이건 **학습시 손실**이라 기각된 C1 *사후* 결정레이어와 다른 계열 — 혼동 금지.
