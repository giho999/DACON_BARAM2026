# 외부 데이터 정리

## 1. ECMWF Open Data (Open-Meteo Previous Runs API)
- **출처**: Open-Meteo Previous Runs API (`https://previous-runs-api.open-meteo.com/v1/forecast`), model=`ecmwf_ifs025`
- **원본 소스**: ECMWF 무료 오픈데이터(AWS/Azure/GCP 미러, CC-BY-4.0)를 Open-Meteo가 재가공
- **좌표**: 태백가덕산 LDAPS 16격자 중 대표 5지점(코너4+중심1), `data/ldaps_train.csv`의 grid_id/lat/lon에서 추출 (`ld_coords.parquet`는 프로젝트에 실존하지 않음)
- **실측 커버리지**: 2024-03-08부터 완전 채워짐(그 이전은 NaN) — 문서상 "2024년 1월부터"와 달리 실제 확인 결과 3월 초부터
- **leakage 근거**: `previous_day2`(밸리드타임 기준 48h 전 예측) 사용, `previous_day1`은 배포시각(00z 공개 05:45~07:34 UTC)이 예측기준시점(전일 04~05 UTC)보다 늦어 사용 금지 — `ecmwf_pipeline/leakage_justification.md`에 상세
- **채택 결과**:
  - 전체 히스토리(2022~) 학습: rolling 6창(2024-07~12) pooled TOTAL diff **-0.00298 (기각)** — 2022~2024초 NaN 희석 문제
  - **B안(2024-01+ 전용 학습)**: pooled TOTAL diff **+0.00711 (채택)**, self-blend 널테스트(diff=0)/시간축 셔플 널테스트(diff=-0.026) 둘 다 통과 — 진짜 신호로 확인
  - 프로덕션: `submission_ficr_w1_v4.csv` (v3b:planB = 5:1, cal=2024-11~12 고정, G3 형태는 v3b와 동일 강제)

## 2. 기상청 ASOS 태백(216) 시간자료 — **기각**
- **출처 URL**: 기상청 API허브 `https://apihub.kma.go.kr/`, 엔드포인트 `api/typ01/url/kma_sfctm3.php` (범위조회, `tm1`/`tm2` 파라미터 사용)
  - ⚠️ `kma_sfctm2.php`는 **단일시점 조회 전용**(`tm` 파라미터만 인식, `tm1`/`tm2`를 넘겨도 무시하고 항상 "현재" 관측 1행만 반환) — 최초 시도 시 오탐(2026-07/2025-07/2024-01 세 범위가 모두 동일한 "현재" 행 반환)으로 발견, `help=1` 문서에서 확인 후 `kma_sfctm3.php`로 전환
  - ⚠️ `kma_sfctm3.php`는 요청 기간이 약 31일을 넘으면 **에러 없이 최근 ~31일치로 조용히 잘라서 반환**(90일 요청 시 745행만, 그것도 tm2 기준 최근 구간만) — chunk를 30일 이하로 제한해 우회
- **지점**: 태백 ASOS **216번**
- **라이선스**: 공공누리 제1유형(출처표시), 무료 재사용 가능
- **접근 방법**: apihub.kma.go.kr 계정 가입 + 휴대폰 인증 → authKey 발급은 자동이지만, **API 상품별 "활용신청"이 별도 필요**(authKey만으로는 403 "활용신청이 필요한 API 입니다") — 사용자 본인이 활용신청 완료 후 정상 수집됨(2026-07-20)
- **⚠️ 공개시각 근거(leakage 설계의 핵심)**:
  - data.kma.go.kr ASOS 자료 페이지 원문(WebFetch로 확인, 2026-07-19): **"전일 자료는 당일 10시 이후 확인 가능"**
  - 즉 관측일(D-1) 전체가 다음날(D) 10시에 공개됨 — "관측 후 수 시간 내 공개"라는 최초 가정은 틀렸음(실제 지연 약 10~34시간)
  - **D-2 컷오프 논증**: 예측기준시점 = D-1 13:00 KST. D-2일 자료는 D-1 10:00에 공개 완료 → 기준시점(D-1 13:00)보다 **3시간 여유** 있게 안전. 따라서 관측 사용 상한을 **D-2 23:00**으로 확정.
  - 검증: 피처 계산에 쓰인 관측의 `max(kst_dtm) ≤ D-2 23:00`를 assert로 강제(`asos_features.py` `verify_leakage()` + `main()` 최종 어서션, 2단계 모두 통과)
- **롤링 윈도**: 7일 = D-8~D-2, 14일 = D-15~D-2, 최근3일 = D-4~D-2 (모두 D-2 마감 기준)
- **피처**: `asos_ws_mean_{7d,14d}`, `asos_ws_day_mean_7d`/`night_mean_7d`(06-18시/야간), `asos_sector_{N,E,S,W}_7d`(풍향 4분면 비율), `asos_ws_recent3d_mean`, `has_asos`
- **수집 결과**: 2021-12-01~2025-12-31 (KST), 35,755행, 결측(ws_obs/wd_obs) 총 20건뿐 — 프로젝트 학습/테스트 전체 커버
- **1차 범위**: ASOS 216 단독. 인근 AWS 지점 확장 조건은 216 단독 신호 +0.001 이상 — **미충족으로 확장 없음(아래 참조)**
- **수집/검증일**: 2026-07-20
- **rolling 12개월(2024) pooled 검증 결과**: base(ASOS 미사용) TOTAL-max 평균 **0.63868** vs base+ASOS **0.63489** → **diff = -0.00379** (채택 기준 +0.0015에 크게 미달, 부호도 반대)
  - 12개월 중 9개월 악화(Jan/Apr/Oct만 개선), 캘리브레이션 이전 raw 점수도 악화(0.61770→0.61448) — 문제가 캘리브레이션이 아니라 모델 예측 자체에서 발생
- **결론**: **기각**. ASOS 216 단독 신호가 명확한 마이너스이므로 사전 정책("+0.001 이상일 때만 인근 AWS 확장")에 따라 인근 지점 확장 없이 파이프라인 종결. 프로덕션 미반영.
- **코드**: `asos_pipeline/asos_fetch.py`(수집), `asos_pipeline/asos_features.py`(D-2 컷오프 피처+leakage 검증+캐시 병합), `rolling_asos_check.sh`(12개월 rolling 검증, `rolling_eval.py --use_asos`)

## 3. NOAA GEFS 앙상블(mean/spread) — **기각 (영구, 재시도 없음)**
- **출처**: AWS 공개버킷 `s3://noaa-gefs-pds`(미 정부 퍼블릭도메인, 라이선스 문제 없음), 서브셋 다운로드는 `herbie-data` 사용
- **⚠️ 환경 격리 필수**: `herbie-data`(→numpy2/pandas3/xarray 등 최신 스택 요구)를 시스템 python에 pip install하면
  기존 프로덕션 스택(anaconda의 numpy1.26/pandas1.5, pyarrow11 기반 lightgbm/xgboost/catboost/parquet)이
  전부 깨짐(2026-07-21 실제 발생·즉시 복구). **반드시 별도 venv**(`gefs_pipeline/.venv`)에서만 herbie 사용,
  메인 환경은 절대 건드리지 않을 것.
- **제품**: `gefs.{YYYYMMDD}/{cycle}/atmos/pgrb2sp25/ge{avg,spr}.t{cycle}z.pgrb2s.0p25.f{lead:03d}` (0.25° 사전집계
  앙상블 평균/스프레드, herbie `product="atmos.25"`로 접근). **10m U/V는 확실히 존재**. **100m U/V는 이 사전집계
  제품엔 없음**(개별 멤버 pgrb2b에만 있음, 31개 멤버 각각 다운로드+수동집계 필요해 과함 → 이번 범위 제외).
- **0단계 타당성 검증 결과(2026-07-21)**:
  - 아카이브 완전성: 2021-12~2025-12 월간격 50개 표본점검 **결측 0건**(최대리드 f051 포함)
  - herbie 서브셋 다운로드 정상 동작(byte-range, idx 기반)
  - 용량/시간 실측(32-worker 병렬, S3 직접 접근이라 NOMADS같은 rate limit 없음): 파일당 평균 1.37MB,
    0.722s/파일 → 전체 1461일×18파일(9리드×2제품)=26,298개 = **약 5.3시간, 36GB** 예상 → 48h 게이트 여유있게 통과
    (리드간격 6h 축소 불필요)
- **leakage 설계**:
  - 예측기준시점 = D-1 13~14 KST(=D-1 04~05 UTC)
  - D-1 00 UTC cycle: 공개시각(cycle+4~5h ≈ D-1 04~05 UTC)이 기준시점과 겹침 → **사용 금지**
  - **채택: D-2 12 UTC cycle** — 공개 ≈ D-2 16~17 UTC, 기준시점(D-1 04~05 UTC)보다 **약 36시간 여유** 있게 안전
  - target일 D의 01~24 KST(=D-1 16시~D 15시 UTC 구간)를 D-2 12z cycle 기준 리드타임으로 매핑하면 **f027~f051**
    (3h 간격 9개 파일: f027,030,...,051)
  - 결측 cycle 대응: D-3 12z로 폴백(리드 51~75h), `fallback_flag`로 기록해 피처에 포함
  - (NOAA dissemination 스케줄 근거는 GEFS 공식 문서 기반 — ASOS 사례처럼 공개시각을 반드시 원문 확인하는 절차를
    동일하게 적용함. 상세 캡처는 `gefs_pipeline/` 내 보관 예정)
- **1차 범위**: 태백 인근 0.25° 격자 4점, 변수 gefs_ws_mean/gefs_ws_spread/gefs_ws_spread_rel/u_mean/v_mean,
  ldaps_ws−gefs_ws_mean(모델간 괴리), fallback_flag. 멤버 모델 없이 cache에 직접 피처로 병합(전기간 커버라 NaN 희석
  문제 없음, ECMWF planB 같은 커버리지 함정 회피).
- **1단계 수집 결과(2026-07-21)**: 1492 cycle 전량 성공, fallback 16건(1.07%), 7시간 소요.
  `gefs_pipeline/collect_gefs.py` → `gefs_raw_long.parquet`(13,428행) → `build_gefs_features.py` →
  `gefs_features.parquet`(35,809시간, 2021-12-03~2026-01-03).
- **2단계 판정 결과**: rolling 12창, 기준선=v7 레시피(clean-downweight+subsample_freq수정). config 3분해
  (mean만/spread만/전부):
  - mean만: diff **-0.00329 (기각)** — 기존 GFS 결정론 예보와 중복돼 오히려 해가 됨(가설 확인)
  - spread만: diff **+0.00144** (게이트 +0.0015에 근소 미달, 유일한 양의 신호)
  - 전부: diff **-0.00168 (기각)** — mean이 spread의 이득을 상쇄
- **spread 3-seed 안정성 재확인**: 사전 고정 판정규칙(①3seed평균diff≥+0.0010 ②2/3seed양수
  ③seed42월별양수≥7/12, 하나라도 미달시 영구기각) 적용.
  seed42 +0.00144 / seed123 +0.00203 / seed2024 **-0.00084**(부호반전) → 3seed평균 **+0.00088**
  → 기준①(≥0.0010) 미달, 기준②③은 충족했으나 규칙상 **영구 기각**.
  (seed42 월별진단: 7/12 양수였으나 최대기여월 2024-04가 양수합의 70.9% 차지 — 단일월 편중 신호였고
  3-seed 재현에서 부호반전으로 노이즈였음이 실증됨)
- **결론**: GEFS 라인 완전 종결(mean/spread/both 전부 기각 확정, 재시도 없음). v7이 최종 프로덕션.
- **상태**: 종결. 데이터(`gefs_pipeline/gefs_raw_long.parquet`, `gefs_features.parquet`)는 보존 —
  향후 다른 리드타임/피처링 접근 시 재수집 없이 재사용 가능.

## 4. 사전학습 에뮬레이터 (DEVINE / FuXi-CFD) — TASK16b 규칙 심사 (2026-07-25) — **둘 다 기각**
전략 리드 지정 3조건(①가중치 2026-07-05까지 공개 ②라이선스가 사용·수정·배포·상업적 이용 모두 허용
③로컬 로드 실행 가능, 하나라도 미달 시 즉시 종료) 적용. WebSearch/WebFetch로 확인(2026-07-25).

### FuXi-CFD (Fudan대, Nature Communications 2026, "Reconstructing fine-scale 3D wind fields with
terrain-informed machine learning")
- ① 가중치 공개일: **2026-02-25**(Zenodo record, HuggingFace `linchensen/FuXi-CFD-model`에 실제 가중치
  호스팅 확인) → 컷오프(2026-07-05) **이전, 충족**
- ② 라이선스: **CC BY-NC 4.0** (비상업 조건, Zenodo record 명시) → **상업적 이용 불허, 미충족**
- → **②에서 즉시 기각**(③ 로컬 로드 가능 여부는 미충족 확인 후 검토 중단, 미확인)

### DEVINE (Le Toumelin et al., Météo-France, NPG 2024 "A two-fold deep-learning strategy to correct
and downscale winds over mountains")
- 코드/Zenodo(DOI 10.5281/zenodo.10594274, 2024)는 존재하나, GitHub 저장소
  (`louisletoumelin/wind_downscaling_cnn`, WebFetch로 README 확인)에 **`LICENSE ← To be created`로
  명시** — 라이선스 미확정 상태
- ② 라이선스: **미명시** → 전략 리드 지정 규칙("라이선스 미명시 → 전부 불가")에 따라 **즉시 기각**
- (① 가중치가 코드와 함께 실제 포함되는지, ③ 로컬 로드 가능한지는 ②에서 이미 기각되어 미확인)

### 결론
**둘 다 기각**. WindFM(자기회귀·NWP미입력·2025 test 발전량 히스토리 부재)에 이어 사전학습 에뮬레이터
라인도 종결 — 이 세션에서 검토한 외부 사전학습 모델(WindFM/DEVINE/FuXi-CFD) 전부 라이선스 또는 입력
가용성 문제로 실패.

## 5. TASK23 — 공개 NWP 실사(반나절, 조사 전용, 데이터 미수집) (2026-07-25)
목적: 우리 격자(37.28N,128.96E) 커버, day-ahead(전일 13-14시 KST) leakage 안전, 공개+상업이용 허용+
파일다운로드(비-API), 재현가능 4요건을 전부 만족하는 신규 NWP 후보 존재 여부 확정(A' 재개 여부 판단용).
WebSearch/WebFetch로 확인(2026-07-25), 데이터 수집·모델링 없음.

| 후보 | ①커버리지(2022-01~2025-12) | ②leakage(run-time 명시) | ③공개+상업라이선스+파일다운로드 | ④재현가능 | 결론 |
|---|---|---|---|---|---|
| GFS 0.25°(NCAR GDEX ds084.1, AWS 미러) | O(2015-01~현재 연속, 소스: gdex.ucar.edu) | O(GRIB2 표준 = ref/lead time 내장, 우리 기존 gfs_* 파이프라인과 동일 leakage 설계 재사용 가능) | O(CC BY 4.0 상업이용/재배포 허용, THREDDS/AWS 파일다운로드, API 아님) | O | **형식상 통과(단, 아래 캐비어트)** |
| ICON(DWD, Open-Meteo 경유) | **X** — Previous Runs API(leakage-안전 리드타임 선택에 필수)는 **2024-01부터만 아카이브**(2022~2023 전체 공백, ECMWF 전철보다 심함). Historical Forecast API는 2022-11부터지만 "최근 수시간 스티칭"만 제공해 day-ahead 리드타임 선택 자체가 불가 | 부분적(Historical Forecast API는 run-time 노출 안 함, Single Runs API 필요하나 커버리지 미확인) | O(라이선스는 기존 ECMWF 건에서 이미 확인) | — | **기각(①실패, 정확히 예견된 "공백월" 패턴)** |
| GDPS/RDPS(기상청 API허브/자료개방포털) | **X** — RDAPS 페이지 명시: "최근 1년 자료까지만 제공"(rolling), 2022-2024 접근 불가. 대용량자료신청(수동 승인)이 더 과거를 커버하는지 미확인·불확실 | 미확인(①실패로 중단) | 부분(승인기반 신청, 순수 파일다운로드 아닐 수 있음) | 미확인 | **기각(①실패, ASOS 사례와 동일한 "rolling 제한" 재현)** |
| ERA5(ECMWF 재분석) | 해당없음 | 해당없음 | 해당없음 | 해당없음 | **즉시 기각 — 재분석(사후자료)은 대회 규칙상 사용 자체가 금지, 4요건 확인 불필요** |
| GEFS reforecast v12 | **X** — 아카이브가 2000~2019만 커버(AWS `noaa-gefs-retrospective`), 2022~2025 데이터 자체가 존재하지 않음 | — | — | — | **기각(①실패, 명백)** |

### ★캐비어트: GFS 0.25° "통과"는 약한 통과 — 새 정보원이 아닐 가능성 높음
GFS 0.25°는 **우리가 이미 쓰고 있는 결정론 GFS의 고해상도/전변수 버전**(같은 모델, 같은 예보센터) —
독립적인 "제2의견" NWP가 아니라 같은 소스의 재탕. 이 프로젝트에서 정확히 같은 유형의 시도가 이미
실측 기각됨: §3 GEFS mean만 diff **-0.00329(기각)** — "기존 GFS 결정론 예보와 **중복**돼 오히려
해가 됨"이 확인된 실패 원인. 유일한 잠재 가치는 우리가 현재 추출 안 한 **추가 변수/기압면**(현재
피처는 gust/100m/80m/850hPa 일부만 추출, 전체 37+변수 중 일부만 사용 중) — 하지만 이는 "새 NWP"가
아니라 "같은 NWP에서 피처 재추출"이라는 다른 성격의(그리고 낮은 기대치의) 레버.

### 결론
형식적 4요건 통과 후보 **1개**(GFS 0.25° 전변수), 나머지 4개는 전부 ①(커버리지) 또는 재분석 금지
규칙에서 기각. 단 그 1개도 "새 데이터 소스"가 아니라 "기존 GFS의 미추출 변수"에 가까워 A'가 원래
전제한 "구조적으로 독립적인 멀티 NWP 컨센서스"의 취지에 부합하지 않음 — 전략 리드 재판정 시 이 점
반영 필요.

## 6. J-트랙(J0/JC) — JMA GSM (Open-Meteo Previous Runs API) — **기각(영구, 재시도 없음)**
`model=jma_gsm`, `previous_day2`. 커버리지 2022-2025 전구간 100%(허브밴드 기압면/확장고도면은
API 구조적 한계로 리키지-안전 경로 부재, 지표 세트만 획득 가능 — J0). 축소안(지표세트 전단외삽)은
전략 리드 종료. 잔여 관찰(surface_pressure≈종관정보)만 JC에서 단발 S1 스크린: F1(surface_pressure)
기존 LDAPS/GFS 지면기압과 중복(r=0.97, S1a 기각), F2(3h 기압경향)는 중복 아니나 v7+CNN
production-mode 2024 홀드아웃 부분R² 0.22%(9셀 전부 0.5% 문턱 미달, S1b 기각). 상세:
`Jtrack_J0_recon.md`, `Jtrack_JC_surface_pressure.md`. **J-트랙 완전 종결.**

## 7. TASK27 — 기상청 "대용량자료신청" 실사(조사 전용, 데이터 미수집) — **미발견·종결** (2026-07-27)
TASK23이 미확인으로 남긴 각주("GDPS/RDPS 대용량자료신청이 더 과거를 커버하는지 불확실") 해소 목적.
WebSearch/WebFetch 비로그인 조사 결과: ①기간 무공백(2022-01~2025-12) **미확인**(경량화 등급의
"최근1년" 명시는 있으나 대용량자료신청 표준등급 자체의 보존기간을 명시한 공개 페이지 부재, GDPS/KIM
전용 목록 페이지는 플레이스홀더 응답) ②GRIB2+run/lead 내장 파일명 확인(`kim_g128_ne36_pres_h???.*.gb2`
등, 충족) ③일반인 신청 가능 확인(승인 소요일수는 미확인) ④공공누리 라이선스 확인(단 일부 자료
상업제한 가능성 문구 상존). **①(결정적 항목) 미확인 → 선등록 규칙상 "하나라도 미충족" 분기 →
미발견·종결**(계정 로그인 기반 확인은 조사 범위 밖 한계로 남김). 기준 외 관찰: GDPS 명칭은 역사적으로
UM 기반이었으나 2020-04부터 한국형모델(KIM)로 완전 전환·운영 중임을 확인 — 모델 코어 자체는
LDAPS(UM)와 독립적이라는 fable 가설은 유효. 상세: `TASK27_kma_bulk_recon.md`.
**데이터 경로 종료 상태 유지 — 소명 "3중 실사" → "4중 실사"로 승격.**
