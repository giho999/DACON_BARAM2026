# 하위 모델 실행 지침 — 진단 트랙(D1~D3) · M1 · A' 스펙

작성: fable 전략기획 · 2026-07-22 · 팀 냥볶밥
공통 규율: 모든 지침은 **판정 기준이 먼저 고정된 선등록 문서**다. 결과를 본 후 기준 수정 금지.
공통 제약: Leakage 금지(예측기준시점 = 전일 13~14시 KST 이전 정보만) · Python only · API 추론 금지 · 제출은 전략 리드 승인 전 금지(**진단 트랙은 제출 0회**).
공통 환경: cs.dongguk.edu `~/DACON_baram2026`, GPU `CUDA_VISIBLE_DEVICES=4`, nohup 실행.
데이터: `cache_train/test.parquet`(456 feats), `merged_train/test_v9.csv`, `scada_meas_hourly.parquet`. train-only 컬럼은 `c in te.columns` 가드 필수.

---

## D1. ECMWF 전이 실패 부검 (기한: 7/25)

목적: planB(로컬 +0.0071 → LB −0.0002)의 실패 모드를 아래 4개 중 하나로 판정. 이 판정이 A'(물리층 멀티 NWP)의 착수/기각을 결정.

**판정 후보** (배타 아님, 주 원인 1개 지목): (a) 커버리지/NaN 희석 · (b) 학습 가중치 비전이(2024-특화 가중치가 2025 공변량 이동에서 붕괴) · (c) 레짐 편중(이득이 희소 레짐 집중) · (d) 통합 버그/staleness(낡은 예보 주입).

**실행**:
1. planB 재현 후 train(2022-24)/test(2025) 각각 ECMWF 파생 피처 결측률·유효 커버리지 월별 표. → (a).
2. 로컬 +0.0071을 월별·풍속레짐(풍속 분위 4구간)별 분해. 이득 상위 2구간이 70%↑ ∧ 2025 예상 빈도 낮음 → (c).
3. ECMWF−LDAPS 풍속 괴리 분포 2024 vs 2025 비교(KS·분위). 괴리 분포 이동 크면 → (b) 정황.
4. init time·발표시각 로그 감사: 주입 run 발표시각 ≤ 예측기준시점, >24h staleness 없는지. → (d).
5. ECMWF 피처 중요도 × 2024→2025 분포이동 크기 대조. 중요 피처일수록 이동 크면 → (b) 확정.

산출물: `D1_ecmwf_autopsy.md` — 주 원인 판정 + 근거 표 + A' 착수 권고(판정 (a)/(b)/(d)면 착수, (c)면 기각).

---

## D2. 공변량 이동 vs 개념 이동 판별 + 스트레스 게이트셋 구축 (기한: 7/26)

목적: 2025 이동 성격 규명 + 이후 **모든 레버에 적용할 "2025-유사 스트레스 게이트셋"** 산출.

**실행**:
1. adversarial(2022-24 vs 2025, AUC 0.965) 분류기 피처 중요도 상위 30 + 각 피처 2024 vs 2025 분위표.
2. ablation: 상위 k(5/10/20) 이동 피처 제거 or quantile mapping 후 AUC 재측정. AUC ≤ 0.6 되는 최소 k 보고 → 이동이 국소적인지 전면적인지.
3. 연도별 교차예측 3×3 행렬: 2022/23/24 단일연도 학습 → 타 연도 평가 TOTAL 프록시. 각 off-diagonal에 밀도비 재가중 후 재평가.
   **판정 규칙(고정)**: 재가중이 교차연도 열화의 ≥60% 회복 → "공변량 지배", <30% → "개념이동 유의", 중간 → "혼합".
4. 스트레스 게이트셋: 2024 시간대 adversarial 점수 → 상위 20%를 `stress_2025like_2024.parquet`(채점필터 적용·미적용 두 벌). v7 base TOTAL 프록시 베이스라인 기록.
5. Public 노이즈 추정(D3 공유): 2024 채점대상서 40% 부표본 2000회 부트스트랩 → TOTAL σ. **LB 판정 임계 = 1.5σ** 확정.

산출물: `D2_shift_diagnosis.md` + `stress_2025like_2024.parquet` + σ. 판정은 3의 고정 규칙으로만.

---

## D3. 프론티어 FICR 우위의 출처 분해 (기한: 7/26)

목적: 상위권 FICR 0.448~0.455 vs 우리 0.428 격차가 **base 정밀도인지 후처리인지** 정량 판정.

**실행**:
1. 2024 홀드아웃 v7 동형 예측 시간별 오차율 $e_i=|\hat y−y|/C_g$ 분포(그룹별·채점필터).
2. 균일 축소 시뮬: $e_i ← s\cdot e_i$, s∈{1.0…0.70} → FICR(s) 곡선. FICR 0.45 도달 $s^*$ 보고. ($s^*\le0.85$면 "base 정밀도 없이 불가").
3. 후처리 상한 시뮬: base 오차 고정 하 ±δ shift로 얻는 FICR 최대 개선(2024 적합→별 폴드 평가). $s^*$와 대조.
4. 그룹별: G1/G2(V126) vs G3(U136) NMAE·FICR·6%밴드 진입률. G3 열위 유의하면 G3 파워커브 재적합 후속 후보.
5. 6%/8% 밴드 경계: $e_i$ 0.06~0.08 몰린 시간 비중·특성. "밴드 직전 탈락" 질량 크면 후처리 여지, 작으면 base.

산출물: `D3_ficr_decomposition.md` — "프론티어 FICR 우위 = base X% + 후처리 Y%" + 그룹 우선순위 + 밴드 경계 질량.

---

## M1. 메트릭-구조 레버: 채점조건부 캘리브레이션 (기한: 7/27, 진단 병행 가능)

가설(선등록): 채점은 실측 $y\ge0.1C_g$ 시간만 → 최적 예측은 $E[y]$가 아니라 $E[y\mid y\ge0.1C_g]$. 경계 근처 저출력 시간대 조건부 상향 보정이 기대점수↑. **메트릭 구조 성질이라 2025 이동에 강건(전이 가설 자명).**

**실행**:
1. 2024에서 $P(y\ge0.1C\mid \text{피처})$ 추정기(분류기 or base 예측 기반 시그모이드) — 피처만, leakage 없음.
2. 보정: $\hat y$가 임계 근방(0.05C~0.20C)일 때 채점조건부 기대값 방향 shift. shift 함수 2024 적합→별 폴드 평가.
3. **게이트(v7 대비, 고정)**: 3-seed rolling pooled ≥ **+0.0005**(소형 레버 별도 카테고리 선등록) / 연도전이 홀드아웃 ≥ +0.0004 / D2 스트레스 게이트셋 ≥ +0.0003 / 널테스트 / **base 불변 assert(캘리 층 외 변경 금지)**.
4. 통과 시 전략 리드에 LB 프로브 요청(제출은 리드 승인 후).

산출물: `M1_scored_conditional_calib.md` + 게이트 결과표.

---

## A'. 물리층 멀티 NWP 컨센서스 — 착수 대기 스펙 (D1 판정 (a)/(b)/(d) 시에만 개시)

전이 가설(선등록): ECMWF planB 실패는 **ML층 통합(2024-특화 가중치)** 기인. A'는 통합을 **물리층**으로 내려 학습 파라미터를 ~2개로 제한: NWP별 허브고도 풍속 → NWP별 quantile mapping(train 연도 적합, 계통편차만) → 역-RMSE 가중 컨센서스 풍속 → base에 소수 물리 피처 주입. 물리 결합은 공변량 이동에 구조적으로 둔감, 독립 NWP 컨센서스 풍속오차 감소는 $P\propto v^3$로 증폭 회수.

**실행**:
1. 소스 우선순위: ① 기상청 KIM(apihub, 2023-02+) ② ICON(Open-Meteo — 착수 전 아카이브 커버리지 월별 실측, 2024 미만이면 기각). 각 run 발표시각 ≤ 전일 13:00 KST assert 내장.
2. 커버리지 공백(고정 규칙): NaN 주입 금지 — LDAPS/GFS-only 컨센서스 폴백 + 폴백 지시자 1개. (ECMWF NaN 희석 차단.)
3. 컨센서스: quantile mapping 2022-23 적합→2024 검증. 가중치 2024 역-RMSE 고정(추가 학습 금지).
4. **게이트(고정)**: 3-seed rolling pooled ≥ +0.0010 / 연도전이 홀드아웃 ≥ +0.0008 / D2 스트레스 게이트셋 ≥ +0.0005 / 널테스트(컨센서스 풍속 셔플 시 이득 소멸) / leakage assert / 겨울편향.
5. **LB 판정(리드 승인 후 제출 1회)**: ΔPublic ≥ 1.5σ → 채택 / 0.5σ~1.5σ → 보류·변형 1회 / <0.5σ → 영구 기각.
6. 외부데이터 소명 동시 작성(출처·발표시각·라이선스·수집 코드) → `외부데이터_정리.md` 추가.

산출물: `A1_multinwp_physical.md` + 게이트 결과표 + 소명 문서.

---

## 공통 보고 형식

각 산출물 말미에 반드시: ① 선등록 기준 대비 판정(통과/미달/기각) ② 기준 외 관찰(참고용, 판정 미사용 명시) ③ 다음 액션 권고 1줄.
**"좋아 보이는 신호"는 기준을 통과하지 못하면 버린다 — 이 규율이 팀의 자산이다.**
