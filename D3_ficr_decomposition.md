# D3 — 프론티어 FICR 우위의 출처 분해

작성: 2026-07-22 (클로드코드 실행) · 데이터: `preds_team1_nf0_ens1_s42_2024-*.parquet` (v7 GBDT base rolling OOF, 2024 전체 eval-role 8,778행), 캘리는 월별 cal→eval(leakage 없음, `deepseq_pipeline/common.py`의 `calibrate_total` 재사용).

## 헤드라인

**프론티어 FICR 우위 = base 정밀도 ~95% + 후처리 ~5% (사실상 후처리 여지 거의 없음)**

## 1. s* (균일축소 시뮬)

post-calib pooled: TOTAL=0.63535, NMAE=0.12704, **FICR=0.39774**.
e_i ← s·e_i로 축소하며 FICR(s) 재계산:

| s | 1.00 | 0.95 | 0.90 | **0.86** | 0.85 | 0.80 | 0.75 | 0.70 |
|---|---|---|---|---|---|---|---|---|
| FICR(s) | 0.398 | 0.416 | 0.435 | **0.450** | 0.457 | 0.479 | 0.503 | 0.529 |

**s\* = 0.86** — 규정 임계(0.85)에 사실상 걸침(0.01 차이, 오차 내에서 사실상 임계 충족). 오차를 ~14% 줄여야 FICR 0.45에 도달 — **후처리만으로는 불가, base 정밀도 개선이 필수**라는 판정 규칙이 (근소하게) 발동.

## 2. 후처리 shift 상한 (교차검증, 넓은 그리드 ±0.30)

H1(1~6월) eval로 그룹별 shift 적합(넓은 그리드, 프로덕션 ±0.08보다 훨씬 넓게 탐색) → H2(7~12월)에 적용:

- H1 적합 shift: G1 +0.03, G2 −0.01, G3 +0.03 (cap 대비)
- H2 원본 FICR = 0.39892 → **H1-shift 적용 후 FICR = 0.38853 (오히려 하락)**

**같은 연도 안(H1→H2)에서조차 후처리 shift가 일반화되지 않고 역효과**입니다. 즉 post-hoc 튜닝의 진짜 강건한 상한은 사실상 0%(또는 음수) — 로컬에서 보이는 shift 이득은 캘리 창에 대한 과적합이지 실제 전이 가능한 헤드룸이 아님. 이 결과가 s*=0.86과 결합해 "정밀도 vs 후처리" 판정을 강하게 정밀도 쪽으로 기울입니다.

## 3. 그룹별 우선순위

| 그룹 | NMAE | FICR | 6%밴드 진입률 | n |
|---|---|---|---|---|
| G1+G2 (Vestas V126) | 0.1180 | 0.4331 | 35.6% | 9,965 |
| **G3 (Unison U136)** | **0.1451** | **0.3268** | **28.2%** | 4,566 |

**G3가 뚜렷하게 열위**(FICR -0.106, 6%밴드 -7.4%p) — 우선순위는 G3. 단, SCADA 실측풍속 기반 G3 파워커브 재구성은 이번 세션에 이미 시도·기각됨(`외부데이터_정리.md` §SCADA 클린마스크 v2, 2026-07-21) — 동일 접근 재시도는 비권장. 다른 각도(예: G3 전용 피처 엔지니어링, 다른 모델클래스 조합)가 필요하면 별도 트랙으로.

## 4. 밴드 경계 질량 분포

| e 구간 | 비중 |
|---|---|
| [0, 0.06) — 상위밴드 진입 | 33.3% |
| **[0.06, 0.08) — "턱걸이 실패" 존** | **9.0%** |
| [0.08, 0.10) | 8.4% |
| [0.10, 0.15) | 16.3% |
| **[0.15, ∞) — 대오차** | **33.1%** |

"턱걸이 실패"(0.06~0.08) 질량은 9%뿐인 반면, **e≥0.15인 대오차가 전체의 1/3을 차지** — 소폭 shift로 밴드 안쪽으로 밀어넣을 수 있는 시간대는 소수이고, 대부분의 오차는 애초에 밴드 근처에도 없습니다. 이는 "넛지로 해결 가능한 문제"가 아니라 **근본적인 정밀도 문제**라는 걸 뒷받침합니다.

## 판정 (선등록 규칙 대비)

- 규칙: s* ≤ 0.85 → "base 정밀도 없이 불가". 실측 s*=0.86(임계에 0.01 차이로 근접, 사실상 동일 결론).
- **판정: 통과(정밀도 필요 확정)** — 후처리 상한이 사실상 0%/음수라는 보강 증거까지 더해 프론티어 격차는 압도적으로 base 정밀도 문제.

## 기준 외 관찰 (참고용, 판정 미사용)

- H1→H2 shift 역전(−0.0104)은 이 프로젝트가 이미 겪은 "로컬 좋음→전이 실패" 패턴(v8b/v9)과 같은 계열 — 캘리 레이어조차 연도/반기 전이에 취약함을 보여주는 부가 증거.
- G3(U136) 열위가 밴드경계 질량의 상당 부분을 차지할 가능성 있음(본 분석은 그룹 합산 밴드질량만 봄, 그룹별 분해는 후속 과제).

## 다음 액션

프론티어 FICR 격차는 후처리로 메울 수 없다 — 향후 레버는 반드시 **base 예측 정밀도 자체**를 겨냥해야 하며(G3 우선), 순수 캘리브레이션/블렌드 축(v7a, M1 등)에는 낮은 기대치를 걸 것.
