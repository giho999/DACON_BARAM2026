# TASK27. 기상청 자료개방포털 "대용량자료신청" 정찰 — 결과

작성: 2026-07-27 (클로드코드 실행) · 조사 전용(WebSearch/WebFetch만, 신청·다운로드·모델링 없음), 제출 0.
목적: TASK23이 "미확인·불확실"로 남긴 각주 — 대용량자료신청이 GDPS(KIM)/RDPS 2022-01~2025-12
과거 아카이브를 커버하는지 — 를 닫는다.

## 조사 결과 (4항목)

| 항목 | 결과 | 근거 |
|---|---|---|
| ① 기간 무공백(2022-01~2025-12) | **미확인(불확실 지속)** | `data.kma.go.kr` RDAPS 페이지는 "경량화(lightweight)" 등급이 "최근 1년 자료까지만 제공"임을 명시하나, 이는 실시간·경량 등급 한정 문구이고 **대용량자료신청(표준/원본 등급) 자체의 보존기간·아카이브 최고령 시점을 명시한 공개 페이지를 찾지 못함**. GDPS/KIM 전용 데이터 리스트 페이지(`code=300`, `code=310` 등 추정 경로)는 전부 "컨텐츠 준비 안됨" 플레이스홀더로 응답, 직접 확인 불가. 로그인 후 신청 화면 내부(날짜 선택기)에서만 확인 가능한 정보로 추정 — **이번 조사 범위(비로그인 웹 조사)로는 원천 봉쇄된 항목**. |
| ② 파일 포맷·런타임(발표시각) 메타 | **확인(충족)** | `apihub.kma.go.kr`의 수치모델 파일목록 문서: KIM은 `kim_g128_ne36_pres_h???.*.gb2` / `kim_g512_ne36_pres_h???.*.gb2` 명명, GRIB2(`.gb2`), 예보시간(h???)과 실행시각(00/06/12/18Z) 포함. RDAPS/LDAPS(UM 코어)도 동일 패턴(`g120_v070_erea_pres_h???.*.gb2` 등) — **GRIB2 표준 자체에 run/lead가 내장**되므로 leakage 정렬은 우리 기존 gfs_* 파이프라인과 동일 설계로 재사용 가능할 것으로 판단. |
| ③ 신청자격(누구나)·승인 소요일 | **부분 확인** | 실사용 가이드(pro.re.kr)에 "일반 사용자"도 가입·신청 가능하다는 서술 확인(기관 전용 아님). 단 **승인 소요 기간(일수)을 명시한 공개 자료는 발견 못함**(공식 FAQ 페이지도 플레이스홀더성 응답만 확인됨). |
| ④ 라이선스(공공누리) | **부분 확인** | RDAPS 자료 페이지에 공공누리(공공저작물 자유이용허락) 로고·출처표시 조건 확인. 단 별도 가이드에는 "일부 자료는 상업적 사용 제한 가능"이라는 애매한 문구도 존재 — 수치모델 자료가 그 예외에 해당하는지는 불명확. |

## 핵심 발견 (기준 외 참고, 판정에는 미사용)
"GDPS"라는 명칭 자체가 과거(2020년 이전) 영국 통합모델(UM) 기반 전지구예보시스템을 가리켰던 이력이 있어
혼동 소지가 있으나, **한국형수치예보모델(KIM)이 2020-04부터 전지구 현업모델로 완전히 KIM 코어로
전환·운영 중임을 확인**(9년간 자체개발, 2019년 세계 9번째 자국산 전지구모델). 즉 fable의 전제
("GDPS=KIM 코어, UM계 LDAPS와 모델 코어가 다름")는 **모델 계보 측면에서는 유효**해 보임 — 이 부분은
데이터 접근성과 별개로 긍정적으로 확인됨.

## 판정 (선등록 규칙 적용)
①~④ 중 **①(가장 결정적 항목)이 미확인** — "확인"이 아니라 "불확실 지속"이므로, 선등록 규칙
"①~④ 전부 충족 확인 → 발견 보고" 조건 미충족. 규칙에 따라 **"하나라도 미충족" 분기 적용 → 종결**.

### ① 선등록 기준 대비 판정
①~④ 전부 충족 미확인(①이 핵심 미확인) → **미발견, 종결**. 8/3까지 착수 여부를 판단할 실데이터
자체가 없어 "착수 포기"를 별도로 선언할 필요도 없음(애초에 미개시).
### ② 기준 외 관찰
GDPS/KIM 자체는 UM과 독립된 모델 코어라는 점은 확인됨(모델론적으로는 fable 가설 유효) — 향후 누군가
로그인 계정을 통해 실제 대용량자료신청 신청화면의 날짜 선택 범위를 직접 확인한다면 결론이 뒤집힐 수
있음(이 조사의 한계는 "비로그인 공개페이지 조사"라는 점, 계정 기반 확인은 범위 밖).
### ③ 다음 액션
`외부데이터_정리.md` §5(TASK23) 각주 해소 — §7로 이번 조사 결과 등재, "4중 실사"로 소명 승격.
데이터 경로는 여전히 종료 상태 유지, R-트랙 추가 조치 없음.
