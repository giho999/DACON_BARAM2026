# 인수인계서 v2 — DACON 풍력발전량 예측 (새 세션용)

작성: 2026-07-21 · **갱신: 2026-07-25(클로드코드, TASK23 판정 후 엔드게임 전환 반영)**
이 문서 하나로 새 채팅(Cowork/클로드코드 모두)이 즉시 이어받을 수 있게 정리.
※ 아래 0~2·6~8절은 2026-07-25 기준 갱신됨. `DACON_풍력_전체기록.md`는 **이제 서버 루트에도 존재**
(2026-07-25 클로드코드가 사용자 제공 로컬본 위에 4-18~4-24·엔드게임 상태까지 갱신해 신규 생성) —
단일 진실원으로 이걸 참조할 것. `V7_공유_가이드.md`·`local_eval_방식.py` 등은 여전히 로컬 전용.

## 0. 먼저 읽을 파일 (로컬 `~/DACON_baram2026/outputs/`, 서버 `~/DACON_baram2026/`)

| 파일 | 내용 |
|---|---|
| **`DACON_풍력_전체기록.md`** | ★ 단일 진실원(single source of truth). 전체 히스토리·판정·규율. 갱신 의무 있음. **2026-07-25부터 서버 루트에도 존재**(CNN 게이트 TASK1-15·TASK16-23·엔드게임 반영판) — 로컬본과 병합 필요 시 대조 |
| `외부데이터_정리.md` | 외부데이터 규정·leakage 논증·소명 체크리스트 (ECMWF/ASOS/GEFS). **외부데이터 손댈 때마다 이 대장을 읽고 즉시 갱신(수집방법·시점·변수·라이선스·leakage 논증) — 2차 평가 소명 의무** |
| `local_eval_방식.py` | 로컬 평가 체계 (산식·rolling·pooled) 정리본 |
| `v7_pipeline_full.py` + `V7_공유_가이드.md` | 현재 best 파이프라인 재현본 (팀 공유용) |
| `ms_ts_개선_브리프.md` | 팀원 컴포넌트 진단 — ms·ts·base 공선성 0.98, **ts의 G3 월불변 −4% 바이어스**, 탈상관 개선방향 |
| `2차평가_준비.md` | 2차 관문 준비물 — 코드 분리·발표 PDF 골격·증빙·최종파일 전략. **⏸ 보류: 30위 진입 확정 + 사용자 요청 시에만 가동 (지금 준비 안 함)** |
| `SCADA_보정_실험지시문.md` | (종결·기각) SCADA 실측풍속 잔차보정 v8 후보. 기록만 보존 |
| `FICR손실_실험지시문.md` | (종결·기각) value-loss v8b — 로컬통과→LB 전이실패. 기록만 보존 |
| `시계열모델_이동보정_실험지시문.md` | (전 게이트 통과·채택) v9 딥 시계열. 빌드 대기 |
| `모델_스코어_대장.md` | ★모델간 격차 추적 — 각 모델 로컬 1-NMAE/FICR/TOTAL + 예상 LB + 실제 LB 한 줄씩. 매 실험 회신에 이 형식 채울 것 |
| `fable_전략보고서.md` | fable에 넘긴 전략 인수인계(병목·격차·방향성) |
| `fable_실행지침_진단트랙.md` | ★현행 전략 — fable의 D1~D3·M1·A' 선등록 스펙. 클로드코드 실행·판정 기준 |

## 1. 현재 상태 (한 줄) — ★2026-07-25: 엔드게임 전환(경쟁적 개선 확정 종료)

**최종파일 = 팀원 `submission_ficr_w1_v7_cnn(0.65183).csv`(v7 GBDT+MLP 앙상블 × CNN w=0.20 블렌드), LB 0.65184.**
8/14 09:59 전 이 파일이 선택 상태인지 최종 확인 필수, **추가 제출 금지**(진단만, 제출 0).

**★모델링·데이터 두 경로 전부 봉쇄 확정(TASK9~TASK23, 2026-07-22~25)**:
- **모델링 경로**: 스택가치(전체 앙상블 기여분) 0.735%로 확인, 오차가 입력(NWP 정밀도)에 지배당함,
  base가 물리적으로 선점한 신호 영역이라 후처리·구조변경·손실변경·다른 모델클래스(딥시계열 v9)·
  회전DEM 방향구조·캘리 shift 조정·시간내변동성(젠센보정) 전부 게이트 실패 또는 이득 미미 확인.
- **데이터 경로**: TASK23 실사 결과 규칙(공개+상업라이선스+파일다운로드)과 커버리지(2022-01~2025-12
  공백없음)를 동시 만족하는 **독립 NWP가 공개영역에 부재** 확정(ICON 2022-23 공백, GDPS/RDPS 최근1년만,
  GEFS reforecast 2022-25 부재, ERA5 재분석 규칙위반, 유일 형식통과 GFS0.25°는 기존 GFS 재탕이라
  독립정보 아님 — GEFS-mean 중복실패(-0.00329) 재현 위험). **A' 재개 불가, 데이터 경로 종료.**
- 프론티어(1등팀 추정) FICR 0.02 격차는 우리가 접근 불가능한 입력(자체 고해상도 NWP·기관전용 데이터·
  규칙상 배제된 소스)에서 왔을 가능성이 이 실사로 뒷받침됨 — 우리 통제 밖으로 판단.

**잔여 옵션(저비용·저기대, 사용자 판단 대기)**: GFS 0.25° 미추출 변수/기압면 추가 추출(새 NWP 아님,
"오차는 입력지배" 원칙과 상충 위험) — 원하면 S1 스크린만 30분.

**다음 국면 = 엔드게임**: ① 최종파일 확인 ② 팀원 체크포인트 회신 처리 ③ REPRODUCIBILITY_STATEMENT·
ENDGAME2 매니페스트 유지 ④ 2차 발표 소재 정리(아래 참조). **재가동 조건**: 30위 진입 확정+사용자 요청 /
팀원 신규 레버 제시 / 사용자 명시적 지시. 그 외 전부 동결.

**2차 발표 소재 후보**: 게이트 양방향 검증(가양성·가음성 둘 다 실제로 잡아냄) · 재검증에서 잡힌 버그
3건(v9 캘리 버그 등) · 스택가치 정량화(0.735%) · FICR-NMAE 환율(트레이드오프 비대칭 정량화) · CNN
실현체(realization) 로또 비전이성(TASK15, Spearman ρ=0.02) · 캘리 SHIFT_GRID ±0.08=안전장치임을
실증(TASK20, 확장 시 freeze게이트 붕괴) · base가 물리적으로 이미 선점한 신호 영역 · ★데이터 실사로
독립 NWP 부재를 실증한 것(TASK23) — "안 찾아본 게 아니라 없다"는 소명.

<details><summary>[구버전 기록, 2026-07-21 시점 — 참고용, best 파일은 위 최신 상태로 대체됨]</summary>

**best = v7 (`submission_ficr_w1_v7.csv`) LB 0.64926, 48~52위(Public). 30위 컷 ~0.65230, 격차 ~0.0030, 마감 8/14.** 목표: Private 상위 30 (2차 평가 관문).

**★ v7 = 최종 모델 확정.** 데이터·피처·캘리·블렌드·손실·딥 시계열(v9)까지 전부 2025 전이 벽에 걸림(v8b LB 반전, v9 연도전이 게이트 실패 — 천장 확인, 7절 13항). 기각 이력: GEFS·v7a·SCADA·v8b·v9. **최종 선택 파일 = v7** (v7a 0.64996이 Public 최상단이라 DACON 자동선택됨 → **마감 전 v7 수동 재선택 필수**). 전략 = consolidate + (30위 진입 시) 2차 서사.
(★그 후 팀원 v7+CNN(0.65184)이 새 최종파일로 확정됨 — 위 최신 상태 참조)
</details>

**대회 일정(확정)**: 팀 병합 마감 **8/7** · 1차 종료 **8/14 09:59**(예측 CSV) · 2차 산출물 마감 **8/17 10:00** · 산출물 검증 8/21 · 오프라인 발표 **8/28** · 시상 9/4.
**규칙 핵심(위반=실격)**: Python only · 팀 최대 3인(현 3인 충족) · leakage 금지(예측기준시점=전일 13~14시 이전 생성·공개·확정 정보만, 파생·집계값 포함) · 외부데이터는 공개·라이선스·재현·leakage 4요건 + 2차 소명 의무 · 사전학습 모델은 '25-07-05까지 공개 상업이용 오픈웨이트만(우리 무관) · **API 추론 금지, 로컬 가중치 실행만** · 1일 5회 · UTF-8 · 최종 채점 파일 1개 직접 선택.

## 2. 즉시 진행 중인 것 (새 세션이 이어받을 일)

### ★★ 2026-07-25 갱신: 진단트랙도 완주·종결 — 현재는 엔드게임 국면(진행 중인 실험 없음)

D1~D3·M1·A'(진단트랙, 아래 원문 보존)를 거쳐 TASK9~TASK23(회전DEM 방향구조·캘리shift·시간내변동성·
외부NWP실사)까지 전부 완주. **2026-07-25 TASK23 판정으로 모델링·데이터 두 경로 모두 봉쇄 확정**
(1절 참조) — 현재 진행 중인 실험 없음, 재가동 조건 충족 전까지 신규 착수 안 함. 각 TASK 결과는
`task16_wind_residual/`, `task18_calib_shift/`, `task20_shift_cap/`, `task22_intra_var/` 폴더 및
`외부데이터_정리.md` §5(TASK23)에 보존.

### [원문 보존] fable 진단트랙 (2026-07-22 착수 시점 계획, 아래는 당시 원문)

5연속 전이 실패 후(정정: ECMWF planB는 TA_audit_transfer_failures.md AUDIT-3에서 "전이실패"가 아니라
"연도내 과적합/검증설계 한계"로 라벨 정정됨 — 학습·평가가 전부 2024년 내부에서만 이뤄져 2025 전이를
애초에 검증한 적이 없었음. 진짜 연도전이 실패는 GEFS·SCADA·v8b·v9 4건), fable이 "부검 먼저 → 그 위에서만
레버" 전략 수립. **제출 0회(진단), 판정은 선등록 게이트로.**

- **D1 ECMWF 부검**(기한 7/25) → 실패모드 (a)/(b)/(c)/(d) 판정. **(a)/(b)/(d)면 A' 착수, (c)면 A' 기각.** → 산출 `D1_ecmwf_autopsy.md`.
- **D2 이동성격 판별 + 스트레스 게이트셋**(7/26) → "공변량/개념/혼합" 판정 + `stress_2025like_2024.parquet`(이후 모든 레버 필수 검증) + Public σ(LB 임계=1.5σ). → `D2_shift_diagnosis.md`.
- **D3 프론티어 FICR 출처 분해**(7/26) → "base X% + 후처리 Y%". → `D3_ficr_decomposition.md`.
- **M1 채점조건부 캘리**(7/27, 병행) → 메트릭구조 레버(전이 강건). 게이트 통과 시 LB 프로브 요청. → `M1_scored_conditional_calib.md`.
- **A' 물리층 멀티 NWP**(D1 조건부) → ECMWF ML층 실패를 물리층(quantile map + 역RMSE 컨센서스, 학습 ~2param)으로 우회. KIM/ICON. → `A1_multinwp_physical.md` + 소명.
- **내 역할(Cowork)**: 각 산출물 오면 선등록 게이트로 판정, A' go/no-go 결정, 스코어 대장 갱신. **실행 순서 권고: D1·D2·D3·M1 병렬 → D1 결과로 A' 개시 판단.**

**[종결·기록]** 아래는 완료된 판정들:

1. ~~v7a 제출 대기~~ → **제출·종결(2026-07-21): LB 0.64996 (+0.00070) — 점수 보기 전 선등록한 ±0.001 노이즈밴드 내 = 플랫.** 블렌드비율 축 종결. 이득 FICR 주도(ms/ts의 G3 고출력 드래그 진단 확증)나 재가중 점수이득은 노이즈 바닥. **최종파일 v7 고정**(v7a가 Public 근소 우위라 DACON 자동선택될 수 있음 → 마감 전 v7 수동 재선택 확인). `v7baseonly.csv` 후속 불필요.
2. ~~SCADA 풍속 잔차보정 실험~~ → **종결(2026-07-21): 3-seed B−A 평균 −0.00276, 3/3 음수 → 영구 기각**(팀 `ws_corr_`가 이미 SCADA 보정이라 증분 0, 추가 시 희석). **정밀도(FICR) 축 in-hand 레버 소진.** 상세 전체기록 4-14.
3. ~~value-loss v8b~~ → **종결·기각(2026-07-21): 로컬 rolling +0.00191(전 게이트 통과)이 LB에서 −0.00089로 반전**(FICR −0.00119). 2024 밴드구조 과적합 → 2025 이동 붕괴. **손실 변경도 전이 벽에 걸림.** v7 유지. 상세 전체기록 4-15·7절 13항.
4. **★ [진행·활성] v9 = 딥 시계열 × 이동보정** (`시계열모델_이동보정_실험지시문.md`) — A(BiGRU/TCN로 램프 포착→FICR) + B(adversarial importance weighting로 2025 전이 강건화). **피처 추가가 아닌 다른 모델클래스 + 이동 직접보정** — 지금까지 실패 원인(분포이동 전이 벽)을 정면 공략하는 첫 스윙. ★연도전이 홀드아웃(2022-23→2024) 게이트로 v8b 전이실패 교훈 반영.
   **★ 기각 (2026-07-22, 상세 전체기록 4-16)**: 1차 "채택"은 **캘리 버그**로 부풀려진 값이었음. 버그수정 재검증 = 게이트1 +0.00153(간신히)·**게이트2 연도전이 +0.00041(2/3, 부호 갈림) 실패** → 영구 기각. `submission_ficr_w1_v9.csv` 빌드됐으나 **미제출**. **딥 시퀀스도 전이 벽 못 넘음**(ECMWF·GEFS·SCADA·v8b·v9 동일). 단 연도전이 게이트가 제출 전 걸러냄 = 방법론 작동. **best=v7 유지.**
5. **[solo 모드]** 팀원 개선 미가정. ms/ts/quant CSV는 v7 블렌드 고정 입력. 예비: ICON 독립 NWP(C-4, 얇음). v7은 바닥 사수(fallback).

**⏸ 배제(현재 비활성)**: 2차 평가 준비(산출물·발표 PDF·증빙서류)는 **30위 진입 확정 + 사용자 요청 시에만** 가동. 지금은 준비하지 않음. 준비물은 `2차평가_준비.md`에 보존.

> **[종결] GEFS spread 안정성 재확인 (2026-07-21)**: 3-seed(42 **+0.00144** / 123 **+0.00203** / 2024 **−0.00084 부호반전**) 평균 **+0.00088** → 선고정 규칙 ①(≥+0.0010) 미달(②③ 충족) → **영구 기각·재시도 없음**. mean −0.00329·both −0.00168도 기각. GEFS 전 계열 종결, 원본 데이터 보존. best=v7 유지. (상세: 전체기록 4-13)

## 3. 작업 체계 (역할 분담)

- **Cowork(이 채팅 라인)**: 전략 판단·판정·기록 문서 갱신·논문 조사·팀 공유물 제작. 서버 접근 불가(로컬 outputs 폴더만).
- **클로드코드(서버 `cs.dongguk.edu`, `~/DACON_baram2026`)**: 학습·검증 실행. GPU6 (`CUDA_VISIBLE_DEVICES=6`). 세션 리셋 시 `claude --continue` 또는 요약 복원 후 이 문서 참조 지시.
- **사용자**: DACON 제출(하루 팀 5회), 점수 공유, 최종 승인.
- 흐름: Cowork가 지시문 작성 → 사용자가 클로드코드에 붙여넣기 → 결과를 Cowork에 붙여넣기 → 판정.
- **지시문 포맷(사용자 선호)**: 클로드코드용 지시문은 **한 번에 복사되는 단일 코드블록**으로 제공(상세본은 별도 .md 파일로 보관, 붙여넣을 요약 블록은 따로). 복붙 마찰 최소화.

## 4. 방법론 규율 (요약 — 상세는 기록 문서 7절, 위반 금지)

1. 판정 = **rolling 12창 pooled TOTAL** (월평균 아님, 4창 등 프록시 금지 — HPO에서 과적합 실증)
2. 채택 게이트 = 단일 seed +0.0015 (3-seed 평균이면 +0.0010 등가)
3. eval 정답으로 선택 금지 (cal 내부검증/LOMO만), 큰 이득(+0.005급)은 널테스트(셀프블렌드/시간셔플) 필수
4. 캘리 형태는 강제 고정: G1/G2=shift_only, G3=iso+shift, SHIFT_GRID ±0.08 (자동선택은 노이즈)
5. train-only 컬럼(clean_1/2/3 등)은 피처 금지 (`c in te.columns` 가드), 학습 가중 전용
6. 로컬 절대값 ≠ LB (ms/ts/quant OOF 부재 — 상대 diff만), 확정은 제출 1회
7. 제출은 검증 통과분만, 최종 선택 파일은 "Public + 로컬 pooled 겸비" 후보. 마감 직전 재제출로 선택 리셋 주의
8. herbie 등 신규 패키지는 별도 venv (프로덕션 스택 파손 실사고 1회)

## 5. 현재 파이프라인 (v7) 요약

teamfeat 456피처 → [LGB(MSE)+XGB+Cat+LGB(L1), clean downweight 0.3, subsample_freq=1] + [MLP 256-128-64 가중L1] → GBDT:MLP=5:1 → 겨울 캘리(형태 강제) → ens=(3·base+1·ms+1·ts)/5 → final=(5·ens+1·quant)/6. 상세·근거는 `V7_공유_가이드.md`.

## 6. 닫힌 레버 (재시도 금지 목록)

### 2026-07-21 시점까지
HPO(프록시·12창 직접 모두) · OOF stacking · SCADA 마스크 재구성 · v7×w=2 · 그룹 joint · 착빙 피처 · ECMWF(planB 포함, LB 기각) · ASOS · quant w≠1 · seed bagging · spread/gap/ramp 파생 · seasonal/OOF 캘리 창 · C1 결정 레이어 · lean/프루닝 · **GEFS 전 계열(mean/spread/both — 3-seed 판정 영구 기각)** · **v7a 블렌드비율(ms/ts 축소 — LB 플랫, 축종결)** · **SCADA 풍속 잔차보정(3-seed −0.00276, ws_corr_ 중복 — 영구 기각)** · **value-loss v8b(로컬 +0.00191 전게이트 통과 → LB −0.00089 전이실패 기각)**. v9는 게이트1 간신히(+0.00153) 통과했으나 **연도전이 게이트(게이트2 +0.00041, 2/3)에서 실패**.

### 2026-07-22~25 추가 (fable 진단트랙 + TASK9~23, CNN 게이트 포함)
- **CNN 게이트**: TASK1(게이트 역검증, 무효판정 정정) · TASK2(production-mode 연도전이, "그 사이"=유지) ·
  TASK3(후보4종 스크리닝) · TASK14(비결정성 수정, `cnn_common.py` 패치) · TASK15(CNN 실현체 품질
  로또, 연도간 비전이 Spearman ρ=0.02 — 실현선택 라인 영구종결).
- **TASK4-A 후류/지형**(터빈좌표 결정론적 θ함수, −0.00121·0/3 기각) · **TASK4-B**(시계열편향 차단).
- **TASK9-A 터빈별**·**TASK9-B 잔차학습**(둘 다 NMAE·FICR 동시악화 기각) · **TASK9-C 시간내변동성** →
  아래 TASK22로 재개했다가 재기각.
- **SCADA cleanmask v2**(gate1 B-A 3seed 전부 음수 — 결정론적 기각) · **가중치 상호작용(weight_interaction)**.
- **회전 DEM 방향구조**: TASK16(풍향 잔차구조 진단) — 전제 "약하게 참"(3독립모델 재현 셀 6개, 1~2.7%cap)
  이나 규모가 작아 **TASK17(빌드) 보류 유지, 착수 안 됨**.
- **캘리 shift**: TASK18(부분복원/축소, α<1 전부 열위 — CALIB_ALREADY_OPTIMAL) · TASK20(SHIFT_GRID 상한
  확장 ±0.08→±0.15, 롤링구간 이득 있었으나 **연도전이 freeze 게이트에서 대폭 붕괴 — 기각, ±0.08 유지**).
  **calibrate_total 완전 동결 확정.**
- **TASK22 시간내 변동성(젠센보정)**: STEP0 게이트에서 부분R² 0.086%(<0.5% 문턱) — v7+CNN 잔차와
  거의 무관 확인, STEP1 진입 없이 종결.
- **사전학습 파운데이션모델/에뮬레이터**: WindFM/Tyan-WP(자기회귀 구조, day-ahead 문제에 입력구성
  불가) · OffWindFM · DEVINE(라이선스 미명시) · FuXi-CFD(CC BY-NC, 상업이용 불허) — **전부 기각,
  사전학습 FM 경로 전체 종결**.
- **TASK23 외부 NWP 실사**: ICON(2022-23 아카이브 공백) · GDPS/RDPS(최근1년만 제공) · GEFS
  reforecast(2022-25 미커버) · ERA5(재분석, 규칙위반 즉시제외) — **독립 NWP 공개 데이터 부재 확정,
  A' 재개 불가, 데이터 경로 종료**. 유일 형식통과(GFS0.25° 전변수)는 기존 GFS 재탕이라 저가치.

**결론(2026-07-25)**: 피처·캘리·블렌드·손실·모델클래스(딥시계열)·방향구조(회전DEM)·후처리(shift)·
전처리(젠센보정)·외부데이터(NWP실사)까지 **레버 계열 전부 소진**. 핵심 자산 = 규율 서사(선등록 게이트가
헛제출·거짓개선·과적합을 반복적으로, 그리고 양방향(과잉교정형·과소검증형 둘 다)으로 차단한 기록 — 버그
3건도 재검증에서 잡음). **최종파일 = 팀원 v7+CNN(0.65184), 바닥 사수 확정.**

## 7. 제출 이력 (핵심만)

| 파일 | LB | 비고 |
|---|---|---|
| ficr_w2 | 0.64528 | 구세대 best |
| ficr_w1_v3b | 0.64687 | 그룹별 캘리+L1 |
| ficr_w1_v5 | 0.64716 | +MLP |
| **ficr_w1_v7** | **0.64926** | +clean downweight (현 best·현 선택 파일) |
| 기각 프로브 | — | w05 0.64670, v4(ECMWF) 0.64666 |

## 8. 새 세션 시작 멘트 (복붙용) — ★2026-07-25 갱신

> `~/DACON_baram2026/인수인계서.md`(v2)를 읽고 이어받아주세요.
> **현재 국면 = 엔드게임(경쟁적 개선 확정 종료, 2026-07-25).** 모델링 경로(스택가치 0.735%·오차
> 입력지배·base 물리선점)와 데이터 경로(TASK23 실사 — 규칙+커버리지 동시만족 독립 NWP 공개영역에
> 부재) 둘 다 봉쇄 확정. **최종파일 = 팀원 `submission_ficr_w1_v7_cnn(0.65183).csv`(LB 0.65184)**,
> 8/14 09:59 전 선택 상태 확인 필수, 추가 제출 금지. 진행 중인 실험 없음.
> 잔여 저비용 옵션 1건(GFS0.25° 미추출 변수, 새 NWP 아님) 사용자 판단 대기, 그 외 전부 동결.
> 재가동 조건: 30위 진입 확정+사용자 요청 / 팀원 신규 레버 / 사용자 명시 지시.
> 다음 할 일: 엔드게임 체크리스트(1절) — 팀원 체크포인트 회신 처리, REPRODUCIBILITY_STATEMENT·
> ENDGAME2 매니페스트 유지, 2차 발표 소재 정리.

<details><summary>[구버전, 2026-07-21 시점]</summary>

> `~/DACON_baram2026/outputs/인수인계서.md`와 `DACON_풍력_전체기록.md`를 읽고 이어받아주세요.
> 현재 대기: **v9(딥 시계열×이동보정) 게이트1(정규 12창 rolling) 결과 판정** — 게이트2(연도전이 홀드아웃)는 이미 3/3 통과, B는 폐기·A 단독. 선등록 게이트(2절 4항, 시계열 지시문)로 판정. 규율(4절)·답변은 요체로. 판정·기록 갱신 계속.
> best=v7(0.64926) 최종 선택 파일(마감 전 v7a 위로 수동 재선택 확인). 기각 이력: GEFS·v7a·SCADA·v8b.
</details>
