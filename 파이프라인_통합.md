# 파이프라인_통합 — 데이터 및 모델 파이프라인

> **생성일: 2026-08-03 · DACON BARAM 2026 풍력발전 예측**
> **목적: 데이터 계보, 파이프라인 구조, 학습/추론 명령어를 한 문서에 통합**

---

## 1. 데이터 계보 (원자료 → v9 → cache → processed)

```
기상 원자료 (데이터/open/)
  LDAPS 16격자 + GFS 9격자 + SCADA 10분원자료 + KPX 라벨
    │
    ▼
① preprocess_for_team.py   (전처리 파이프라인, 9단계)
  NWP 피처엔지니어링 + SCADA 풍속보정 + 시계열피처 + G3 RF 보간
  → preprocessed_train/test_data.csv  (26,303×323 / 8,760×321)
    │
    ▼
② 파워커브 계열 15열 추가  (생성 코드: 로컬 노트북, 서버 미보관)
  pc_pred / pc_clean / ws_corr / ws_qm / bestlayer_vcubed × 3그룹
  → merged_train_v8.csv (26,303×338) / merged_test_v8.csv (8,760×336)
    │
    ▼
③ engineer_new_features.py  (재현 검증 완료, bit-identical)
  LDAPS 신규 76열 (grids 5,6,11,12) + GFS 신규 44열 (grids 5,6)
  → merged_train_v9.csv (26,303×458) / merged_test_v9.csv (8,760×456)
    │
    ├─────────────────────────────────┐
    ▼                                 ▼
④ v7 파이프라인 계보              cache_train/test.parquet 계보
  (최종제출 계보)                (신규실험 계보)
  merged_v9 직접 사용            merged_v9 → parquet 변환
                                    │
                                    ▼
                              pipeline_v2 prepare_data.py
                              (+ clean_1/2/3, y_corr_ merge)
                              → processed_train.parquet (26,303×463)
```

### merged_v9.csv vs cache_train.parquet 차이

| 항목 | merged_v9.csv | cache_train.parquet |
|---|---|---|
| shape | 26,303 × 458 | 26,303 × 459 |
| 시간 컬럼 | `kst_dtm` (datetime) | `t` (datetime) |
| clean_1/2 | 별도 `cleaned_train_v3.csv` 필요 | 내장 |
| 내용 | **453 피처 동일, 값 일치** | 형식만 parquet |
| 사용처 | v7 GBDT+MLP+CNN (팀원 원본) | rolling_eval, M1 게이트, pipeline_v2 |
| 현재 최종 계보 | **작업물 A (submission_A_m1v2.csv)** | — |

---

## 2. v7 파이프라인 (최종 제출 계보)

### 구조

```
v7 GBDT 4종 (LGB-MSE + XGB + Cat + LGB-L1 평균)
  ├── 피처: merged_v9 453개
  ├── clean downweight: 1.0 (clean) / 0.3 (dirty) → sample_weight
  ├── 하이퍼: n_est=317, max_depth=6, lr=0.03, num_leaves=64
  ├── subsample=0.8, colsample=0.7, subsample_freq=1 (버그수정)
  ├── cal/test/hold 3종 예측셋
  └── base = (5·GBDT + 1·MLP) / 6
        │
        ▼
MLP (456→256→128→64→1, BN+ReLU+Dropout 0.3, Adam 1e-3, 가중L1)
        │
        ▼
CNN (SpatialCNN, dual-branch LDAPS 4×4 + GFS 3×3)
  └── w=0.20 블렌드
        │
        ▼
팀원 멤버 (ms + ts + quant) → 최종블렌드:
  ens = (3·base + 1·ms + 1·ts)/5
  final = (5·ens + 1·quant)/6
  submission = (1-w)·final + w·CNN  (w=0.20)
```

### 캘리브레이션 (FORCE_FORMS)

| 그룹 | 형태 | SHIFT_GRID | 선택 기준 |
|---|---|---|---|
| G1 | **shift_only** | ±0.08 | FICR-max |
| G2 | **shift_only** | ±0.08 | FICR-max |
| G3 | **iso+shift** | ±0.08 | FICR-max |

- 캘리 윈도우: 프로덕션 `cal=2024-11~12`, CV 게이트 `cal=09~10 → eval=11~12`
- **G3는 iso+shift가 필수** — 비선형 bias로 shift_only는 평균 -762 kWh 과소예측 (E1 실패로 LB 실증)

### M1 밴드 보정 메커니즘

1. **기존 calibrate_total**: 그룹당 상수 1개로 **모든 예측범위 균일 적용**
2. **M1**: 이미 캘리된 예측값 중 **[0.03, 0.15]×cap 구간에 들어온 행에만** 별도 2차 shift 적용
3. **파라미터**: 그룹당 shift 1개, 총 3개 (G1 +0.06, G2 +0.03, G3 +0.05 × cap)
4. **적합**: 2024-11~12 자기참조 적합 (train-only, test leakage 없음)
5. **효과**: LB +0.00109 (FICR 84% / NMAE 16%), **전이율 90% (프로젝트 최고)**

상수는 `build_m1_submission.py`에서 100% 결정적 산술로 산출 — `m1_holdout_preds_2024.parquet` + `frozen_cnn_pred_2024_s42.parquet` → w=0.20 블렌드 → calibrate_total → fit_band_correction 그리드서치.

---

## 3. CNN 파이프라인

### SpatialCNN 구조

- **LDAPS branch**: 4×4 격자 → Conv3×3(64) → BatchNorm → Conv3×3(128) → AdaptiveAvgPool
- **GFS branch**: 3×3 격자 → Conv3×3(64) → BatchNorm → Conv3×3(128) → AdaptiveAvgPool
- concat → MLP head (3개 그룹 출력)
- **w=0.20 블렌드**: `submission = 0.8 * final + 0.2 * CNN`
- LB 효과: +0.00258 (v7 단독 → v7+CNN), 91% 전이

### 재현성

- **결정성 패치** (TASK14, `cnn_common.py`): CUBLAS_WORKSPACE_CONFIG + deterministic_algorithms + AdaptiveAvgPool2d→수동 mean + DataLoader generator 명시 → bit-identical 실행 가능
- **원본 (팀원)**: 체크포인트 미저장·매실행 재학습·TASK14 이전 GPU 비결정 연산 → **재현 불가능**
- **동결 CNN**: `task14_determinism/frozen_cnn_pred_2024_s42.parquet` (seed42, 2024 예측)
- **실현체간 변동**: SD≈0.002, 스프레드≈0.005 (8-seed 실측, TASK15)

### CNN 관련 금지사항

- CNN 재학습 금지 (동결 예측만 재사용)
- W_CNN=0.20 고정, 재탐색 금지
- seed로 "좋은 실현체" 선택 금지 (TASK15: ρ=0.02, 연도간 무상관)

---

## 4. pipeline_v2 구조 (8단계)

> **상태: E1 + S6 LB 연속 실패로 사용 금지 권고 — 참고용으로만 기록**

| 단계 | 파일 | 역할 |
|---|---|---|
| 1 | `prepare_data.py` | cache_train/test.parquet 로드 → clean_1/2/3 + y_corr merge → processed_*.parquet |
| 2 | `metrics.py` | 정확한 DACON FICR (10% 필터 + 6%/8% bracket) |
| 3 | `train_gbdt.py` | 4-GBDT 앙상블 (LGB+XGB+Cat+LGB-L1), clean downweight, 3예측셋 |
| 4 | `train_mlp.py` | MLP 456→256→128→64→1, BN+ReLU+Dropout(0.3), Adam, 가중L1 |
| 5 | (내부) | 블렌드 `(5*gbdt + 1*mlp)/6` |
| 6 | `calibrate.py` | FORCE_FORMS, FICR-max shift, SHIFT_GRID ±0.08 |
| 7 | `m1_bandcorrect.py` | M1 밴드보정 + adaptive (3밴드 CV 선택) + L1-synergy |
| 8 | `generate_submission.py` | CV + 제출물 생성 오케스트레이션 |

### Variant 종류

| Variant | 블렌드 | 캘리 | M1 밴드 |
|---|---|---|---|
| A `proven_M1` | (5g+1m)/6 | FORCE_FORMS | [0.05, 0.20] |
| B `m1_adaptive` | (5g+1m)/6 | FORCE_FORMS | CV best of 3 |
| C `l1_synergy_M1` | (5g+1l1+1m)/7 | FORCE_FORMS | [0.05, 0.20] |

### G3 2022 synthetic (TASK54 S8A)

- `PIPE_G3_SYNTH=1`: G3 2022 합성 라벨 (G2-proxy, corr 0.988)을 전가중 1.0으로 유지
- G3 CV +0.030 (3/3 seed), G1/G2 0.000 격리
- `PIPE_G3_SYNTH_FULL_WEIGHT` 기본 on → synthetic 행 weight 1.0
- **pc_pred G3 확장**: RF 합성 → pc_pred(G2-corr 0.862, 자연 수준)로 합성 라벨 교체
  - `PIPE_CACHE_TRAIN=cache_train_pcg3.parquet` + `PIPE_G3_SYNTH=1`
  - CV C_m1_adaptive 0.64957 (+0.00421 vs RF 합성)

---

## 5. 전처리 데이터셋 명세 (453 피처)

### 컬럼 구성 (processed_train: 26,303 × 463)

```
t(1) + kpx_group_1/2/3(3) + clean_1/2/3(3) + y_corr_1/2/3(3) + 피처(453)
```

### 피처 카테고리

| 카테고리 | 개수 | 설명 |
|---|---|---|
| **GFS** | 250 (55.2%) | 미국 기상청. 88개가 g5/g6 격자 집중 |
| **LDAPS** | 175 (38.6%) | 기상청 동네예보. g4/g5/g6/g7/g11/g12 위주 |
| 파워커브 | 6 | pc_clean, pc_pred (그룹별) |
| 공간 그라디언트 | 4 | spatial_grad_* |
| 최적층 v³ | 3 | bestlayer_vcubed |
| 시간 파생 | 3 | hour, hour_sin, hour_cos |
| 풍속 보정 | 3 | ws_corr (그룹별) |
| 풍속 quantile map | 3 | ws_qm (그룹별) |
| 윈드시어 | 2 | wind_shear, shear_grad |
| 기타 | 4 | atmos_stability, day_of_year, month, etc |

### 피처 금지 (규율 #5)

- `clean_1/2/3` — 학습 가중(sample_weight) 전용, 피처로 사용 시 assert 차단
- `y_corr_1/2/3` — 보정 참조 전용

### 타겟 통계 (kWh)

| | G1 (cap 21600) | G2 (cap 21600) | G3 (cap 21000) |
|---|---|---|---|
| 평균 | 6,622 | 7,077 | 5,564 |
| CF 평균 | 0.307 | 0.328 | 0.265 |
| G3 2022 | — | — | 구조적 NaN (synthetic 보간) |
| clean 비율 | 92.1% | 93.5% | 60.4% (2022 미가동 포함) |

---

## 6. config.py 주요 설정

```python
# ${BARAM_ROOT}/.opencode/pipeline_v2/config.py

SEED = 42
DOWNWEIGHT_FACTOR = 0.3  # w=1.0(clean) / 0.3(dirty)
TARGETS = ["kpx_group_1", "kpx_group_2", "kpx_group_3"]
CAPS = {G1: 21600, G2: 21600, G3: 21000}
CAL_START, CAL_END = "2024-11-01", "2024-12-31 23:59:59"

# G3 synth
G3_SYNTH = os.environ.get("PIPE_G3_SYNTH", "0") == "1"
G3_SYNTH_FULL_WEIGHT = G3_SYNTH and env("PIPE_G3_SYNTH_FULL_WEIGHT", "1") == "1"

# CACHE_TRAIN 경로 (PIPE_CACHE_TRAIN env로 pc_pred G3 등 교체 지원)
CACHE_TRAIN = os.environ.get("PIPE_CACHE_TRAIN", ".../cache_train.parquet")

# FORCE_FORMS: G1/G2 shift_only, G3 iso+shift
# PIPE_G3_FORM=shift_only → E1 실험 전용 (LB 실패로 기각)

# Temporal ensemble: PIPE_TEST_TRAIN_MAX_YEAR=2023 → test 모델 2022-2023만
# PIPE_FORCE_VARIANT=B_m1_adaptive → 강제 variant 선택
```

---

## 7. 학습/추론 명령어

### 작업물 A 재현 (v7+CNN+M1, 현재 최종)

```bash
cd "$BARAM_ROOT/work_a_package"
python run.py
# → submission_A.csv 생성 (submission_A_m1v2.csv와 md5 동일)
```

### pipeline_v2 실행 (3단계 분할 — 권장)

```bash
cd "$BARAM_ROOT/.opencode/pipeline_v2"

# 1. 전처리
PIPE_G3_SYNTH=0 python prepare_data.py                # 기본 (G3 2022 → NaN)
PIPE_G3_SYNTH=1 python prepare_data.py                # G3 synth 유지

# 2. 학습 (~25분)
PIPE_G3_SYNTH=1 python train_gbdt.py                  # GBDT 4종 ~20분
PIPE_G3_SYNTH=1 python train_mlp.py                   # MLP ~5분

# 3. 제출물 생성
PIPE_G3_SYNTH=1 PIPE_FORCE_VARIANT=C_m1_adaptive \
  PIPE_SUBMISSION_OUT=submission.csv \
  PIPE_CV_RESULTS_OUT=cv_results.json \
  python generate_submission.py
```

### pc_pred G3 제출물

```bash
cd "$BARAM_ROOT/.opencode/pipeline_v2"

export PIPE_CACHE_TRAIN="$BARAM_ROOT/cache_train_pcg3.parquet"
export PIPE_G3_SYNTH=1

python prepare_data.py
python train_gbdt.py
python train_mlp.py
PIPE_FORCE_VARIANT=C_m1_adaptive \
  PIPE_SUBMISSION_OUT=submission_pcg3_C.csv \
  python generate_submission.py
```

---

## 8. 서버 환경

| 항목 | 설정 |
|---|---|
| **OS** | Ubuntu 22.04.5 LTS, kernel 6.8.0-124-generic |
| **GPU** | NVIDIA RTX A6000 49GB, `CUDA_VISIBLE_DEVICES=4` |
| **GBDT/MLP 환경** | numpy 1.26.4, pandas 1.5.3, scikit-learn 1.2.2 |
| | lightgbm 4.6.0, catboost 1.2.10, xgboost 2.1.4 |
| **CNN 환경** | `deepseq_pipeline/.venv`, Python 3.11, torch 2.5.1+cu121, CUDA 12.1 |
| **실행 방식** | `nohup python3 -u <script> > log 2>&1 &` + `tail -f log` |
| **백그라운드 주의** | generate_submission의 subprocess 호출은 nohup에서 실패 — 수동 분할 실행 권장 |

---

## 9. 핵심 파일 경로

> ★ 2026-08-07 (P2): 경로 정본이 `/home/gpu_04/DACON_baram2026/`로 전환됨(계정 이관 완료).
> 이전 정본 `/home/student/DACON_baram2026/` 표기는 전부 아래로 대체한다. 파일 경로는
> `BARAM_ROOT` 환경변수(기본: 프로젝트 루트) 기준 상대 경로로도 참조 가능 (P3).

| 파일 | 정본 경로 (gpu_04) | 용도 |
|---|---|---|
| merged_v9 | `${BARAM_ROOT}/merged_train_v9.csv` | v7 계보 입력 |
| cache_train | `${BARAM_ROOT}/cache_train.parquet` | pipeline_v2 입력 |
| cache_test | `${BARAM_ROOT}/cache_test.parquet` | pipeline_v2 test 입력 |
| cleaned_train_v3 | `${BARAM_ROOT}/cleaned_train_v3.csv` | clean 마스크·y_corr |
| cache_train_pcg3 | `${BARAM_ROOT}/cache_train_pcg3.parquet` | pc_pred G3 2022 적용 |
| frozen CNN | `${BARAM_ROOT}/task14_determinism/frozen_cnn_pred_2024_s42.parquet` | 동결 CNN 예측 |
| M1 holdout | `${BARAM_ROOT}/work_a_package/m1_holdout_preds_2024.parquet` | M1 shift 적합용 |
| 최종파일 | `${BARAM_ROOT}/submission_A_m1v2.csv` | 현재 최종 (68위) |
| config | `${BARAM_ROOT}/.opencode/pipeline_v2/config.py` | pipeline_v2 설정 |
| engineer_features | `${BARAM_ROOT}/teammate feature/engineer_new_features.py` (⚠️ 미이관 — ASSET_INVENTORY §2) | v8→v9 생성 |

---

*이 문서는 opencode 세션 기준 전체 파이프라인을 담고 있습니다.*
*생성일: 2026-08-03*
