# 발표 골격 v1 — 제3회 풍력발전량 예측 AI 경진대회 (팀 냥볶밥)

작성: 2026-07-27 (클로드코드 실행, TASK28-P2) · 대상: 오프라인 발표 8/28, 10분 분량 골격(PDF 전 단계)
선등록 5장 구조(fable 엔드게임 재판정 지시 승계) · 최종파일: `submission_ficr_w1_v7_cnn(0.65183).csv`(LB 0.65184)
★2026-07-29 갱신: TASK37 M1 밴드보정 채택으로 최종파일 교체 — `submission_m1_bandcorrected.csv`
(LB 0.6529253, 순위 약 62위). 아래 본문의 LB 0.65184 언급은 M1 채택 이전 서사(CNN 블렌드 자체의
역사적 의의 설명)로 유지하되, 발표 슬라이드 반영 시 헤드라인 점수는 0.6529253으로 갱신할 것.
★★2026-07-30 갱신(TASK46, 2차 산출물 패키지 최종화): 3장에 M1 채택 서사(리더보드 진단→밴드보정→
LB 실측)와 D2 스트레스 게이트 폐기 서사, §7-20(문턱 검정력 0.84 SE)·§7-17(잡음대조가 실제
판별자) 정량 근거, 잡음이 real과 구분되지 않은 4건을 추가. 5장 뒤에 "배제 공간 지도"를 신설(6장).
탐색은 이 시점부로 완전 종결(전처리 48후보 0/48·모델클래스 16종·데이터경로 4중봉쇄, 기각건
재점검도 0/3) — 아래 내용이 이 프로젝트 방법론 서사의 최종판.
★★★2026-08-08 갱신(E3, V8/W6 후): ① 6장 배제 공간 지도에 W6(모델 불일치) 기각 + W5 최종 기각(CPU
재검증) 반영 — "실험 경로 재폐쇄" 완결. ② 클로징 헤드라인 최종파일 갱신(`submission_A_m1v2.csv`,
LB 0.65403, 68위 — 30위 컷 0.65977, 격차 −0.00512). ③ 부록 HEFTCom 멀티NWP 문구를 TASK32 기각
확정 기준으로 수정. 슬라이드 제목·핵심문장 골격은 이 수준으로 확정, 디자인·시각화는 8/14 09:59
순위 확정 후 착수.

---

## 0. 오프닝 (30초)
"우리는 6개월간 거의 모든 종류의 레버를 시도했고, 대부분은 로컬에서 좋아 보였다가 실전에서 무너졌다.
이 발표는 '무엇이 통했는가'보다 '왜 대부분이 통하지 않았는지, 그리고 그걸 어떻게 알았는지'에 관한
것이다." — 결과 서사가 아니라 방법론 서사로 프레이밍.

---

## 1장. 병목 진단 (2분)
**헤드라인**: 점수는 NMAE와 FICR 두 축인데, 우리가 지는 쪽은 FICR — 그리고 격차의 원인을 3층으로
구분해야 정직하다.

- NMAE(오차율) vs FICR(발전량가중·계단형 단가) 분해 — 인접 순위 팀들은 NMAE가 아니라 FICR로 이긴다.
- 프론티어(추정 1등팀) 대비 격차의 ~60%가 FICR (2026-07-21 재진단). FICR은 고출력 시간대 오차를
  6%cap 밑으로 촘촘히 눌러야 하는 base 정밀도의 하류 비선형 보상 — 가중치로 짜는 레버가 아니다.
- **★서사 정정(2026-07-28, fable 지시)**: 이 대회 참가자격은 대학(원)생·졸업예정자·취업준비생이며
  기관전용 데이터 사용은 규칙 위반이다 — 즉 상위팀도 우리와 같은 공개 데이터로 경쟁한다. "격차가
  접근 불가능한 입력 때문"이라는 서술은 이 전제와 충돌하며 Q&A에서 곧바로 붕괴한다. **전부 폐기.**
  대신 정직하게 말할 수 있는 것: 피처·캘리·후처리·손실·모델클래스·공간표현(2종, TASK29/30)·
  데이터경로를 선등록 게이트와 널대조로 전부 배제했다. 격차가 존재하고, 같은 입력으로 달성 가능하다는
  것도 안다. **우리는 그 경로를 찾지 못했고, 어디에 없는지는 정확히 안다.**
  ★TASK34(2026-07-29): TASK30의 배제 판정이 LDAPS 래스터 기하결함(4×4 오귀속, 실제 4×5 다이아몬드)
  위에서 나온 것으로 확인되어 잠정 무효 처리됐으나, 올바른 지도(4×5+마스크채널)로 동일 Gate-W1을
  재검정한 결과도 FAIL(real−null −0.00127, 원래보다 나쁨) — 공간표현 배제 판정은 **유효한 근거
  위에서 최종 확정**됐다. 위 문장("전부 배제했다")은 수정 없이 그대로 유지 가능.
- 스택가치 정량화(TASK12, 2026-07-24): 우리 전체 스택(피처+CNN+캘리+블렌드)이 원시 격자 그대로
  GBDT에 넣은 것 대비 만드는 부가가치 = **겨우 0.00735**. 이 수치는 "우리 스택의 한계"를 보여주는
  정량적 근거로만 쓴다 — "그래서 입력이 다를 것"이라는 추론은 위 서사 정정에 따라 더 이상 주장하지
  않는다.
- **★G3 병목 소거 근거 (2026-08-06, W5-4)**: G3(U136) 예측 난이도가 "기종 특성" 때문이라는
  설명은 **비출력 계산으로 반증**된다 — V126(3.6MW, D=126m) 비출력 **288.7 W/m²** vs U136(4.2MW,
  D=136m) **289.1 W/m²** (차이 0.4 W/m² = 0.14%). 같은 단위 로터면적당 출력 밀도이므로 기종이
  아니라 다른 원인이다. G3의 실제 구조적 차이는 **2사업장(태백가덕산 1기 + 태백원동 4기) 2.05km
  확산 배열** — 단일 사업장 G1/G2(간격 mean 314m/350m)와 공간 구조가 다르다. 여기에
  **LDAPS 1.5km 해상도 대비 그룹 간 거리 2.17km(G1–G3)**가 더해져, 그룹별로 서로 다른 격자
  셀(5/6/12)이 필요함을 보였다. (재계산 근거: `REPRODUCIBILITY_STATEMENT.md` §12)

---

## 2장. 원시격자 CNN 돌파 — 4개월간 유일하게 움직인 축 (1.5분)
**헤드라인**: 손으로 만든 물리 피처는 전부 실패했다. 원시 격자를 그대로 삼킨 CNN만 성공했다.

- 팀원의 SpatialCNN(dual-branch, LDAPS 4×4 + GFS 3×3 원시 격자, w=0.20 블렌드) → LB **+0.00258**
  (91% 전이, v7 0.64926 → v7+CNN 0.65184). 4개월간 FICR 축을 못 움직이다 처음 건드림.
- 대조: 수작업 물리 피처(허브외삽·밀도·경험적 파워커브)는 실패 — base가 이미 v³·공기밀도·전단을
  다 갖고 있어 노이즈였을 뿐. "새 알고리즘"이 아니라 "새 정보원(원시 격자 직접 소비)"이 오차구조를
  실제로 가른다(TASK11-S1 교훈: GBM→ExtraTrees는 r=0.99로 사실상 동일 신호).
- CNN 실현체(seed) 로또 검정(TASK15): 2023 vs 2024 홀드아웃 순위 Spearman ρ=0.02(사실상 무상관) —
  좋은 실현을 "고르는" 시도는 전이 근거가 없음을 실증, 라인 영구종결. **+0.00258은 실력이자 동시에
  운(seed 실현)의 조합** — 정직하게 인정.

---

## 3장. 선등록 게이트 규율 — 가양성·가음성을 양방향으로 차단 (2.5분, 핵심 장)
**헤드라인**: 우리가 자체개발한 최대 성과는 모델이 아니라 "결과를 보기 전에 판정 기준을 고정하는 규율"
그 자체다 — 이 규율은 우리 자신(전략 리드 포함)의 오판까지 잡아냈고, **규율 그 자체가 스스로의
오심(게이트셋)을 감사해 폐기하고 진짜 신호(M1)를 구제해냈다.**

### 3-0. ★헤드라인 성과 — M1: 리더보드 반증에서 시작해 LB 실측으로 끝난 유일한 채택 사례
- **출발점(리더보드 진단)**: D3가 "후처리 헤드룸≈0"이라 결론지었던 것에 대해, 실제 Public
  리더보드를 직접 대조하니 반증이 나왔다 — 1-NMAE가 우리와 비슷한(±0.0015 구간) 15개 팀의 FICR
  중앙값이 0.44144(최대 0.44663)인데 우리는 0.43051, 즉 **같은 base 정밀도에서 FICR만 약 −0.011
  뒤처져 있었다**. base 정밀도가 우리보다도 낮은데 순위는 위인 팀이 17개, 9위와의 격차 93%가
  FICR — "손댈 공간이 없다"는 이전 진단이 틀렸다는 외부 증거.
- **기전(채점 문턱의 검열 선택편향)**: FICR은 오차율이 6%/8%×cap 문턱을 넘으면 점수가 0→3.0→4.0로
  계단식으로 뛰는 구조다. M1은 예측값이 "채점 카운트 문턱(정답의 10%×cap) 근방"([0.05,0.20]×cap)에
  몰린 행만 골라 그룹당 상수 shift 1개(총 3개)를 더해, 그 행들을 오차율 6%/8% 계단의 유리한 쪽으로
  밀어넣는다 — 예측 모델도 정답 정보도 새로 쓰지 않는 순수 후처리(자세한 코드 근거는
  `task37_m1_revival/M1_mechanism_plain.md`). 이 계단 구조 때문에 **FICR은 84%, NMAE는 16%만
  개선**되는 비대칭 분해가 관측됐고, 이 비대칭 자체가 기전과 정합적으로 들어맞는다 — "그냥 어쩌다
  된 보정"이 아니라 채점식 구조를 정확히 겨냥한 개입임을 뒷받침.
- **왜 90% 전이했는가(§7-18 정밀화)**: 이 프로젝트에서 지표구조(FICR 계단식)를 이용한 레버는 두
  갈래로 갈렸다 — **"사후 조정"으로 적용한 M1은 LB에서 로컬 효과의 90%가 전이**(로컬 +0.00121 →
  LB +0.00109)했지만, 같은 지표구조를 **"학습 손실함수"에 반영한 v8b는 음전이**(LB −0.00089)했다.
  이유: 사후 조정은 행 단위로 독립이라 "채점 안 되는 행의 오차는 공짜"라는 전제가 정확히 성립하지만,
  학습 단계에서는 모델이 전체 행이 공유하는 함수이므로 이 전제가 깨진다(TASK43이 같은 지표를
  표본가중으로 넣어 −0.00140/−0.00074로 실증). **"무엇을 이용하는가"가 아니라 "어디에
  적용하는가"(사후조정 vs 학습단계)가 전이 여부를 가른다.**
- **채택 경로**: 로컬 게이트(연도전이 3-seed +0.00121, 3/3 양수, 잡음대조 −0.00259와 뚜렷이 분리)
  통과 → 선등록 판정규칙(ΔLB≥+0.0010→교체) 그대로 적용 → **LB ΔLB +0.00109 확인, 채택**. 이 프로젝트
  전체에서 로컬 다중게이트를 통과해 LB에서도 살아남은 몇 안 되는 사례이자, 유일하게 신규 채택된 레버.

### 3-1. 가양성 차단 — 로컬 좋음 → 실전 실패를 걸러냄
- value-loss v8b: 로컬 rolling 3-seed +0.00191(전 게이트 통과) → LB **−0.00089**(반전). 2024 FICR
  밴드구조 과적합 → 2025 붕괴. 이후 모든 레버에 **연도전이 홀드아웃 게이트** 필수화의 직접 계기.
- 딥시계열 v9: 게이트1(+0.00153) 근소 통과, **게이트2(연도전이) +0.00041·2/3 실패** — 제출 전
  차단(방법론이 헛제출을 막은 사례).
- TASK20(캘리 그리드 확장): rolling +0.00232(3/3) 인데 **freeze 게이트**(1회 적합→고정)에서
  −0.016~−0.026로 붕괴 — 롤링/월별 재적합의 착시를 잡아냄.

### 3-2. ★가음성 차단 — 규율이 자기 자신의 오심을 감사해 뒤집은 사례(M1)
D2 스트레스 게이트셋은 한때 "연도전이 홀드아웃조차 놓치는 전이실패를 잡아내는 최종 관문"으로
격상됐었다 — M1을 게이트1·2·널테스트 전부 통과했는데도 −0.0000078(≈0)로 기각시켰기 때문. 그러나
TASK36 전수감사 결과, 이 게이트셋의 실제 성적은 **진양성 0건·위양성 1건(TASK7b/S2, D2 스트레스
평균 +0.00146·3/3인데 실제로는 편향)·위음성 1건(M1 그 자체)** — 예측력 자체가 없는 게이트였다.
**판정: D2 스트레스 게이트셋 영구 폐기**(참고 지표로도 사용 금지), 최종 관문은 연도전이 3-seed +
잡음대조(§7-17)로 환원. 이 감사가 없었다면 M1(이 프로젝트 유일의 LB 채택 사례)은 영구히 묻혔다.
같은 감사 사이클에서 전략 리드 자신이 3-seed 게이트에 단일seed 문턱(+0.0015)을 오적용해 M1을
또 한 번 죽일 뻔한 것도 발견·정정(올바른 3-seed 등가 문턱은 +0.0010, 팀 원규율 §4-2) — **규율이
전략 리드의 오판까지 두 번 잡아냈다.**

### 3-3. ★★문턱의 실제 판별력 — §7-20 측정과 잡음대조가 진짜 판별자였다는 정량 근거
대회 명세를 재대조해 일 블록 부트스트랩 표준오차(SE)를 직접 계산: **SE=0.00119**. 이 세션이 써온
3-seed 채택 문턱(+0.0010)은 **0.84 SE**(1 SE 미만)에 불과 — 참효과가 0이어도 우연히 문턱을
통과할 확률이 약 20%나 된다는 뜻으로, **문턱 단독은 판별력이 없다.** 실제로 이 세션의 위양성 3건
(ECMWF planB·nullV2·noise16, 아래 6장 참조)이 정확히 이 검정력 부족으로 설명된다. 반면 M1은
로컬효과(+0.00121, 1.0 SE로 문턱만으로는 근거 부족)와 잡음대조(−0.00259) 사이의 **간격이 0.0038
≈ 3.2 SE** — 이 간격이야말로 M1이 진짜 신호라는 실제 근거였고, 이후 LB +0.00109로 사후 확인됐다.
**결론: 이후 모든 게이트는 §7-17 잡음대조 없이는 판정 자체가 성립하지 않는다** — 문턱이 아니라
"real과 잡음 사이의 간격"이 유일하게 검증된 판별자.

### 3-4. 버그 3건 — 재검증이 스스로 잡음
v9 1차 채택의 캘리 버그(+0.00450 부풀림 → 정정 후 기각) · CNN `_nan_to_med` 채널축 순회 버그
(TASK6) · LightGBM `subsample_freq` no-op 버그(HPO 라운드). 전부 "로컬 좋아보임"에 안주하지 않고
재검증한 결과 발견.

### 3-5. 전략 리드의 오판을 규율이 컴퓨트 지출 전에 차단한 사례(2026-07-27)
전략 리드 자신이 지시한 재판정(TASK26, gh500 GBDT 재빌드)도 착수 전 대조 결과 **TASK24가 이미
동일 게이트로 결정적 기각(연도전이 −0.00105·0/3)한 건**임을 확인 — 부분R² 스크린 통과는 필요조건이지
충분조건이 아니라는 패턴의 실증(v8b·v9·TASK20·gh500, §3-2의 M1 문턱 오적용까지 더하면 이 세션
자체 오판 사례가 2건 더 추가된다).

---

## 4장. 스택가치 정량화 — "오차는 입력지배" (1.5분)
**헤드라인**: 우리가 손댈 수 있는 층(피처·캘리·블렌드·손실·모델클래스)은 이미 거의 다 시도했고,
그 층 전체의 가치는 0.735%다.

- TASK12(2026-07-24): 원시 격자 그대로 GBDT 단독 TOTAL 0.62198 vs 전체 프로덕션 스택 0.62933 —
  차이 **0.00735**. 6개월치 피처엔지니어링+CNN+캘리+블렌드 전체의 값이 이 정도.
- 방향구조(회전DEM, TASK16): "약하게 참"이나 규모(1~2.7%cap)가 스택가치에도 못 미쳐 빌드 보류.
- 캘리(TASK18/20)·시간내변동성/젠센보정(TASK22)·GFS 미추출 변수(TASK24) — 전부 동일 결론 재확인:
  **연도전이 홀드아웃을 통과하는 신규 신호가 더 이상 남아있지 않다.**
- FICR-NMAE 환율(TASK7/18 실측): 두 축이 비대칭 트레이드오프 — 무작정 한쪽을 밀면 다른 쪽이 더
  크게 깨진다(TASK18: 환율 −5.64).

---

## 5장. 데이터 실사 — "안 찾아본 게 아니라 없다" (2분)
**헤드라인**: "격차가 여기서 왔다"고 주장하지 않는다(참가자격상 상위팀도 같은 공개 데이터를 쓰므로
이 주장은 성립하지 않음, 1장 서사정정 참조). 대신 **우리가 실사한 4개 경로에서는** 규칙(공개+상업
라이선스+재현)과 커버리지(2022-2025 무공백)를 동시 만족하는 독립 NWP를 찾지 못했다는 사실만 정직하게
보고한다 — "안 찾아본 게 아니라, 찾아봤고, 못 찾았다."

- **TASK23**(공개 NWP 실사): 커버리지+상업라이선스+파일다운로드 4요건으로 5개 후보 심사 — ICON
  (2022-23 공백), GDPS/RDPS(최근1년만), GEFS reforecast(2022-25 자체 미커버), ERA5(규칙상 즉시제외).
  유일 형식통과(GFS0.25°)는 기존 GFS 재탕이라 독립정보 아님.
- **J-트랙**(JMA GSM, 준독립 예보센터): 리키지-안전 API로 100% 커버리지 확보했으나, **API 자체가
  기압면(허브밴드)·확장고도면에 리드타임 오프셋을 구조적으로 미제공** — 지표 세트만 획득 가능.
  잔여 관찰(surface_pressure)도 S1 스크린에서 기존 피처와 중복(F1)·무신호(F2)로 기각.
- **J'-트랙**(원시 GRIB 우회): API 제약을 우회해 원시 데이터에 직접 접근 시도 — JMA 공식 배포는
  인증장벽, 오픈메테오 자체 아카이브는 2023-12부터만 존재(+구조적으로 과거 리드타임 재구성 불가),
  대체 미러는 서버 폐쇄. **API 문제가 아니라 이 생태계 전체의 구조적 한계임을 재확인**.
- **TASK27**(기상청 대용량자료신청): 마지막 남은 각주 봉합 시도. GRIB2 표준 포맷·일반인 신청가능은
  확인했으나, **핵심(2022-2025 무공백 아카이브 실존 여부)은 비로그인 조사의 한계로 끝내 미확인**
  — "확인해서 열림"도 "확인해서 닫힘"도 아닌, 정직하게 남겨둔 유일한 열린 각주.
- **결론(범위 한정)**: 4개의 독립적인 실사 경로(공개 NWP 커버리지, 준독립 예보센터 API, 동일 소스
  원시데이터, 국내 포털 대용량신청) 모두 우리가 규칙 또는 커버리지에서 막혔다. "안 찾아본 게 아니라,
  찾아봤고, 여러 각도로 접근했고, 막힌 지점까지 규명했다." — 단 이것이 "그래서 격차가 여기서 왔다"는
  뜻은 아니다. 참가자격(대학(원)생·취준생, 기관전용 데이터 금지)상 상위팀도 같은 공개 데이터를 쓰는
  것으로 추정되므로, 격차의 원인은 우리가 찾지 못한 다른 곳(피처·모델·앙상블 구성 등)에 있을 가능성이
  더 크다 — 이 실사는 "데이터 경로가 막혀 있었다"는 좁은 사실만 소명하며, 격차의 원인 설명으로
  확대하지 않는다.

---

## 6장(신설, TASK46). 배제 공간 지도 — "안 해본 게 아니라, 다 해보고 닫았다"
**헤드라인**: 6개월간 열어본 모든 축을 표로 못박는다. 이 표가 나오면 "그거 해봤어요?"류 질문은
전부 이 표 안의 항목이다.

| 축 | 시도 규모 | 결과 |
|---|---|---|
| 전처리(변수·집계·격자·표현·표본가중) | **48후보**(fable 직접 스크린) + TASK34/35/43 + 과거 ~15건 | **0/48+ 통과** — 완전 폐쇄 |
| 모델클래스 | GBDT 4종 앙상블·MLP·CNN(2종)·딥시계열·quantile·L1·median 등 **16종** | 채택은 GBDT+MLP+CNN(팀원) 조합뿐 — 나머지 전부 게이트 미달 또는 전이실패 |
| 데이터 경로(외부 NWP) | 4개 독립 실사(공개 NWP 5종 심사·JMA API·원시 GRIB 우회·기상청 대용량신청) | **4중 봉쇄** — 규칙 또는 커버리지에서 전부 막힘(5장 참조) |
| 손실함수·지표구조 개입 | 학습 손실 반영(v8b/TASK43) vs 사후 조정(M1/TASK38/44) | 학습단계 반영은 전부 음전이, 사후조정만 전이(§7-18) |
| 기각건 재점검 | 게이트 재설계(§7-21) 이후 3건 전량 재검정(TASK45) | **0/3 재역전** — 기존 판정 전부 확정 |
| NWP 공간 보간(W5, 2026-08-06) | 그룹 중심 LDAPS 보간(셀 5/6/12 상이) vs GFS 보간(동일 셀 5) — 기전 검증 병행 | ❌ 기각(2026-08-07 CPU 재검증) — LDAPS −0.00225·GFS −0.00561·BOTH −0.00467, 잡음대조(+0.00291)가 실측 초과 → "좌표 정보가 아니라 잡음" |
| 모델 간 불일치(W6, 2026-08-08) | \|z(ws_LDAPS)−z(ws_GFS)\| — 그룹 대표 격자, 2022-23 train 통계 z-score | ❌ **W6-0 사전등록 기각** — 2024 홀드아웃 오차 상위10% vs 나머지: G2 p=0.366(유의차 없음), G1 p=0.0475(경계, d=0.08), G3만 유의(p<0.0001)하나 주입 대상 아님 → 학습 0회 |

### ★W1 실패 원인 기록 (2026-08-06, W5-4)
W1(wake-geometry) 기각의 근본 원인은 **G1 열을 단일 축으로 표현한 오류**였다 — G1은 굽은 능선
배열로 방위가 구간별 2°(t1→t4)~35°(t5→t6)까지 변동하는데, 전체 최소자승 PCA(θ_row=28.6°) 하나로
압축해 정렬도 피처가 실제 정렬을 반영하지 못했다 (ΔTOTAL −0.00340, G1 −0.0091).
교훈: 곡선 배열에 단일 방위 표현은 부적합. (근거: `endgame_0804/w_geometry/W0_report.md` §4)

### ★잡음이 real과 구분되지 않은 4건 — 파이프라인이 "아무 컬럼에나" 유의해 보이는 효과를 만든다는 실증
같은 개수의 순수 가우시안 잡음 컬럼(§7-17 잡음대조)을 병행 투입했을 때, 잡음이 실제 신호(real)와
같거나 더 나은 크기를 낸 사례가 이 세션에서만 4건 확인됐다 — 이 프로젝트가 문턱 판정에서 잡음대조를
필수 관문(§7-17, §7-20)으로 격상시킨 실증적 근거 그 자체.

1. **TASK32(ECMWF)**: real(G1) 게이트 통과 수치에 대해, 순수 잡음 5:1 블렌드도 **거의 동일한 크기
   (+0.00992)로 같은 문턱을 통과** — 게이트 문턱 자체가 잡음도 통과시킬 만큼 느슨했다는 반증.
2. **TASK31 X-2(clean-downweight)**: real 변형(V1/V2/V3) 전부 게이트 실패인데, **무작위 재배정
   대조군(nullV2)만 유일하게 문턱을 기계적으로 통과**(+0.00212, 2/3).
3. **TASK35(16셀 확장)**: real(expand16) 연도전이 −0.00069(1/3 양수)로 실패인데, **잡음
   컬럼(noise16)은 +0.00073(2/3 양수)로 오히려 더 나은 점수** — 잡음이 진짜보다 좋아 보인 사례.
4. **TASK42(u* 마찰속도)**: real(ustar) 평균 −0.00058, **잡음 평균 −0.00068로 real보다도 더
   나쁜 동일 규모** — "잡음 팔이 real의 절반 이상을 재현하면 즉시 기각"이라는 선고정 규칙을
   발동시킨 사례. real의 실패 자체도 잡음과 구분되지 않는 크기였다는 뜻.

### ⚠미확정 항목 — 풍속 RMSE→점수 환율표
fable 지시문은 "실측풍속(SCADA) 2피처가 NWP 456피처보다 +0.083 우월"이라는 환율표 인용을
요청했으나, 클로드코드가 레포 전체(`DACON_풍력_전체기록.md`, `D1~D3`, `모델_스코어_대장.md`,
`task9*`)를 검색한 결과 이 정확한 수치·비교의 산출 근거(코드·결과파일)를 **찾지 못했다**. 가장
근접한 기존 자료는 §4-14(SCADA 실측풍속 잔차보정, 팀 `ws_corr_`와 중복이라 영구 기각, B−A
3-seed −0.00276)뿐이며 방향·크기 모두 다르다. **이 항목은 발표 자료에 반영하지 않고 보류** —
[금지] 규정(신규 모델링·재검토 없음)에 따라 클로드코드가 새로 값을 산출하지 않았다. fable이
원 수치의 출처(어느 TASK·어느 실행)를 특정해주면 그때 검증 후 반영.

---

## 클로징 (30초)
"최종 점수(LB 0.65403, 작업물A M1v2 — Public 68위)는 우리가 자랑할 숫자가 아니라, 우리가 무엇을
검증했고 무엇을 정직하게 포기했는지를 보여주는 숫자다. 이 프로젝트에서 가장 재사용 가능한 자산은
모델이 아니라, 결과를 보기 전에 기준을 고정하고 — 그 기준이 우리 자신을 향하더라도 — 지키는
규율이었다."

> ★2026-08-08 갱신: 최종파일 `submission_A_m1v2.csv`(LB 0.65403, 68위) 기준으로 헤드라인 숫자 갱신.
> 30위 컷 0.65977, 우리 격차 −0.00512 — 2차 평가(30위 미진입 시 불성립) 대비 방법론 서사 중심 프레이밍 유지.

---

## 한계 (정직하게 명시, 은폐 금지)
- **ms(multistage)·ts(timeseries) 팀원 멤버의 2024년도 OOF 예측이 끝내 제공되지 않음** — 요청은
  했으나(TASK5-B, 부록 S의 S0) 미해결로 남음. 이 때문에 ms/ts가 v7 블렌드에 기여하는 절대적 효과
  크기(예: "ms가 있어서 몇 점 올랐다")는 **검증 불가** — 우리가 말할 수 있는 것은 상대적 진단(TASK4-B가
  측정하려 했던 ts의 G3 편향 등)뿐이며, 절대값 비교는 발표에서 주장하지 않는다.
- CNN 원본 실현은 원리적으로 사후 재현 불가(위 3장·부록 T ①), 동형 실현 + 편차 정량화(SD≈0.002)로
  대체 소명.
- 부록 T 감사로 드러난 NaN 버그 3변종은 팀원 코드의 결함이지 우리 스택의 결함이 아니지만, 최종파일에
  그 팀원 CNN이 편입되어 있으므로 발표에서 이 사실 자체를 숨기지 않는다(질문이 나오면 위 자료로 정직하게 답변).

## 부록 후보 (시간 남으면/Q&A 대비)
- **HEFTCom2024**(동형 대회, Hybrid Energy Forecasting and Trading Competition 2024) 비교:
  - GEB팀 솔루션 논문(arXiv 2505.10367, 코드 github.com/BigdogManLuo/HEFTcom24) — 성공요인 = 멀티NWP
    "sister forecast" 스태킹(여러 예보센터의 앙상블 멤버를 개별 피처로 스태킹).
  - 우승팀 SVK — 성공요인으로 기상예보 데이터셋 3종(멀티NWP) + **발전소 가용성 문제(케이블 고장 등)
    처리**를 명시. 우리 TASK31(clean_1/2/3 원인 진단)이 바로 이 축과 정합.
  - 시사점: 이 두 팀의 공통 성공요인(멀티NWP)을 우리도 TASK32(ECMWF 재시도)로 3번째이자 마지막
    시도했고 **게이트에서 기각**(TASK32 real≈잡음, 잡음대조가 동일 문턱 통과 — 배제 공간 지도 6장 참조) — 
    "규칙상 접근 불가"가 아니라 "실제로 시도했고 게이트로 검증했다"는 점을 명확히 한다(1장 서사정정 참조).
  - 평가식 맥락: 이 대회 평가식은 한국 재생에너지 발전량 예측제도(오차 6% 이하 → 4원/kWh 인센티브,
    6~8% → 3원/kWh)를 참고한 것으로, FICR의 계단형 단가 구조가 실제 정책 인센티브 구조를 반영함.
- 방법론 규율 16개 전문(전체기록 §7) — Q&A에서 "왜 그렇게 확신하냐"는 질문 대비.
- 재현성 소명(`REPRODUCIBILITY_STATEMENT.md`) — CNN 비결정성 정체 규명·편차 정량화(SD≈0.002).
- TASK29(원시격자 재스크린, 검정력 교정 R0→3-arm/3-base R1 ③판정)·TASK30(공간표현 재설계,
  GAP제거+터빈좌표 위치조건부 풀링, Gate-W1 FAIL)·TASK34(LDAPS 래스터 기하결함 발견·수정 후 올바른
  지도로 재검정, 재검정도 Gate-W1 FAIL — 원래보다 나쁨) — "격차를 찾는 시도마다 게이트가 정직하게
  닫았고, 심지어 자기가 쓴 지도에 결함을 발견했을 때도 다시 검정해서 정직하게 닫았다"는 사례로
  3장(게이트 규율)에 추가 인용 가능. 수치 확정됨, 슬라이드 반영 가능.

## 다음 단계 (P2 후속)
이 골격을 팀원(Cowork/사용자)과 시간 배분·강조점 합의 후 슬라이드(PDF)로 전환. 3장(게이트 규율)이
가장 차별화된 서사이므로 시간 배분 시 최우선 확보 권고. 각 장의 정확한 수치는 인용 원본
문서(전체기록 §해당 절)와 대조 확인 후 슬라이드에 반영할 것.
