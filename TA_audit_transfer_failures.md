# TA-트랙. 전이실패 3건 판정 무결성 감사 — 결과

작성: 2026-07-26 (클로드코드 실행) · 재학습·새 레버 없음(기존 산출물 재검증만), 제출 0.
**종합 판정: 3건 전부 판정 견고. 재개 없음. ENDGAME 유지.** (단 AUDIT-3은 라벨 정정 필요.)

---

## AUDIT-1. v9 딥시계열 — H1: "1차 캘리 버그 수정 시 반대방향 오류로 이번엔 과소평가"

**실행**: `deepseq_pipeline/holdout_check.py`(게이트2, 2022-23→2024 홀드아웃) 소스 확인 → `common.py`의
`calibrate_total`(3그룹 전부 iso+shift, canonical)만 import·사용, `FORCE_FORMS`(프로덕션 최종블렌드
전용) 참조 전혀 없음 — **이미 canonical 경로로 채점됨을 코드 레벨에서 확인**. 저장된
`holdout_result_A_joint_s{42,123,2024}.csv`에서 pooled diff 독립 재계산:

| seed | pooled diff(월평균) |
|---|---|
| 42 | +0.00024 |
| 123 | +0.00134 |
| 2024 | **−0.00035** |
| **3-seed 평균** | **+0.00041** |

메모리 기록(+0.00041, 2/3, seed2024 부호반전)과 **정확히 일치** — 재현 성공.

**seed2024 부호반전 해부(월별)**: 2024-03은 s42(-0.0229)/s123(-0.0148)/s2024(-0.0158) **전 seed에서
동시에 붕괴**(특정 seed만의 문제 아님, 3월 자체의 캘리창/데이터 특성으로 보임). seed2024의 음수는
단일 재앙월이 아니라 04·05·07·08·11월에서 다른 두 seed 평균보다 골고루 낮은 **광범위한 약세**
(spread열 기준 5/11개월이 음의 편차)로 구성 — "특정 캘리창 아티팩트"가 아니라 "전반적 약세" 패턴.
게다가 AB(중요도가중) 변형에서는 **음수 seed가 s123으로 바뀜**(seed2024는 오히려 +0.00020) — 어느
seed가 음수가 되는지 자체가 불안정하다는 것은 이 효과가 **역치 부근의 순수 노이즈**라는 증거.

### ① H1 판정: **거짓**(반대방향 버그 없음). kill 기준 충족(canonical 재채점 후에도 평균<+0.0008 ∧
seed2024 부호반전) → **판정 견고 확정**.
### ② 기준 외 관찰: 3월 전-seed 동시붕괴는 원인 미상(캘리 창 길이 최단월이라는 점과 연관 가능성,
미검증·판정 미사용).

---

## AUDIT-2. v8b value-loss — H2: "LB 반전은 빌드 시 v7과 다른 설정이 섞인 빌드 버그"

**실행**: `train_msecalib_ensemble_teamfeat_v7.py` vs `_v8b.py`, `build_v7.py` vs `build_v8b.py`,
`reblend_v7.py` vs `reblend_v8b.py` 3쌍 전부 라인 diff.

| 항목 | v7 | v8b | 동일? |
|---|---|---|---|
| `DOWNWEIGHT_FACTOR` | 0.3 | 0.3 | ✅ |
| `CAL_START/END` | 2024-11-01~12-31 | 동일 | ✅ |
| `subsample_freq` | 1 | 1 | ✅ |
| `FORCE_FORMS`(G1/G2 shift_only, G3 iso+shift) | 동일 | 동일 | ✅ |
| GBDT:MLP 블렌드(`W_BASE:W_MLP`) | 5:1 | 5:1 | ✅ |
| ens 블렌드(base:ms:ts) | 3:1:1 | 3:1:1 | ✅ |
| 최종(ens:quant) | 5:1 | 5:1 | ✅ |
| multistage/timeseries/quant 입력파일 경로 | 동일 팀공유 파일 | 동일 | ✅ |
| MLP7 raw npz | 재사용 | 재사용(재학습 안 함) | ✅ |
| **손실함수/멤버 구성** | 4-GBDT(LGB-MSE,XGB,Cat,LGB-L1) 평균 | **+5번째 LGB(regression_l1,
value-weighted) 추가, 5-GBDT 평균** | ❌(의도된 유일 차이) |

빌드 로그(`train_v8b_gbdt.log`) 정상 완주 확인, 제출 CSV(`submission_ficr_w1_v8b.csv`) shape=(8760,5)·
forecast_id 순서 = sample_submission과 100% 일치(v7도 동일 확인) — 빌드 자체의 구조적 결함 없음.

### ① H2 판정: **거짓**(빌드 교란 없음, value-loss만 유일 차이). kill 기준 충족(손실함수 외 bit-동일) →
**판정 견고 확정(진짜 전이실패)**.
### ② 기준 외 관찰: 없음.

---

## AUDIT-3. ECMWF planB — H3: "전이실패가 아니라 검증설계가 애초에 2024내 구조라 2025 전이를 못 봄"

**실행**: 로컬 +0.00711(라운드 최대)을 만든 두 산출물 확인.
1. 탐색용 `rolling_ecmwf_planB.sh`: `rolling_eval.py --train_start 2024-01-01 ...`(월별 rolling, 학습
   시작을 강제로 2024-01-01로 고정) — 평가월(2024-07~12) 전부 **학습 구간도 2024년 내부**(예: eval
   2024-12월 → 학습은 2024-01~11월).
2. 프로덕션 스타일 `ecmwf_pipeline/train_planb_member_v4.py` 로그: `학습 행수: 7314
   (2024-01-01 ~ 2024-11-01 직전)  cal 행수: 1441` — **역시 2024년 안에서만** 학습·cal 완결.
3. `ecmwf_pipeline/leakage_justification.md`: ECMWF(100m풍속 등 확장변수) 실질 커버리지 시작일 =
   **2024-03-08**(공식 문서상 "2024-01부터"와 다름, 실측 이분탐색 확인) — 2022-01~2024-03-07 구간은
   해당 피처가 원천적으로 전부 결측이라,애초에 `--train_start 2024-01-01` 설계 자체가 이 커버리지
   제약에서 나온 불가피한 선택이었음(2022-23을 포함해도 대부분 NaN이라 학습에 실질 기여 없음).
4. 팀 기록(`DACON_풍력_전체기록.md` 4-11)에도 이미 "2024내 학습→2024내 평가라 같은 해 이득이 2025로
   일반화 안 됨"이 상세 기록에는 정확히 적혀 있음. 그러나 상위 요약(`인수인계서.md` L78)은 이를
   "5연속 **전이 실패**(ECMWF·GEFS·SCADA·v8b·v9)"로 v8b/v9(진짜 2022-23→2024 연도분할 게이트로
   실패)와 **동일 라벨로 뭉뚱그림** — 이것이 이 AUDIT이 지목한 라벨 부정확성.

### ① H3 판정: **참**. kill 기준 충족(학습·평가 전부 2024년 내부, 코드·로그 이중 확인) →
**판정 견고(기각 유지), 단 라벨 정정: "전이실패"가 아니라 "연도내 과적합/커버리지 한계로 인한 검증설계
결함(2025 전이 자체를 검증한 적이 없음)"으로 기록.**
### ② 기준 외 관찰: 널테스트(신호 진위)는 통과했으므로 로컬 +0.00711 자체가 노이즈였다는 뜻은 아님 —
"2024 내부에서는 진짜 신호였으나 그 신호가 2025로 일반화된다는 근거는 애초에 이 실험에 없었다"는
뜻(판정에는 미사용, 향후 재검토 시 참고).

---

## 종합 판정
| 건 | H | 참/거짓 | 판정 |
|---|---|---|---|
| AUDIT-1 v9 | 반대방향 캘리버그 | 거짓 | 판정 견고 확정 |
| AUDIT-2 v8b | 빌드 교란 | 거짓 | 판정 견고 확정 |
| AUDIT-3 ECMWF planB | 라벨 오류(전이실패 아님) | **참** | 판정 견고(기각 유지) + 라벨 정정 |

**③ 재개근거 유무: 없음.** 3건 모두 원 기각 판정 자체는 견고 — "천장 서사"는 강화된다(ECMWF planB는
"전이실패"가 아니라 "검증설계 결함"으로 더 정확히 재분류되었을 뿐, 결론은 동일하게 기각/미채택).
ENDGAME·최종파일(v7+CNN, LB 0.65184) 불변. 팀 요약 문서(`인수인계서.md` L78)의 "5연속 전이 실패" 표현은
차기 문서화 시 "ECMWF planB(연도내 과적합), GEFS/SCADA/v8b/v9(진짜 연도전이 실패)"로 구분 기재 권고
(문서 수정은 별도 승인 필요 사안이라 이 감사에서는 실행하지 않음).
