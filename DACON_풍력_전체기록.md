# 제3회 풍력발전량 예측 AI 경진대회 — 전체 작업 기록

팀: 냥볶밥 · 문서 작성 기준일: 2026-07-18 · **최종 갱신: 2026-07-25 (엔드게임 선언, 클로드코드)**
**현재 최종파일 = 팀원 `submission_ficr_w1_v7_cnn(0.65183).csv` (v7 GBDT+MLP × CNN w=0.20 블렌드), LB 0.65184.**
(이전 최고 0.64926은 v7 GBDT+MLP 단독, 2026-07-21 시점 기록. 이후 팀원이 CNN 멤버를 편입해 갱신됨.)

이 문서는 대회 시작부터 현재까지 시도한 모든 것 — 참고자료, 성공/실패, 방향성, 점수 — 을 한 곳에 정리한 기록이다.
서버(`~/DACON_baram2026`)에 이번에 처음 생성됨 — 로컬(Cowork) `outputs/` 폴더의 동명 문서와 별도로 관리되어 왔던
이력이 있다면 두 문서를 대조·병합 필요.

## 1. 대회 개요와 평가 산식

목표: 태백가덕산풍력(64.2MW, 17기)의 시간단위 발전량을 KPX 3개 그룹별로 예측. 입력은 NWP 예보(LDAPS 1.5km·16격자 / GFS 0.25°·9격자). test = 2025년 전체.

설비용량: kpx_group_1 = 21600, kpx_group_2 = 21600, kpx_group_3 = 21000 (kWh).

G1/G2: VESTAS V126 각 6기, G3: UNISON U136 5기. 허브고도 두 기종 모두 117m.

평가 산식 (실제 발전량이 설비용량의 10% 이상인 시간대만 채점):

TOTAL = 0.5(1−NMAE) + 0.5·FICR

NMAE = (1/3)Σ_g (1/N_g)Σ_i |ŷ_i − y_i| / C_g,  FICR = (1/3)Σ_g [Σ_i y_i·u_i] / [Σ_i y_i·4.0]

시간별 단가 u_i: 오차율 e_i = |ŷ_i − y_i|/C_g 에 대해 e_i≤0.06⇒4.0, e_i≤0.08⇒3.0, 그 외 0.

운영 규칙: Public 40% / Private 60% (Private가 수상 기준). 1일 제출 5회(팀 공유 → 우리 몫 1~2회). Data leakage 금지(예측기준시점 = 전일 13~14시 이전 정보만). 외부데이터·공개 오픈소스 가중치 허용, API 추론 금지. **사전학습 모델은 2025-07-05까지 공개·상업이용 가능한 오픈웨이트만 허용, API 추론 금지.**

**대회 일정(확정)**: 팀 병합 마감 **8/7** · 1차 종료 **8/14 09:59**(예측 CSV) · 2차 산출물 마감 **8/17 10:00** · 산출물 검증 8/21 · 오프라인 발표 **8/28** · 시상 9/4.

## 2. 데이터 구성

여러 전처리 버전이 병존했고, 각 팀원이 다른 방식으로 가공했다.

| 데이터 | 출처/제작 | 특징 |
|---|---|---|
| merged_train/test_v9.csv | 팀원 B 전처리 | 메인 피처셋. LDAPS·GFS 다격자·다고도 + 팀 파생피처 포함 |
| cleaned_train_v3.csv | 팀원 C 전처리 | clean_1/2/3 이상치 마스크 제공 |
| cache_train/test.parquet | 통합 캐시 (459 cols) | 위의 병합본. 로컬 실험의 기준 데이터. targets+t 포함 |
| 내 초기 csv | 이기호 | AutoGluon 시절 피처 |
| **SCADA 10분 원자료** | `data/scada_vestas_train.csv`(2022-01~2025-01) / `scada_unison_train.csv`(2023-01~2025-01) | 터빈별 10분 ws/wd/전력. G1=vestas01-06·G2=vestas07-12·G3=unison01-05 매핑(TASK9 사전게이트로 상관 0.95~0.97 검증). 이번 세션(TASK22)에서 시간내 분산(σ²_intra) 산출에 사용 |

cache의 피처 구조 (물리 이미 풍부):

- LDAPS: ldaps_ws_mean/50m/5m, ws_squared/cubed, air_density, ws_sin/cos(풍향), blh, 격자 g4·g5·g6·g7·g12
- GFS: gfs_ws_80m/100m/850hPa, cubed, air_density, gust, shear_850_700/700_500, dir_shear, lapse_rate, stability, pbl, 격자 g1~g9

### 2-1. ★나셀 풍속계·NWP 고풍속 편향 EDA (2026-07-29, fable, 유도/NTF 가설 종결 시 발견)

기록에 없던 EDA 사실 3건 — 유도(induction)/NTF(nacelle transfer function) 과소추정 가설을
검토하는 과정에서 fable이 실측·등재. 가설 자체는 아래 4-51에서 종결됐으나, 이 EDA 사실들은
독립적으로 유효해 발표 EDA 절에 활용 가능:

1. **나셀 풍속계 편향 실측**: 허브고도(117m) SCADA 풍속이 전단(shear) 외삽 자유류 풍속 대비
   **약 16% 낮음** — 문헌상 NTF(nacelle transfer function) 보정계수 범위(5~15%)와 정합하는
   규모(약간 상회).
2. **LDAPS의 고풍속 과대예보**: SCADA/LDAPS-50m 비율이 풍속 구간 전체에서 단조 감소(1.041→0.772)
   하며, 특히 16~30m/s 고풍속 구간에서 비율이 **0.772**까지 떨어짐 — LDAPS가 고풍속을 과대예보
   하는 경향.
3. **GFS 100m의 지형평탄화 프록시 약화**: GFS 100m 평균 풍속 3.46 m/s vs 실제 허브고도 실측
   6.99 m/s — 0.25° 격자 해상도가 이 능선 지형을 평탄화해 실제 풍속 대비 약한(under-representing)
   프록시가 됨.

### 2-2. ★터빈 단지 정보 EDA (2026-07-30, fable, `info.xlsx` 확인)

- **그룹별 위치 상이**: G1(VESTAS1-6) 북서·G2(VESTAS7-12) 중동·G3(UNISON1-5) 남동 — 단지 중심간
  최대 약 1.9km 이격. ★**G3의 2~5호기는 '태백원동', 나머지는 '태백가덕산'으로 단지명 자체가
  다르다** — G3가 이 세션 전체 진단에서 계속 이상 신호를 보인 것(SD 2.6~2.8배, 균일편향
  −6.6~7%, shift 상한포화, FICR 최악)과 연관 가능성. **단 TASK30(위치조건부 풀링)·TASK9-A
  (터빈별 모델링)가 이미 이 방향을 실패로 확인했으므로 신규 착수 없음** — EDA 사실로만 등재.
- **로터 직경·용량 상이**: VESTAS(G1/G2) 로터 126m·개별용량 3.6MW, UNISON(G3) 로터 136m·개별용량
  4.2MW — 터빈 기종 자체가 G3만 다르다는 점도 위 이상신호의 잠재 요인으로 병기(미검증).

- 팀 파생피처(그룹별, 5계열 × 3그룹 = 15개): pc_pred_(파워커브 예측), ws_corr_(풍속 보정), pc_clean_(정제 파워커브), ws_qm_(풍속 분위매핑), bestlayer_vcubed_(최적층 v³)

핵심: base(merged)는 이미 v³·공기밀도·풍향·80/100m·전단을 다 갖고 있다. 이 사실이 뒤의 "손으로 만든 물리 피처가 실패한" 이유가 되고, 이후 CNN(원시 격자 직접 소비)이 유일하게 이 병목을 우회한 레버였다는 것이 이 기록 후반부의 핵심 서사다.

## 3. 진단 — 우리의 병목

두 축으로 점수를 분해한 것이 모든 방향의 나침반이 됐다.

- **NMAE 빈자 / FICR 부자 (인접순위 기준)**: 인접 순위(50위권)에서 우리를 이기는 팀들은 NMAE로 이겼다.
- **★ 프론티어 재진단(2026-07-21)**: 상위 5팀 대비 격차의 약 60%가 FICR. FICR은 발전량가중·계단형이라 고출력 시간대 오차를 6%cap 밑으로 촘촘히 눌러야 뛰는, base 시간별 정밀도의 하류(비선형) 보상 — 가중치로 짜는 레버가 아님(quant w 정점 실증).
- **분포이동(전이)**: adversarial validation AUC 0.965 = 2025 test가 심하게 이동. "train에서 좋아도 test에서 무너지는" 함정을 항상 경계해야 한다는 원칙이 이후 전 세션의 핵심 규율이 됨(연도전이 홀드아웃 게이트로 구체화, §7).
- **★ D3 재진단(2026-07-22, 아래 4-17)**: 프론티어 FICR 격차의 ~95%가 base 정밀도, ~5%만 후처리 — 후처리 헤드룸이 사실상 0이라는 것이 확정적으로 재확인(D3 + M1 + 이후 TASK7/10/18/20 전부 동일 결론에 수렴).
  ★★철회(2026-07-29, fable 긴급지시): 리더보드 반증 — 1-NMAE ±0.0015 구간 15팀의 FICR 중앙값
  0.44144·최대 0.44663 vs 우리 0.43051, base 정밀도가 우리보다 낮은데 순위가 위인 팀 17개, 9위와의
  격차 93%가 FICR. D3가 잰 것은 "우리 캘리 family 내 헤드룸"이지 "달성 가능한 헤드룸"이 아니었다는
  판단(이 수치는 fable 외부 관찰로 레포 내 독립검증 불가, 그대로 인용). 후속 TASK36(D2 스트레스
  게이트 감사)·TASK37(M1 부활)·TASK38(밴드최대화)로 재조사 — 결과는 해당 절 참조.
- **★ TASK12 정량화(2026-07-24)**: 스택 전체(피처엔지니어링+CNN+캘리+블렌드)가 원시 격자 그대로 GBDT에 넣은 것 대비 만드는 추가가치 = 겨우 **0.00735**(pooled TOTAL 기준). "스택가치 0.735%"로 이후 전략 판단(엔드게임 여부)의 정량적 근거가 됨.

## 4. 시도 연대기 (버전별)

### 4-1 ~ 4-17. (2026-07-18 ~ 2026-07-22, v1~v9 · GEFS · SCADA correction · value-loss · 딥시계열 · fable 진단트랙)

아래는 2026-07-21까지의 원본 기록을 그대로 보존한다(이후 세션에서 검증·참조되는 원문이므로 재작성하지 않음).

<details>
<summary>4-1 ~ 4-16 원문 펼치기 (quantile 라인 → v1 → ens_v2 → ficr_w1/w2 → teamfeat 재투입 → rolling CV/v3b → ECMWF planB/v4 → v5 MLP → HPO 라운드 → v6a/clean-target/v7 → v7a → GEFS 라운드 → SCADA 잔차보정 → value-loss v8b → 딥시계열 v9)</summary>

**4-1. quantile 회귀 라인 (초기, ~115위)**: train_predict_v10~v18.py, LGB+XGB+Cat quantile 앙상블. v16 ens ≈ 0.6406. "캘리는 소용없다"는 초기 결론(나중에 base 의존적 오판으로 판명).

**4-2. lean 피처 앙상블**: adversarial AUC 0.866, forward +0.0031이나 실제 LB 중립.

**4-3. 외부데이터(JMA/Open-Meteo) 1차 시도**: 허브고도 풍속 없음, 실패.

**4-4. AutoGluon 파이프라인**: 초기 베이스라인, 이후 GBDT 수동 앙상블로 전환.

**4-5. ★ v1 채택 — base 교체(돌파구)**: MSE loss + 팀파생피처 제외 + isotonic + FICR-max shift 캘리(2024-11~12). LB **0.64301**. base가 바뀌면 결론도 뒤집힌다는 첫 사례.

**4-6. ① ens_v2**: LGB+XGB+Cat 앙상블. LB **0.6446**(+0.0016).

**4-7. ② ficr_w1/w2**: 고분위 quantile(α0.80) shift-only 멤버 소가중 블렌드. w1 LB **0.6453**, w2 LB **0.64528**. w≈1~2에서 정점 확인.

**4-8. A1 base 다양화(기각)**: +HistGBM+ExtraTrees 5-model, forward 악화로 미제출.

**4-9. 워크숍 기반 물리 실험**: (a) 손으로 만든 물리 피처(허브외삽·밀도·경험적 파워커브) → base에 이미 있어 노이즈, 실패. (b) ★제외했던 팀 파생피처 재투입 → forward 성공(1-NMAE +0.0059~). MSE base에선 팀피처가 병목(NMAE)을 직접 개선 — 또 하나의 base-의존 반전.

**4-10. ★ 전계절 rolling CV → v3b (새 best 0.64687)**: rolling_eval.py(12창 pooled) 구축, 월평균 채점의 겨울-편향 확인 후 pooled 채점으로 전환. 그룹별 캘리 형태 자동선택(안정성 체크로 G3 'none' 기각, G1/G2=shift_only·G3=iso+shift 확정) + L1 멤버(4-model). **LB 0.64687**, 이득 거의 전부 NMAE — 병목 축 적중.

**4-11. ECMWF planB → v4(기각)**: 1차(전체 NaN-padded 병합) −0.003 기각 → planB(2024-01+ 전용 멤버) rolling +0.00711(라운드 최대 단일 레버, 널테스트 통과) → **LB 0.64666, v3b 대비 −0.0002 전이실패**. "2024내 학습→2024내 평가"라 같은 해 이득이 2025로 일반화 안 됨(널테스트는 신호 진위만 검증, 연도 간 일반화는 별개)이라는 핵심 방법론 교훈.

**4-12. v5(MLP 멤버) 채택**: ECMWF 이후 유일 생존 레버. **LB 0.64717**(+0.0003, 새 best).

**HPO 라운드(전부 기각)**: Optuna 프록시(4창) 60 trials → 12창 재검증 전멸(과적합 교과서 사례). 12창 직접 HPO 58trials도 노이즈 수준. LGB subsample_freq 버그 발견·수정(+0.0008)했으나 w=2 유효시간가중과 합산해도 게이트 미달, v6 미승격.

**4-12b. 4개 병렬 라운드**: v6a 블렌드 프로브(대기) · 12창 직접 HPO(기각) · **clean-target downweight(0.3) 채택**(rolling +0.00485, 리키지 차단 확인 후 배포가능 확정) · OOF stacking(기각, 고정 5:1 블렌드가 우세).

**4-12c. ★ v7 = 최종 GBDT+MLP 모델**: v5 + clean downweight(0.3) + subsample_freq 버그수정. **LB 0.64926**(+0.0021, NMAE·FICR 동시개선 — 강전이, 12창 프로토콜 3번째 전이성공). 48위, 30위컷까지 0.0030. 이후 4건 검증(SCADA cleanmask v2 / v7×w=2 / group joint / icing) 전부 기각 — **v7 국소최적이 단단함을 확인**.

**4-12d. v7a(플랫·축종결)**: ms/ts 축소 프로브, **LB 0.64996**(+0.00070, 선등록 ±0.001 노이즈밴드 내). 이득이 FICR 주도라 진단(ms/ts G3 고출력 드래그)은 확증했으나 재가중 자체의 이득은 노이즈 바닥 — 블렌드비율 축 종결, 최종파일 v7 유지.

**4-13. GEFS 라운드(전 계열 영구 기각)**: mean만 −0.00329(기존 GFS 결정론과 중복) / spread만 +0.00144(게이트 근소미달) / 전부 −0.00168. spread 3-seed 재확인: 평균 **+0.00088**, 규칙①(≥+0.0010) 미달(seed2024 부호반전) → 영구 기각. **선고정 다중seed 게이트가 단일seed 경계선 신호의 자기기만을 정확히 차단한 실증 사례.**

**4-14. SCADA 실측풍속 잔차보정(영구 기각)**: rolling 12창 3분해, B−A 3-seed 평균 **−0.00276**, 3/3 음수(부호부터 반대). 팀 `ws_corr_`가 이미 SCADA 보정이라 중복·희석 — **"기존 피처와 중복되는 외부보정은 해가 된다"는 패턴의 첫 사례**(이후 TASK16/23에서 각각 다른 형태로 재현).

> ★가설로만 기록·미검증(2026-07-29, fable, 유도/NTF 가설 종결 시 병기): 위 "중복·희석" 설명은
> 결과가 0 근처여야 정합인데 실제로는 −0.00276로 적극적 악화였다는 점에서 완전히 매끄럽지는
> 않다. 재해석 후보: NWP(자유류에 가까운 예보)를 나셀 실측(허브고도 자유류 대비 약 16% 낮음,
> §2-1 EDA)로 끌어당기는 보정이었다면, 음의 부호가 "중복·희석"보다 더 자연스럽게 설명될 수
> 있다. **이 재해석은 사후적이며 검증되지 않았다 — SCADA 보정 라인 재개 근거로 사용 금지**,
> 참고 기록으로만 병기.

**4-15. Value-oriented FICR 손실(v8b) — ★로컬 다중게이트 통과 → LB 전이실패**: rolling 12창 3-seed **+0.00191**(3/3 양수, 겨울편향 게이트 통과, NMAE·FICR 동시개선, 메커니즘 정합 — 전 게이트 통과). **LB 0.64837 = v7 대비 −0.00089, 양축 다 하락**. 2024 FICR 밴드구조 과적합 → 2025 분포이동 붕괴. **"로컬 다중게이트 통과 ≠ 연도전이 보장"의 결정적 사례** — 이후 모든 후속 레버(v9, TASK18/20 등)에 연도전이 홀드아웃 게이트가 필수 관문으로 격상되는 직접 계기.

**4-16. 딥 시계열×이동보정(v9) — 기각, 방법론은 작동**: 1차 "채택"이 캘리 버그로 부풀려짐(+0.00450) 발견·정정. 버그수정 재검증: 게이트1(정규 12창) +0.00153(3/3, 간신히) 통과하나 **게이트2(★연도전이 홀드아웃, v8b 교훈으로 신설) +0.00041, 2/3(seed2024 부호반전) — 실패**. 영구 기각, `submission_ficr_w1_v9.csv` 빌드됐으나 미제출. **딥 시퀀스도 전이 벽 못 넘음(모델 클래스를 바꿔도 벽은 그대로)이나, 신설한 연도전이 게이트가 제출 전에 걸러냄 — 규율이 헛제출을 막은 첫 실증.**

**4-17. fable 진단트랙(D1~D3·M1)**: **D1** ECMWF 부검 → 주원인 (a)커버리지/NaN희석 판정 → A' 착수 GO. **D3** 프론티어 FICR 격차 분해 → base정밀도 ~95%/후처리 ~5%(무의미), 후처리 상한실험으로 헤드룸≈0 독립확증. **M1** 채점조건부 캘리 → 게이트1·게이트2(연도전이, 기준의 6.7배!)·널테스트 전부 통과했으나 **게이트3(D2 스트레스 게이트셋)에서 −0.0000078≈0로 소멸 → 기각**. ★★**방법론 대발견**: 연도전이 홀드아웃조차 놓치는 전이실패를 스트레스셋이 포착 — 이후 모든 레버의 최종 관문으로 격상. **D2** 스트레스셋(n=1756) 산출·검증완료.
★★후속 정정(2026-07-29, TASK36): D3의 "헤드룸≈0"은 리더보드 근거로 철회(위 §3 정정 참조). M1을
기각시킨 게이트3(D2 스트레스셋)은 TASK36 전수감사(진양성 0건·위양성 1건 확정, TASK7b/S2가
+0.00146(3배마진 통과)로 스트레스 통과했으나 실LB로는 +0.00012~−0.00361)에서 **차단게이트 자격
정지** 판정을 받음 — M1의 게이트3 기각은 더 이상 최종 판정으로 인정되지 않는다. TASK37에서
연도전이+널대조 기준으로 재검토. 상세는 4-44(TASK36) 참조.

</details>

### 4-18. ★ CNN 멤버 편입 (팀원 레버, 우리 최초 진짜 전이 성공) — 2026-07-23

팀원이 SpatialCNN(dual-branch, LDAPS 4×4 + GFS 3×3 원시 격자 직접 소비, w=0.20 블렌드)을 v7에 편입한
`submission_ficr_w1_v7_cnn(0.65183).csv`를 제출 → **LB 0.65184**(v7 0.64926 대비 **+0.00258**, 91% 전이).
**4개월간 못 움직인 FICR 축을 CNN이 처음 건드림.** A'(멀티NWP)가 아니라 팀원의 CNN이 fable 진단트랙 이후
최초의 실질 개선 — D3가 지목한 "더 촘촘한 base"를, 우리가 시도했던 수작업 피처가 아니라 **원시 격자를
그대로 소비하는 CNN**이 만들어낸 것으로 해석. 이후 세션(TASK1~15, 아래)은 이 팀원 레버를 (1) 우리 게이트로
역검증하고 (2) 우리 harness로 재현 가능하게 만들고 (3) 변형·확장 여지를 탐색하는 데 전부 투입됨.

### 4-19. CNN 게이트 역검증 & 스크리닝 시리즈 (TASK1~15, 2026-07-23~24, `cnn_gate/`·`task4~15_*/`)

**핵심 방법론 전환**: 팀원 로컬 harness에 프로토콜 편향 2건 발견 — (a) CNN 조기종료 검증셋=cal창(채점
대상)이라 CNN만 y_cal을 봄, (b) 캘리 적합·채점이 동일 cal창(in-sample). 편향 제거 후 **production-mode
transfer(2022-23 1회학습→2024 전체→canonical calibrate_total→pooled)** harness가 이후 CNN급 고분산
레버의 표준 검증 프로토콜로 확정(**rolling 12창은 CNN엔 SE 0.0028로 판별력 없음 — 이후 미사용**).

- **TASK1 게이트 역검증**(목적: 채택판정 아님, 게이트 정확성 검증): 편향 제거한 우리 seed42는 게이트①②③
  전부 시드에 따라 부호가 뒤집히는 노이즈 수준(①-0.00065/②+0.00093/③+0.00075, 시드간 폭 0.0095) —
  팀원 로컬 수치(+0.00282)가 편향으로 최소 부분적으로 부풀려졌을 개연성. 단 게이트③(D2 스트레스) 자체가
  M1형 "거짓음성"을 재현하진 않아 게이트 신뢰는 유지. LB(+0.00258)는 진짜 held-out이라 이 결과가 LB를
  부정하진 않음 — production은 rolling 재학습이 아니라 대규모 1회학습이라 조건이 다를 수 있음.
- **TASK2 production-mode 연도전이 측정**: 배포 조건과 정확히 일치하는 재현. (A)단일seed CNN 3-seed 평균
  **+0.00087**(2/3) / (B)3-seed배깅 **−0.00173**(0/3, 배깅이 오히려 방향을 음으로 수렴). 선등록 규칙
  "그 사이"(미달·근접미달 둘 다 아님) → **유지, 추가조치 없음**.
- **TASK3 공간채널 광역탐색(4후보 전부 기각)**: 채점시간가중(w=2.0)·용량증대(64/128)·시공간CNN(Conv3D
  ±3h)·그룹별독립CNN — 전부 게이트(3-seed≥+0.0010∧2/3양수) 미달. 그룹별분리가 가장 나쁨(0/3) — 공유
  head가 그룹 정보를 뭉갠다는 가설과 반대로 그룹간 정보공유 자체가 유익했을 가능성.
- **TASK4-A 후류/지형 기하피처(기각)**: −0.00121(0/3) — G1 가장 나쁨, G3만 국소적으로 가설 부합하나
  그룹평균으로는 미지지. **이번 세션 TASK16(회전DEM)의 직접 전례**로 재소환됨.
- **TASK4-B ts 편향 측정(측정불가)**: `submission_timeseries.csv`가 2024 학습기간 예측이 아니라 2025
  test 제출 파일 자체(정답 없음) — 2024 기간 ts 예측·재현 코드 프로젝트 어디에도 없어 측정 자체가 불성립.
- **TASK5-B 팀원 공유**: 병목·유효harness·프로토콜편향·닫힌레버 12건을 팀원에 전달, ms/ts/quant 2024
  OOF 확보를 최우선 요청.
- **TASK5-C/6 재현성**: 학습·추론 완전분리 재현 성공(`train_cnn_production.py`+`infer_cnn_production.py`).
  팀원 원본 `_nan_to_med` 채널축 순회 버그 발견·수정(train격자 NaN 0개라 TASK1~4A엔 무영향, test격자
  NaN 752개·3개 시각 집중분에만 영향, 전체 0.034%). `submission_v7cnn_fixed.csv` 생성, LB 프로브 대기
  (승인/제출 결과는 서버 문서에 기록 없음 — 최종 선택은 팀원 원본 파일로 유지된 것으로 확인).
- **TASK7 블렌드-후 FICR 재캘리브레이션(게이트 통과)**: TOTAL 3-seed 평균 **+0.00123**(2/3), FICR
  **+0.00502**(3/3) — 단 **G3는 3/3 전부 악화**(−0.00373, G1/G2는 강한 개선). **TASK7b**(G3 shift=0
  고정, G1/G2만): TOTAL **+0.00312**(3/3, 명확 우위) → 채택. 배포 shift 재적합(cal=2024-11~12):
  **G1≈0 / G2=+0.01 / G3=0**. `submission_task8_g1g2ficr.csv` 생성, LB 프로브 대기 — **결과 서버
  문서에 미기록**(최종파일은 여전히 팀원 원본 유지).
- **TASK9-A 터빈별 모델링(기각)**: 사전게이트(SCADA↔KPX 상관 0.95~0.97, 그룹별 배수 ~5.9~5.92로 안정
  — "×6 kWh환산" 가설과 정합) 통과 후 본게이트 TOTAL **−0.00406**(0/3), NMAE·FICR 둘 다 전 seed 악화.
  가장 기대했던 G3(비선형 파워커브)가 가장 나쁨(FICR −0.01085).
- **TASK9-B 잔차학습(기각)**: 타깃을 y−P_phys로 치환, TOTAL **−0.00347**(0/3), 마찬가지로 양축 악화.
  누적 15연속 실패 시점.
- **TASK10 점추정 함수 변경(기각)**: 밴드보상 최대화 지점으로 이동(λ=0.5) TOTAL **−0.00012**(2/3,
  판별력 없음), λ=1.0은 오히려 0/3 뚜렷 악화. G2만 안정적 이득(3/3)이나 G1/G3가 상쇄.
- **TASK11-S1/PC 스크리닝 인프라**: 이종앙상블 멤버 스크리닝 기준(r≤0.90∧w*≥0.10∧|bias|≤0.01∧단독
  TOTAL≥base−0.05) 확립. **양성대조로 CNN 자체를 통과시켜 스크린이 실제 판별력 있음을 검증**(CNN 4항목
  전부 통과 — TASK1의 "저분산 GBDT 기준을 CNN에 오적용"했던 임계 오류를 바로잡음). AnEn/풍속공간우회/
  ExtraTrees 3후보는 전부 기각(r 또는 w* 미달) — "모델 알고리즘의 다양성"과 "오차구조의 다양성"은
  분리된 개념임을 확인(GBM→ExtraTrees 바꿔도 r=0.99로 사실상 동일 신호).
- **TASK12 전체격자 평탄화 멤버(기각, 단 핵심 정량화)**: r=0.9666(미달)·G2 계통편차 0.0153(미달)로
  S1 기각. 그러나 **단독 TOTAL 0.62198 vs 전체 프로덕션(피처엔지니어링+CNN+캘리+블렌드) 0.62933 —
  차이 겨우 0.00735**. ★**스택가치 0.735% 정량화**, 이후 엔드게임 판단의 핵심 근거.
- **TASK13 CNN 그룹별 기여·안정성(S2 미달, 영구기각)**: 팀원 CNN 역산 vs 우리 CNN 비교(2025 test,
  진단용) — G3에서만 평균 **+2751 kWh(cap 13.1%)** 계통차, G1/G2는 상관 0.97대·차이 <1%cap. 전략
  리드 사전 역산치(≈2750kWh)와 거의 정확 일치. G3 SD가 G1/G2의 2.6~2.8배 커 S2(그룹별 가중, w_G3∈
  {0,0.10}) 진행했으나 둘 다 게이트 미달(+0.00047/+0.00075, 둘 다 2/3) — "G3가 안정적으로 나쁘다"가
  아니라 "불안정하다"가 더 정확한 서술. **기준 외 관찰**: 같은 세션 내에서도 CNN best_val_mse가 실행마다
  갈림(0.03415/0.03526/0.03526) — "실현 비결정성이 seed 단위가 아니라 실행 단위로도 존재"함을 시사,
  TASK14/15의 직접 계기.
- **★ TASK14 CNN 결정성 복원(성공)**: `cnn_common.py`에 4건 동시 수정(CUBLAS_WORKSPACE_CONFIG·
  use_deterministic_algorithms(True,warn_only=False)·AdaptiveAvgPool2d→수동 mean·DataLoader generator
  명시). 독립 프로세스 2회 실행 완전 bit-identical 검증(예측·state_dict 전부 `np.array_equal=True`).
  **한계**: 팀원 파일(LB 0.65184)은 이미 비결정 코드로 생성된 과거 실행이라 재현 불가(체크포인트 미확보
  시 소명은 `REPRODUCIBILITY_STATEMENT.md`, §5-c 참조). 이후 모든 비-CNN 실험은 이 동결 CNN 예측만
  재사용, **CNN 재학습 절대 금지** 원칙 확정.
- **★★ TASK15 CNN 실현체 로또 검정(영구 종결)**: K=8 고정seed(1001~1008), 2023 vs 2024 홀드아웃 TOTAL
  순위의 Spearman **ρ=0.0238(p=0.9554, 사실상 무상관)** — 2023 최고seed가 2024에서 8개 중 5위,
  반대도 마찬가지. **CNN seed "실현"의 좋고나쁨은 연도 간 전이되는 신호가 아니라 매 학습마다 새로 뽑히는
  순수 잡음** — 팀원의 LB +0.00258은 "좋은 패를 뽑은 결과"로 재해석. seed선택으로 실현을 개선하려는
  시도는 전이 근거 자체가 없음을 실증, 라인 영구 종결. 누적 실패 23건.

**CNN 계열 총평(2026-07-24 시점)**: 등록된 모든 진행 트랙(23건) 소진. TASK12가 스택가치를 0.735%로
정량화, TASK15가 CNN 실현 개선 시도 자체를 봉쇄 — 이 두 결과가 이후 엔드게임 판단(§9)의 직접 근거.

### 4-20. TASK16. 회전 DEM 방향 재검토 → 풍향 잔차구조 진단 (2026-07-25, `task16_wind_residual/`)

전략 리드가 "회전 DEM(θ의 결정론적 재인코딩이라 §2 법칙상 새 입력 아님, TASK4-A와 동종)"을 진단 1건
조건으로 조건부 재가동. **TASK16**: v7+CNN production-mode 2024 홀드아웃 잔차를 30° 풍향 섹터×3그룹×
3seed로 분해(풍향은 `cache_train`의 GFS 100m u/v에서 TASK4-A와 동일 공식으로 복원). 문언 그대로의
선등록 규칙(≥3셀에서 |bias|≥0.01cap∧CI배제0)은 108셀 중 107개가 flag — 그러나 이는 **방향과 무관한
거대 균일 음편향**(그룹1 −2.9~3.1%cap·그룹2 −3.3~3.7%·**그룹3 −6.6~7.0%**)에 지배된 결과라 규칙이
의도한 질문(방향에 따라 오차가 달라지는가)의 답이 아님을 발견. 균일수준을 제거한 "초과편향"으로
재분석하니, **3개 독립 재학습모델(seed 42/123/2024) 전부 동일부호·유사크기로 재현되는 방향구조가
6개 (그룹,섹터) 셀**에서 확인(±1~2.7%cap: G1·G2 180-210°, G2 120-150°, G3 270-300°, G1 240-270°,
G3 30-60°) — 최소기준 통과. **판정: 전제 "약하게 참"**, 규모가 작아(1~2.7%cap, 스택가치 0.735%에도
못 미침) **TASK17(회전DEM 빌드)은 보류**(종결 아님). 풍속층화는 GFS100m 대표풍속이 정격(11.3m/s)
이상인 시간이 8034시간 중 197시간(2.4%)뿐이라 판정불가(한계로 등재).

### 4-21. TASK18/20. 캘리 shift 조정 라인 (2026-07-25, `task18_calib_shift/`·`task20_shift_cap/`)

TASK16의 "기준 외 관찰"(균일 음편향, 특히 G3 −6.6~7.0%cap)이 `calibrate_total`의 shift 과잉/과소
보정인지 판정.

- **TASK18(α-sweep, 축소 방향)**: shift를 α∈{0,0.25,0.5,0.75,1.0}(1.0=현재)로 부분 되돌림, 3-seed
  재채점. **모든 α<1이 3시드 전부 현재보다 열위**(α*=0.75조차 −0.00200, 0/3양수, ΔFICR/-ΔNMAE 환율
  **−5.64**의 완전 불리한 교환). **판정: CALIB_ALREADY_OPTIMAL — calibrate_total 축소방향 종결.**
  ★기준 외 관찰: shift가 33개 (seed,month) 셀 전부 양수, **G3 shift가 SHIFT_GRID 상한(+0.08)에
  67%(22/33) 도달** — 과잉보정이 아니라 그리드 상한에 막힌 과소보정 가능성 신규 제기.
- **TASK20(그리드 상한 확장, ±0.08→±0.15)**: 월별 상한도달 분해 결과 **3월(1-2월 cal데이터)만 ±0.15
  에서도 3-seed 전부 100% 재추적(진짜 폭주)**, 나머지 10개월은 확장 즉시 0.05~0.12 내부최적으로 안착
  (풀링 도달률 66.7%→12.1%로 착시). ext15_all(전그룹 확장) 롤링구간 **+0.00232(3/3, 1차기준 통과)**
  하나, **연도전이 freeze 게이트(1월 cal 1회적합→3~12월 고정적용)에서 −0.016~−0.026로 대폭 붕괴(0/3)**
  — 3월의 계절 국소해를 그대로 얼려서 밀어붙인 결과. **판정: 둘 다 기각, SHIFT_GRID ±0.08 유지.**
  ★★**"freeze 게이트가 설계 의도대로 롤링 구간의 착시 이득을 정확히 걸러낸" 방법론 실증 사례**
  (v8b/v9 교훈의 세 번째 반복 확인, 이번엔 캘리 레이어에서). **calibrate_total 완전 동결 확정.**

### 4-22. TASK22. 시간내 변동성(젠센보정) 재개 → STEP0 게이트 기각 (2026-07-25, `task22_intra_var/`)

TASK9-C(시간평균 풍속의 젠센부등식 손실, P∝v³ 볼록성)를 사용자 판단으로 재개, gust 중복성 게이트로
저비용 선별. SCADA 10분 원자료로 σ²_intra(t,g) 산출(터빈별 시간내 분산 → 그룹평균), 기존 gust 피처와
상관 측정 + v7+CNN 2024 production-mode 잔차에 대한 부분R²(ws+gust 통제 후 증분) 측정. **gust 상관
최댓값 0.43(문턱 0.85 미달, 중복 아님)이나 부분R² 평균 0.086%(문턱 0.5% 미달)** — σ²_intra 자체가
gust·풍속 통제 후 잔차를 거의 설명 못함(중복이 아니라 더 근본적인 무관성). **판정: REDUNDANT_CLOSE_LINE,
STEP1 진입 없이 종결.**

### 4-23. ★★ TASK23. 외부 NWP 실사 → 데이터 경로 종료, 엔드게임 선언 (2026-07-25, `외부데이터_정리.md` §5)

WindFM/Tyan-WP/OffWindFM(자기회귀 구조, day-ahead에 발전량 히스토리 입력 불가) 이어 **사전학습 파운데이션
모델 경로 전체 종결**(DEVINE 라이선스 미명시·FuXi-CFD CC BY-NC 상업이용불허도 이미 기각, §4-24 참조).
남은 "데이터로 base 정밀도를 올리는" 유일한 경로는 **독립 공개 NWP**(A' 재개 조건) — 5개 후보를 규칙
(공개+상업라이선스+파일다운로드, 비-API) × 커버리지(2022-01~2025-12 무공백) × leakage적합 × 재현가능
4요건으로 실사:

| 후보 | 결과 |
|---|---|
| GFS 0.25°(NCAR GDEX ds084.1) | 형식상 4요건 통과(CC BY 4.0, 커버리지 연속) — **단 기존 GFS의 고해상도판일 뿐 독립 정보원 아님**, GEFS-mean 중복실패(−0.00329, 4-13) 재현 위험 |
| ICON(DWD, Open-Meteo) | 기각 — leakage-안전 리드타임 선택 필수 API가 **2024-01부터만 아카이브**(2022-23 전체 공백, ECMWF 전철 정확히 재현) |
| GDPS/RDPS(기상청) | 기각 — 포털이 "최근 1년 자료까지만 제공"(ASOS와 동일 rolling 제한 패턴) |
| GEFS reforecast v12 | 기각 — 아카이브가 2000~2019만 커버, 2022-25 데이터 자체 부재 |
| ERA5(재분석) | 즉시 기각 — 재분석은 대회 규칙상 사용 자체 금지 |

**결론: 규칙+커버리지를 동시 만족하는 독립 NWP는 우리가 실사한 범위에서 공개영역에 부재 확정 —
데이터 경로 종료, A' 재개 불가.**

**★서사 정정(2026-07-28, fable 지시)**: 위 문단이 한때 "프론티어와의 0.02 격차는 우리가 접근
불가능한 입력에서 왔을 가능성이 이 실사로 뒷받침됨"이라고 적었던 것은 **폐기한다**. 이 대회 참가자격은
대학(원)생·졸업예정자·취업준비생이고 기관전용 데이터 사용은 규칙 위반이므로, 상위팀도 우리와 같은
공개 데이터로 경쟁한다 — "격차=접근불가 입력" 주장은 이 전제와 정면 충돌하며 Q&A에서 곧바로 붕괴한다.
정정된 진술: 우리는 피처·캘리·후처리·손실·모델클래스·공간표현(2종, TASK29/30)·데이터경로를 선등록
게이트와 널대조로 전부 배제했다. 격차가 존재하고, 같은 입력으로 달성 가능하다는 것도 안다. 우리는 그
경로를 찾지 못했고, 어디에 없는지는 정확히 안다(§9 참조).

### 4-24. ★★★ 엔드게임 선언 (2026-07-25)

TASK12(스택가치 0.735%)·TASK16/18/20(방향구조·캘리 전부 소진)·TASK22(젠센보정 무관)·TASK23(독립 NWP
부재)로 **모델링 경로**(오차가 입력지배·base가 물리적으로 신호영역 선점)와 **데이터 경로**(독립 공개
NWP 부재) **동시 봉쇄 확정**. 경쟁적 개선 국면 공식 종료, 엔드게임 전환:
1. 최종파일 확인(팀원 v7+CNN, LB 0.65184) — 8/14 09:59 전 선택 상태 재확인, 추가 제출 금지.
2. 팀원 체크포인트 회신 처리.
3. `REPRODUCIBILITY_STATEMENT.md`·`ENDGAME2_package_manifest.md` 유지.
4. 2차 발표 소재 준비(§9 참조).
**재가동 조건**: 30위 진입 확정+사용자 요청 / 팀원 신규 레버 / 사용자 명시 지시. 그 외 전부 동결.
**잔여 옵션 1건**(저비용·저기대, 재가동 아님): GFS 0.25° 미추출 변수/기압면 재추출 — "같은 NWP 재탕"
이라 비권장이나 원하면 S1 스크린 30분 가능.

### 4-24b. TASK24. GFS 미추출 변수 추가 추출(gh500) — STEP1 통과·STEP2 기각, 라인 종결 (2026-07-25, `task24_gfs_extra_vars/`)

4-24의 "잔여 옵션 1건"(GFS 0.25° 미추출 변수/기압면 재추출, 새 NWP 아님·S1 스크린 30분) 실행.

**STEP0/STEP1(부분R² 신규성 스크린)**: GFS 원본 40컬럼 vs cache 250컬럼 대조로 후보 10개 중 6개(500hPa
지위고도·700/500hPa 풍속 확장·해면기압·850hPa 습도·PBL풍속 확장)를 v7 base 3-seed 2024 홀드아웃 잔차
부분R²(TASK22 동일 0.5% 문턱)로 스크린. **gh500(500hPa 지위고도)만 통과**(평균 0.551%, 9셀=3seed×3group
전부 0.42~0.67%로 일관). 나머지 5개는 미달(PBL풍속은 기존 풍속과 r=0.97~0.99로 사실상 중복).

**STEP2(gh500을 v7 base 4-GBDT에 실주입, 캘리 강제 동결값, rolling+연도전이+스트레스셋+leakage+겨울편향)**:

| 게이트 | 기준 | 3-seed 평균 diff | 양수 | 판정 |
|---|---|---|---|---|
| ① rolling 12창 pooled | ≥+0.0010 ∧ 2/3양수 | +0.00106 | 2/3 | PASS |
| ② 연도전이 홀드아웃(2022-23→2024) | ≥+0.0008 | **−0.00105** | 0/3 | **FAIL** |
| ③ D2 스트레스셋(n=1756) | ≥+0.0005 | +0.00031 | 1/3 | FAIL |
| ④ leakage assert | 기존 gfs_* 250컬럼과 동일 소스·스케줄 | 전 행 통과 | — | PASS |
| ⑤ 겨울편향 | 겨울 편중 아님 | 겨울 +0.00027 vs 비겨울 +0.00146 | — | 편중 아님 |

①만 넘고 **핵심 관문인 ②(연도전이)에서 3/3 전부 반전**(−0.00105) — v8b·v9·TASK4-A·TASK20 ext15_all과
동일한 "rolling 소량양수 → 연도전이 반전" 패턴의 재현. ③도 미달(1/3)로 ②의 반전을 재확인. **기준 외
관찰**: 게이트②에서 seed42가 가장 크게 악화(−0.00239)했는데 STEP1에서 gh500 부분R²가 가장 높게
측정된 것과 같은 seed — rolling 신호가 2024 내부 국소 과적합이었을 가능성. **판정: gh500 기각, 라인
종결(재시도 없음, 선등록 규칙 그대로). TASK24 전체 종결 — GFS 미추출 변수 계열 레버 소진.** 상세:
`task24_gfs_extra_vars/TASK24_result.md`.

★기록 결함 정정(2026-07-27, fable 지시): 이 STEP2 결과가 그동안 단일 진실원에 미등재되어 있었음(JC 문서의
간접 인용 — STEP1 통과 수치만 — 으로만 존재해, "STEP1 통과 = 아직 미빌드 상태"로 오인될 여지가 있었음).
실제로는 STEP1 통과 후 STEP2(연도전이·스트레스셋 포함 v7 프로덕션 레시피 실주입 게이트)까지 이미 완주되어
**결정적으로 기각**된 건이었음 — 이번 절 신설로 정정.

### 4-25. ★ J-트랙 (JMA 멀티-NWP) → J0 abort·JC kill로 종결 (2026-07-26, `jtrack_JC_surface_pressure/`)

TASK23("독립 NWP 부재") 재점검: TASK23이 미확인한 JMA(일본 기상청, KMA-LDAPS·NOAA-GFS와 독립 예보센터)를
Open-Meteo Previous Runs 아카이브로 실사. **J0 실사**: JMA GSM은 previous_day2 리드(공식 배포 실행+4h15m 기반
정렬+실측)로 2022-2025 테스트기간 리키지-안전 100% 커버 — ECMWF식 커버리지 공백 없음(기계적 GO). **그러나
원안 핵심 가치제안(허브밴드 독립 풍속)이 구조적으로 획득 불가**: Previous Runs API가 기압면(hPa)·확장고도면
(80/120/180m)에 previous_dayN 리드 오프셋을 제공하지 않음(GSM·MSM·다hPa 조합에서 재현). 리키지-안전 획득
가능 = 지표 세트뿐(10m 풍속/풍향·2m기온·surface_pressure, 100% 유효). MSM(5km)은 2022 상반기 미가동 → 제외,
GSM 단독. 10m-only 전단외삽 경로는 전략 리드가 종료(marginal·기존 허브풍속과 중복 예상, "오차=입력지배" 상충).

**JC 코다(선등록 단발 S1 스크린)**: 기준 외 관찰(JMA surface_pressure가 TASK24 통과 gh500과 같은 종관계열일
가능성)만 확장·멤버빌드 없이 검정. 선등록 피처 F1=surface_pressure, F2=3h 기압경향, 문턱은 TASK22/24의 0.5% 승계.
- **F1**: S1a max|r|=0.972 (LDAPS sp와) — 문턱 0.90 초과 기각. GSM이 저해상도임에도 기존 LDAPS/GFS 지면기압과
  동일 종관 시스템 관측(독립 기압장 2개 기보유 → 탈상관 여지 없음, 예상대로).
- **F2**: S1a max|r|=0.090 통과(원 신호와 독립)이나 **S1b 부분R² mean 0.219%(9셀 0.13~0.32%, 문턱 0.5% 미달) 기각.**
  v7+CNN production-mode 2024 홀드아웃 예측력 증분 없음. 널대조는 통과후보 부재로 선등록 규칙대로 미실시.

**판정: F1·F2 동시충족 실패 → J-트랙 영구 종결.** ★TASK11 교훈 #14 정밀 재현: F2는 "새 정보원(독립)"이었으나
"예측력 있는 정보"가 아니었음 — 독립성만으로 전이 안 됨. J0 종관계열 가설도 정식 기각 해소. best=v7+CNN 유지,
ENDGAME 동결 변경 없음.

**소명 강화**: TASK23의 "커버리지+규칙 동시만족 독립 NWP 부재"가 → "전 연도 커버리지를 갖춘 유일한 독립 NWP(JMA)는
존재하나, 리키지-안전 공개 아카이브가 지표장만 노출하고 허브밴드는 구조적으로 접근 불가; 유일 잔여 지표 종관변수도
S1 스크린에서 중복(F1)·무신호(F2)로 기각"으로 정밀화. "안 찾은 게 아니라 찾았고·접근했고·리키지-안전 경로의 구조적
한계까지 규명했다" — 단 이것을 "그래서 프론티어 0.02 격차가 통제 밖 입력 기인"이라는 주장으로 확대하지 않는다
(★서사 정정, 2026-07-28: 참가자격상 상위팀도 같은 공개 데이터를 쓰므로 이 확대해석은 성립하지 않음, §9 참조).
이 실사가 소명하는 것은 "우리가 찾은 범위에서는 이 경로가 막혀 있었다"는 좁은 사실뿐이다.

### 4-26. TASK27. 기상청 "대용량자료신청" 실사 → 미발견·종결 (2026-07-27, `TASK27_kma_bulk_recon.md`)

fable의 엔드게임 재판정(R-트랙) 지시로, TASK23이 "미확인·불확실"로 남긴 유일한 각주(GDPS/RDPS
대용량자료신청이 2022-01~2025-12 과거 아카이브를 커버하는지)를 조사 전용(WebSearch/WebFetch,
신청·다운로드 없음)으로 재조사. 결과: ①기간 무공백 커버리지는 **끝내 미확인**(경량화 등급의
"최근1년" 제한만 공개 확인되고, 대용량자료신청 표준등급 자체의 보존기간을 밝힌 공개 페이지 부재 —
GDPS/KIM 데이터 목록 페이지가 플레이스홀더 응답) ②GRIB2+run/lead 내장 파일명 확인(충족) ③일반인
신청 가능 확인(승인 소요일은 미확인) ④공공누리 라이선스 확인(단 일부 상업제한 문구 상존). **①(결정적
항목) 미확인 → 선등록 규칙의 "하나라도 미충족" 분기 적용, 미발견·종결**(계정 로그인 기반 실확인은
조사 범위 밖 한계로 남김). 기준 외 관찰: "GDPS" 명칭의 역사적 UM 계보와 무관하게, 한국형모델(KIM)이
2020-04부터 전지구 현업을 전담 중임을 확인 — 모델 코어가 LDAPS(UM)와 독립이라는 가설 자체는 유효.
**데이터 경로 종료 상태 유지, 소명은 "3중 실사"→"4중 실사"로 승격**(`외부데이터_정리.md` §7).

### 4-27. TASK26(fable 재판정 시도) — TASK24 선행 기각 확인으로 미실행 취소 (2026-07-27)

fable의 엔드게임 재판정 지시문(G-트랙)이 gh500(TASK24 STEP1 통과, 부분R² mean 0.551%)을 "미빌드 상태의
유일 해제 신호"로 보고 GBDT 재학습 + 3게이트(연도전이≥+0.0010∧2/3, D2스트레스≥+0.0005∧2/3, 셔플
널테스트) 재검증을 지시. 그러나 클로드코드가 실행 착수 전 대조한 결과 **TASK24가 이미 STEP2에서 동일
게이트를 동일 프로덕션 레시피로 실행 완료**했음을 확인(4-24b): 연도전이 **−0.00105(0/3)**, D2 스트레스셋
**+0.00031(1/3)** — fable이 지금 요구한 기준을 이미, 그것도 정반대 방향으로 결정적으로 미달한 결과가
존재. 클로드코드가 이 사실을 전략 리드에게 즉시 회신, 전략 리드 재확인 후 **TASK26 미실행 취소** —
TASK24 STEP2 원판정(gh500 기각, 라인 종결)을 그대로 유지, 선등록 판정규칙("게이트 실패 시 영구
기각·재변형 금지")을 소급 적용. 재학습·재게이트 일체 진행하지 않음(GPU/CPU 컴퓨트 지출 0).
**모델링 축 재가동 철회, ENDGAME 완전 복귀.**

★기록 결함(TASK24 STEP2 미등재)이 이번 전략 오판의 직접 원인 — P0(2026-07-27, 4-24b 신설)가 이 오판을
컴퓨트 지출 전 단계에서 차단한 사례로, 방법론 규율 #16(아래 §7) 신설의 계기가 됨.

### 4-28. TASK28. 2차 평가 준비 선가동 (P1 완료·P2/P3 초안, 2026-07-27~, `task28_repro/`)

fable 지시로 보류 해제(8/14→8/17 3일 창 대응, 진입확률 0 아닌 이상 보험가치로 선가동). 진행 상태:
- **P1(완료)**: v7(GBDT+MLP+블렌드, CNN 제외) 전체 파이프라인 클린 재실행 → `submission_ficr_w1_v7.csv`
  까지 **완전 bit-identical 재현**(상세 4-24b 인접 참조 대신 `REPRODUCIBILITY_STATEMENT.md` §6). 비결정성은
  CNN 멤버 단독에 국한됨을 재확인(GBDT raw npz는 병렬연산 잡음 1e-12 수준이나 캘리브레이션 단계에서 완전
  소거, MLP는 CPU+시딩이라 애초에 bit-identical).
- **P2(초안)**: `발표_골격_v1.md` — 10분 분량 5장 구조(병목진단/CNN돌파/게이트규율/스택가치/데이터실사),
  TASK26 취소(4-27)를 게이트 규율 장의 4번째 실증 사례로 편입.
- **P3(초안)**: `2차평가_체크리스트_v1.md` — 최종파일 선택 확인(8/13·8/14 2회)·증빙서류·메일제출 형식
  체크리스트. 단 증빙 세부요건·메일 규격은 대회 공지 원문 대조가 필요해 추정 표시로 남김(서버에서
  대회 공지 원문 접근 불가).

최종파일·ENDGAME 상태 불변. 산출물은 8/10까지 분산 완료 예정, G/R-트랙(TASK26/27)과 무관하게 유일 활성 트랙.

### 4-29. 부록 S. 제출·선택·검증 축 해제 — S2 3-seed 통과(경로 A), S1/S0 사용자 행동 대기 (2026-07-27)

fable 지시(모델링 재가동 아님, 제출·선택·산출물검증 축). S2(`submission_task8_g1g2ficr.csv`를 D2
스트레스셋으로 재채점, 재학습 금지) 1차 시도는 seed42용 동결 CNN 예측만 있어 1-seed로 그쳤음(+0.00099).
**fable이 경로 A로 재지시**: `cnn_gate/gate2_evalrows_s{42,123,2024}.parquet` — JC/TASK22가 이미 기각
근거로 사용한, 진짜 2022-23 학습→2024 3-seed 독립 CNN production-mode 홀드아웃 캘리 완료 예측
(`blend_0/1/2`, 이중기준 방지 원칙대로 동일 산출물 재사용). 이 파일에 task8의 배포 shift(G2=+0.01
고정, G1/G3=0, 재적합 없음)만 원시값 가산 후 D2 스트레스셋(n=1756, 유효행 1639)으로 재채점 —
재학습 0(경로 B의 부트스트랩/freeze 검정은 경로 A가 성립해 불필요, 미실행).

**결과**: seed42 diff=+0.00079 / seed123 diff=+0.00147 / seed2024 diff=+0.00212 → **3-seed 평균
+0.00146, 3/3 양수** — 기준(≥+0.0005 ∧ ≥2/3)을 약 3배 마진으로 **통과**. FICR 이득은 전부 G2에서만
발생(G1/G3 diff 정확히 0 — shift=0 설계대로 정상 작동, 아티팩트 아님). 산출:
`task28_repro/s2_stress_rescore_3seed.py`, `s2_stress_3seed_result.csv`.

### ① 선등록 기준 대비 판정
경로 A 성립(파일 존재·컬럼 정합) → 경로 A 기준(3-seed 평균 ≥+0.0005 ∧ 양수≥2/3) 적용 → **통과**.
`submission_task8_g1g2ficr.csv` 제출 대상 확정(전략 리드 승인 전제, 사용자 제출 필요).
### ② 기준 외 관찰
G2 단독 shift라 이득이 G2 FICR에만 국한되는 것은 설계 그대로 — G1/G3 diff=0은 버그가 아니라 검증.
3개 seed 모두 방향·크기가 유사하게 양수(0.00079~0.00212, 표준편차 작음)라 경계선 신호 특유의 seed간
부호 반전 패턴이 전혀 없음 — GEFS spread류 근소통과 사례와 질적으로 다른, 안정적 통과.
### ③ 다음 액션
S1(`submission_v7cnn_fixed.csv`, 무조건 제출)과 별도로, S2 통과분(`submission_task8_g1g2ficr.csv`)도
전략 리드 승인 시 제출 대상. 두 파일 모두 사용자의 실제 DACON 제출·ΔLB 보고가 필요(서버 권한 밖).

**S0/S1/S3**: 여전히 사용자 행동 대기 — S0(팀원 질의 3건 전달), S1(제출 후 최종파일 재선택 확인).
S1/S2 각각의 ΔLB가 나와야 S3(최종파일 재판정) 진행 가능.

### 4-29b. fable 승인 + 신규 C(`submission_v7cnn_fixed_g2shift.csv`) + S3 판정규칙 선등록 (2026-07-27)

**S2 제출 승인**: 경로 A(3-seed 평균 +0.00146, 3/3) 확인 후 `submission_task8_g1g2ficr.csv` 제출 승인.
★기준 외 관찰 등재(전략 리드 지시): 경로 A seed42(+0.00079) vs 앞선 1-seed 시도(+0.00099)의 차이는
서로 **다른 CNN 실현체**(gate2_evalrows는 TASK1/2 시절 실현, 1-seed 시도는 TASK14 결정성 수정판 동결
실현) 사용에서 온 것 — 실현체가 달라도 두 값이 **동부호·유사크기**라는 점이 이번 shift 이득이 특정
실현체에만 붙은 우연이 아니라 **강건한 신호**라는 추가 증거로 기록.

**신규 C 생성**: `submission_v7cnn_fixed_g2shift.csv` = `submission_v7cnn_fixed.csv`(NaN버그 수정,
TASK14 결정성 계보) + G2에 +0.01×21600=+216 kWh 가산 후 `clip(0,21600)` 재적용. G1/G3 무변경.
목적: "재현가능(bit-identical) + NaN수정 + 게이트 통과 shift 이득" 3개를 한 파일에 결합 — 반면
`submission_task8_g1g2ficr.csv`는 TASK14 **이전** 계보(팀원 원본 CNN 실현 기반)라 재현 불가 위험이
남음(LB가 최고여도 산출물 검증 단계 리스크).

**사전점검(전부 통과)**: shape(8760,5) · 결측/음수/cap초과 0 · forecast_id 순서(sample_submission·
v7cnn_fixed 양쪽) 일치 · G1/G3는 `submission_v7cnn_fixed.csv`와 사실상 bit-identical(CSV 왕복 저장에서
오는 부동소수점 표현오차 최대 3.6e-12, kWh 단위 실질 유효자리 훨씬 아래 — 실 계산상 무변화) · G2는
전 8760행 정확히 +216.00(cap clip 발동 0건, orig가 21600-216=21384를 넘는 행 없음 확인).

**제출 계획(목적 분리, Public 사격 아님)**: A=`submission_v7cnn_fixed.csv`(NaN수정 단독 효과 측정) ·
B=`submission_task8_g1g2ficr.csv`(shift 단독 효과 측정, 이미 승인) · C=`submission_v7cnn_fixed_g2shift.csv`
(결합 효과 측정, 최종파일 1순위 후보). 팀 쿼터 내 하루 분산 제출 가능.

**S3 최종파일 판정규칙(ΔLB 확인 전 선등록, 수정 금지)** — L0=0.65184, ε=0.0005:
① L_C ≥ L0−ε → **C 선택**(기본 경로) · ② (①미충족이고) L_A ≥ L0−ε → **A 선택** · ③ A·C 둘 다
L0−ε 미만 → **max(L0, L_B) 선택** + SD기반(TASK15, SD≈0.002) 복원오차 소명 강화 + 수정판 자체 재점검 ·
④ 밴드 내 동률이면 항상 재현 가능한 쪽 우선(**C > A > B > 원본**) · ⑤ |L_A − L0| > 0.001이면 이미
소명된 752셀·3시각(0.034%) NaN버그 영향 범위로 설명 불가한 크기 → **선택 보류, 원인 조사**(정렬·clip·
인코딩 재점검, 성급한 선택 금지).

**진행 상태**: A/B/C 3건 모두 사용자의 실제 DACON 제출·ΔLB 보고 대기. CNN 재학습·시드 추가생성 금지
유지. S1(A)은 원래 무조건 제출 대상, B는 이번에 게이트 통과로 제출 승인, C는 이번에 신규 생성 —
셋 다 위 판정규칙 적용 대상.

### 4-30. 부록 T (팀원 코드 감사) + T4 재생성 실행 (2026-07-27)

**T4(제출 전 선행 확인, 최우선)**: `submission_v7cnn_fixed.csv`의 결정성 계보를 파일 mtime으로 직접
확인 — 생성시각 **7/23 22:29**, TASK14 결정성 패치(`cnn_common.py`) 적용시각 **7/24 13:20** → **패치보다
하루 먼저 생성된 패치 이전 계보**로 확정(`task5c_repro/submission_ficr_w1_v7_cnn_repro.csv`와 완전
동일 내용도 재확인, 같은 계보). 규칙대로 **seed=42 고정 1회 결정론적 재생성** 실행:
1. 기존 체크포인트 보존: `task5c_repro/cnn_production_s42.pt` → `cnn_production_s42_PRE_TASK14.pt`로 백업.
2. `train_cnn_production.py` 재실행(패치된 `cnn_common.py` 자동 import, 2022-2024 전체 1회학습, seed=42,
   10초 소요, best_val_mse=0.01717) → 새 체크포인트 생성.
3. `infer_cnn_production.py` 재실행 → `submission_ficr_w1_v7_cnn_repro.csv` 갱신(test 격자매칭 100%,
   결측/음수/cap초과 0).
4. **결과 무조건 채택**(실현체 비교·선택 절대 금지 원칙 준수) → `submission_v7cnn_fixed_v2.csv`(=v7cnn_fixed′)로
   루트에 저장. 사전점검 전부 통과(shape, forecast_id 순서, 결측/음수/cap초과 0). 구 실현체와 corr≈0.999
   (통계적으로 동형이나 값은 다름 — 예상된 결과, TASK14가 이미 문서화한 "결정성 수정은 향후 실행만
   보장, 과거 실행 재현은 보장 안 함"의 실제 사례).
5. C′ = `submission_v7cnn_fixed_v2_g2shift.csv` = v7cnn_fixed′ + G2 +216 shift, clip(0,21600) — 사전점검
   전부 통과(G1/G3 vs v7cnn_fixed′ max_diff=3.6e-12 부동소수점 잡음 수준, G2 전 8760행 정확히 +216).

**제출 계획 갱신**: A = `submission_v7cnn_fixed_v2.csv`(v7cnn_fixed′) · B = `submission_task8_g1g2ficr.csv`
(변경 없음, 원래부터 TASK14 이전 계보임을 전략 리드가 인지하고 승인 — B는 "재현성"이 아니라 "shift
효과 측정" 목적이라 이 재생성 대상이 아님) · C = `submission_v7cnn_fixed_v2_g2shift.csv`(C′). S3
판정규칙은 4-29b에 이미 선등록된 그대로 적용(L0=0.65184, ε=0.0005).

**부록 T 전문 (전략 리드 코드 감사 결과)**:

① **체크포인트 부재 + 매실행 재학습 + 비결정 3요소 = 원본 실현 영구 재현불가**. 팀원 원본
(`v7_cnn_submission.py`, `spatial_cnn_v7_top.py`) 코드 감사로 직접 확인: `torch.save` 호출이 코드
어디에도 없음(`state_dict()`는 조기종료용으로 메모리 내에서만 보관·복원, 디스크 저장 없음) → 원본
LB 제출을 만든 그 실행은 재실행할 때마다 처음부터 다시 학습하는 구조이며, 그 학습 자체가 TASK14 이전
GPU 비결정 연산(cuDNN·AdaptiveAvgPool2d backward 등, §2 4건) 위에서 돌아갔다. 세 조건(체크포인트 없음
+ 매실행 재학습 + 비결정 연산)이 겹쳐 **원본이 실제로 만든 그 결과값은 사후에 영원히 복원 불가** —
이것이 `REPRODUCIBILITY_STATEMENT.md`가 "동형 실현"이라는 개념으로 우회하는 이유의 근본 원인.

② **TASK1 편향 코드 확증**: CNN 조기종료 검증셋 = cal창(채점 대상, `v7_cnn_submission.py`에서 train
마지막 10%를 val로 쓰지만 그 마지막 10%가 캘리 적합·채점 구간과 겹치는 프로덕션 방식이라 CNN만
사실상 y_cal 근방을 봄) · 캘리 적합=채점 in-sample(동일 cal창 재사용) · 이 두 편향을 제거한 우리
재현(TASK1)에서 팀원 로컬 seed42(+0.00282)가 우리 seed42 전 게이트 음수(-0.00599/-0.00161/-0.00325)로
완전 반전 — 편향이 최소 부분적으로 로컬 수치를 부풀렸을 개연성 확인(상세 `CNN_TASK1_gate_reverse_validation.md`).

③ **NaN 처리 버그 3변종, 공통 메커니즘 = "(0,0) 격자셀만 채워짐" → 나머지 셀 NaN 잔존 → CNN
forward가 NaN 오염 → 하류에서 조용히 v7단독 폴백**. 코드 직접 대조로 3변종 확인:
  - 변종1(`v7_cnn_submission.py::_nan_to_med`, train 전용): `arr.shape[1]`로 루프하는데 이 시점 `arr`는
    아직 transpose 전 `(n,H,W,C)` 형태라 **H(높이) 축을 채널인 것처럼 순회** — 실害 없음(train 격자
    결측 0개라 이 버그 경로 자체가 발동 안 함).
  - 변종2(`v7_cnn_submission.py::predict_cnn`, **실제 LB 제출을 만든 추론 경로**): `ldaps_flat =
    ldaps.reshape(n,-1)`로 **완전 평탄화**한 뒤 `for i in range(c_l): ldaps_flat[:,i]=...` — C-order
    평탄화라 이 인덱싱이 실제로 접근하는 것은 **격자 좌표 (h=0,w=0)의 채널 0..c_l-1뿐**. 다른 모든 셀
    (h>0 또는 w>0)의 NaN은 전혀 채워지지 않고 그대로 남아 CNN 입력으로 들어가 forward 전체를 NaN
    오염시킴 → 하류 `mask=~isnan(cnn_vals)`가 이 행을 걸러 **조용히 v7단독 폴백**(로그에 경고 1줄만
    남고 나머지는 정상 진행되는 것처럼 보임). TASK6가 발견한 "752개 결측 스칼라, 3개 시각 집중"이
    바로 이 경로의 산물.
  - 변종3(`spatial_cnn_v7_top.py::_nan_to_med`/`predict_cnn`): 변종1과 동일한 함수(H축 오류)를 이
    별도 실험 스크립트에서는 **학습·추론 양쪽 모두**에 재사용 — 이 스크립트 자체의 실측 영향 범위는
    별도 감사 대상 아님(프로덕션 제출 경로가 아님).
  세 변종 모두 "결측 대체"라는 같은 의도를 겨냥했으나 축/형태 처리를 잘못해 사실상 대체가 거의
  일어나지 않았다는 공통점 — **은폐하지 않고 정직하게 기재**(아래 REPRODUCIBILITY_STATEMENT §7 반영).

④ **W_CNN(=0.20) 재탐색(w축) 미재개 사유**: ②에서 확인된 편향(조기종료=cal창, in-sample 캘리)이 걸린
채로 산출된 팀원 로컬 수치는, 설령 그 수치가 w=0.20 이외 값에서 더 유리해 보이더라도 **선택 근거로
쓸 수 없다** — 방법론 규율 8번("프록시 검증 금지. 선택은 판정과 같은 해상도의 검증에서만") 위반이기
때문. HPO 라운드(4창 프록시→12창 전멸)가 이미 실증한 바로 그 함정과 동일 구조. W_CNN은 `cnn_common.py`
주석 그대로 "고정 — 재탐색 금지(Public 과적합)" 유지, 이번 감사로도 재개 근거 없음(오히려 재개하지
말아야 할 이유가 하나 더 늘었을 뿐).

**REPRODUCIBILITY_STATEMENT.md 반영**: ①③을 §7로 추가(은폐 금지 원칙 — 소명 신뢰도 자체가 산출물
검증 단계의 자산이라는 전략 리드 판단).

**패키징**: 팀원 코드 원본 위치 `/home/2022113196/v16_exp`는 **권한 거부(700, 소유자 전용)** —
클로드코드 실행계정으로 목록조회조차 불가, 확인·복사 불가(사용자/팀원 조치 필요, 아래 미해결
참조). 단, 우리 저장소에 이미 사본으로 들여와 있던 `v7_cnn_submission.py`·`spatial_cnn_v7_top.py`의
하드코딩 절대경로(한글 포함 `풍력발전/풍력발전/`)는 확인 결과 **동일 데이터가 이미 우리 프로젝트
루트/`data/`에 전부 존재**함을 확인해, 팀원 계정 접근 없이 ASCII 상대경로(`os.path.dirname(__file__)`
기준)로 정규화 완료 — 로직·아키텍처·seed 변경 없음. 원본 경로는 `v16_exp_PATHS_ORIGINAL.md`에 보존.

**미해결**:
- `/home/2022113196/v16_exp` 자체 접근 불가 — 위 두 파일 외 추가 코드가 있는지 확인 불가. 팀원의
  직접 복사 또는 읽기권한 부여(`chmod o+rx`/그룹공유) 필요.
- ms/ts(multistage/timeseries) 2024 OOF 여전히 미제공 — `발표_골격_v1.md` 한계 절에 "절대값 비교
  불가"로 명시(아래 4-30b 참조 대신 발표 골격 문서 자체에 반영).

### 4-31. V1~V3 제출 전 필수 검증(전부 통과) + 선등록 ε 갱신 + 패키징 후속 (2026-07-27)

★**시점 명시(사후 정당화 방지)**: 이 절 전체는 A/B/C 어느 파일도 아직 실제 DACON에 제출되지 않아
**ΔLB가 전혀 알려지지 않은 시점**에 작성됨. 아래 ε 조정은 결과를 보고 기준을 맞춘 것이 아니다.

**T4 분기 처리 승인**: 전략 리드가 seed42 1회 재생성·무조건 채택·실현체 비교 없음 처리를 적정하다고
확인. 제출 대상 재확정: A=`submission_v7cnn_fixed_v2.csv` · C=`submission_v7cnn_fixed_v2_g2shift.csv` ·
B=`submission_task8_g1g2ficr.csv`(측정 전용, 재현성 주장 없음).

**V1~V3 검증(사전점검과 별개, 제출 전 필수) — 전부 통과**. 상세 수치·판정 근거는
`REPRODUCIBILITY_STATEMENT.md` §8(2차 검증 핵심 소명 문서로 지정)에 등재, 요약:
- V1(팀원 원본과의 그룹별 상관·MAD): G1/G2 corr 0.999·MAD 2%대, **G3만 MAD 8%대**로 크지만 이는
  TASK13(G3 SD가 G1/G2의 2.6~2.8배)·TASK7(G3만 3/3 악화) 등 **기존에 반복 확인된 "G3 실현체 변동성
  큼" 패턴과 정합** — 새 이상신호 아님, 통과.
- V2(결측영향 3행 실제 CNN 적용): forecast_2345/4050/4758 3행×3그룹=9셀 전부 v7단독과 다른 값 확인,
  **폴백 0건**, 통과.
- V3(독립 프로세스 2회, 실제 production 시나리오): best_val_mse·CNN raw예측·state_dict 전부
  `np.array_equal`=True(bit-identical), 게다가 이 두 독립실행의 state_dict가 **실제 제출 체크포인트
  (`cnn_production_s42.pt`)와도 bit-identical** — V3가 검증한 결정성이 임의 재현이 아니라 제출 대상
  그 자체의 결정성임을 확인. (참고: Python `hash()`는 `PYTHONHASHSEED` 프로세스간 랜덤화로 두 실행에서
  값이 달랐음 — 결정성과 무관한 거짓신호임을 인지하고 `np.array_equal`로 판단, 낚이지 않음.)

**3건 전부 통과 → A/C 제출 진행.**

**선등록 ε 갱신(실현체 교차비교 노이즈밴드)**: 기존 S3 규칙(4-29b)의 ε=0.0005는 **같은 실현체 내부**
비교(예: shift 적용 전/후, 동일 CNN 가중치 위에서의 변화)에는 적절하나, **v7cnn_fixed_v2(우리 재현
실현체) vs 팀원 원본(L0=0.65184, 별개 실현체)** 비교에는 과소평가된 기준이라는 게 전략 리드 판단 —
TASK15가 실측한 실현체간 SD≈0.002(§3, `REPRODUCIBILITY_STATEMENT.md`)를 근거로 **실현체 교차비교
전용 ε=0.002로 갱신**, 같은 실현체 내부 비교는 기존 0.0005 유지(이원화).

갱신된 S3 규칙: ① L_C ≥ L0−0.002 → **C 선택** ② (①미충족) L_A ≥ L0−0.002 → **A 선택** ③ 둘 다 미달
→ max(L0, L_B) 선택 + SD기반 소명 강화 ④ 밴드 내 동률이면 재현성 우선(C>A>B>원본) — 변경 없음.

**대칭성 조건(이해충돌 자백, 전략 리드 자진 명시)**: ε을 넓힌 것이 "우리 파일이 통과하기 쉽게" 만드는
효과가 있다는 이해충돌 가능성을 전략 리드가 스스로 인정 — 이를 상쇄하기 위해 **대칭 조건**을 동시
등록: C가 L0보다 +0.001 높게 나와도 그 정도 차이는 같은 노이즈밴드(ε=0.002) 안에 있으므로 "실질
개선"이라고 주장하지 않는다(밴드 안에서는 방향 불문 우열을 논하지 않음). 그리고 **B는 이 비교에서
아예 선택 후보가 될 수 없음**을 재확인 — B가 우연히 제일 높은 점수가 나와도 그것을 이유로 B를
고르는 것은 "결과 보고 유리한 실현체를 사후에 고르는" realization shopping이며 이 프로젝트가 TASK15로
이미 영구 종결시킨 바로 그 패턴이기 때문에 원천 금지.

**패키징 후속**: `/home/2022113196/v16_exp`(권한거부) 의존성은 **C가 선택되면 재현 경로 자체에는
불필요**(C는 우리 저장소 안에서 완전히 재현되는 v7cnn_fixed_v2 기반 — 팀원 원본 체크포인트 불필요)
— 우선순위 하향, 이력 문서(`v16_exp_PATHS_ORIGINAL.md`)로만 유지. 단 산출물 검증 단계의 완전성을
위해 **팀원에게 그 폴더의 파일 목록만이라도 요청**(코드 자체 이관은 후순위).

### 4-32. A 실제 LB 등재 + 규칙⑤ 발동 "S3⑤" 그룹별 진단 (2026-07-27)

**A 등재**: A=`submission_v7cnn_fixed_v2.csv` 실제 제출 결과 — LB=**0.6490737**(1-NMAE 0.8742969,
FICR 0.4238505), 스코어대장(§5)·본 절에 기록. L0(팀원 원본 0.65184) 대비 **ΔLB=−0.00277**.
갱신 S3 규칙② 판정(4-31, ε=0.002 교차실현체): 통과선 L0−ε=**0.64984**에 0.00077 미달 →
**A 단독으로는 규칙② 탈락**. v7(CNN無) 대비로는 NMAE +0.00409(팀원 CNN의 +0.00296보다 개선폭 큼) ·
FICR −0.00446(팀원 CNN은 +0.00219, 부호 반대) — A는 3파일(v7/팀원CNN/A) 중 1-NMAE 최고·FICR
최저: **평균오차 우위·정산밴드(FICR) 열위**라는 일관된 트레이드오프.

**규칙⑤ 발동 조건 성립**: |ΔLB|=0.00277 > 0.001은 4-29b가 이미 "NaN버그 소명범위(752셀·3시각,
0.034%)로 설명 불가한 크기 → 선택 보류·원인조사"로 못박아둔 문턱을 초과 — 그룹별 분해 진단
("S3⑤") 발동. **재학습 금지, 기존 저장 예측만 사용**. 스크립트 `task28_repro/v3_5_group_diagnosis.py`.

**Part 1(라벨 有, 2024 production-mode 홀드아웃)** — canonical `calibrate_total`(월별 롤링 재캘리)
적용 후 base(v7, `m1_holdout_preds_2024.parquet`) vs 우리 CNN(seed42, `cnn_gate/gate2_evalrows_s42.parquet`)
그룹별 비교(공통 평가행 기준):

| 그룹 | ΔNMAE | ΔFICR | Δ고출력밴드 적중률(오차율≤6%, 실측≥50%cap) |
|---|---|---|---|
| G1 | −0.00028(개선) | +0.00078(개선) | −0.0042 |
| G2 | +0.00176(악화) | +0.00382(개선) | −0.0053 |
| G3 | −0.00149(개선) | **−0.01187(대폭 악화)** | **−0.0161(대폭 악화)** |

**Part 2(라벨 無, 2025 test, 블렌드 역산 서술통계)** — `(제출값 − 0.8·v7)/0.2`로 역산한 CNN 기여분,
팀원 원본 CNN vs A(v7cnn_fixed_v2) 실현체 차이:

| 그룹 | corr | 평균차(팀원−A) | %of cap |
|---|---|---|---|
| G1 | 0.9774 | +294.0 kWh | 1.36% |
| G2 | 0.9764 | +13.1 kWh | 0.06% |
| G3 | 0.9693 | **+2796.8 kWh** | **13.32%** |

**진단 결론: ①G3 주도 확정, TASK13 계통차(+2751kWh)와 정합 — 이상신호 아님(②G1/G2 주도 가설 기각)**.
FICR 붕괴·고출력밴드 적중률 붕괴 둘 다 G3에 압도적으로 집중되고 G1/G2는 오히려 FICR가 소폭
개선됐다(부호 정상). Part 2도 두 실현체 간 값 차이가 G3에서만 유독 크다는 것을 독립 경로로
재확인(13.3% vs 1.4%/0.06% of cap) — TASK13(G3 SD가 G1/G2의 2.6~2.8배)·TASK15(실현체 로또
비전이성, ρ=0.02)·4-31 V1(G3 MAD 8%대 vs G1/G2 2%대)에서 이미 반복 확인된 **"G3 실현체 변동성"
패턴이 이번에 실제 LB로 발현**된 것. G1/G2에 이상 없음(정렬·clip·인코딩 버그 가설 기각) →
추가 원인조사 불필요, 규칙⑤의 "보류·조사" 요건 충족·종결.

**금지 재확인**: 이 결과를 W_CNN 재조정(w축) 근거로 쓰는 것은 절대 금지 — "우리 CNN이 FICR을
해치니 w를 낮추자"는 v7a로 이미 종결된 Public 기반 블렌드비율 스윕과 동일 구조(부록 T ④, 방법론
규율 8). A의 G3 결손은 **실현체 뽑기(로또) 문제**이지 CNN 가중치 자체의 결함이 아님 — 팀원 원본도
동일 CNN 구조·동일 w=0.20을 쓰고, 단지 다른 실현체(다른 초기화·학습경로)일 뿐.

**현재 상태**: A는 규칙② 탈락 확정. C(`submission_v7cnn_fixed_v2_g2shift.csv`) 제출·ΔLB 대기 —
C가 규칙①(L_C≥0.64984)을 통과하면 C 선택, 실패하면 규칙③(max(L0,L_B) 채택 + 위 G3-실현체 소명을
SD기반 근거에 추가해 강화)으로 진행. 최종파일은 아직 미변경(팀원 원본 유지).

### 4-33. ★ TASK29 R0. ENDGAME 부분 재개(사용자 명시 지시) — 검정력 교정, 널대조가 재개근거 ②를
반증(2026-07-27, `task29_power_rescreen/TASK29_R0_power.md`)

**재개 배경**: fable이 사용자 명시 지시로 ENDGAME을 원시격자 CNN 모델 범위에 한해 부분 재개
(TASK29). 근거 ①T4에서 CNN 학습 10초 확인(K=30 배깅도 5분대, 비용가정 붕괴) ②본게이트가 검정력
부족(CNN SE=0.0028 vs 3-seed게이트 문턱 +0.0010 → 참효과 +0.002 탐지율 <50%, TASK11-PC가 S1에서
교정한 오류가 본게이트엔 미적용). 데이터경로·GBDT피처·캘리·블렌드비율·실현체 선택은 여전히 봉쇄,
원시격자 모델만 재개 대상. R0(선행 필수, 후보 실행 금지)부터 시작 — 고정 base + CNN 학습시드
1001-1030(30개) 스윕으로 순수 CNN-시드 SD 실측, y-셔플 널대조(2001-2010, 10개) 동시 실행.

**R0 실측(base=seed42, 지시 그대로)**: real(30개) 평균 diff=−0.00237·SD=0.00177, null(10개)
평균=−0.00171·SD=0.00173. **real−null=−0.00067, t=−1.05(유의하지 않음, 방향도 기대와 반대)**.
K-배깅(30개 중 K개 무작위추출→예측평균→재채점) 곡선: K=20에서 실측 SE≈0.00091까지 줄지만 평균은
0이나 양(+)이 아니라 오히려 −0.0032까지 더 음수화.

**★자체 추가 검증(지시에 없었으나 R0 신뢰성 확보 위해 클로드코드가 즉시 확장)**: base=seed42가
TASK2(4-18 이전)에서 이미 **3개 base 중 유일하게 CNN과 음의 방향**이었던 base라는 기존 기록을
근거로, 동일 파이프라인을 base=seed123(축소판, real 10개/null 5개, GPU 5.4분)에도 실행 —
real=−0.00096·SD=0.00213, null=−0.00255·SD=0.00133, **real−null=+0.00160, t=+1.78(역시 유의하지
않으나 방향은 base42와 정반대)**.

**★★핵심 발견: real-null 차이의 부호가 base42(−)와 base123(+)에서 정반대로 뒤집힘.** 두 base
개별 t값 모두 |t|<2로 유의하지 않고, 방향까지 엇갈린다는 것은 "참효과가 있는데 노이즈에 묻혀 안
보인다"(재개근거 ②의 전제)가 아니라 **"이 설계(단일 base 고정) 자체가 real과 null을 구분하지 못하며,
그 결과가 어느 base를 골랐는지에 좌우된다"**는 뜻 — TASK2가 남긴 "base42만 유별나다"는 관찰과
정합. CNN-시드 순수분산(SD≈0.0018~0.0021)이 fable의 SE=0.0028 추정과 같은 자릿수라는 점에서
재개근거 ②의 "검정력 부족" 자체는 확인되지만, 검정력을 높여도 감지할 실체가 있다는 근거는 R0
스스로 제공하지 못함 — **재개근거 ②는 "미검정력"에서 "확증불가"로 재평가되어야 함**.

**권고(재개 지속 여부는 fable 판단)**: R1을 현재 설계(단일 base 고정 K-배깅)로 그대로 진행하면
이번에 발견한 바로 그 함정(base가 real·null 방향을 함께 좌우)에 R1 자신이 빠짐 — 계속하려면 최소
base 42/123/2024 세 개 전부에서 real vs null을 병행 측정해 방향이 일관될 때만(TASK2 원등록 규칙
"3 base 중 2/3+"과 동일 정신) 신호로 인정해야 함. 이를 생략하고 R1을 승인하면 방법론 규율 8(프록시
검증 금지)·13(양성대조 캘리브레이션) 위반. 비용은 낮음(3-base 확장판도 GPU 약 20분대) — 승인 시
즉시 착수 가능.

**현재 상태**: R0 1차 산출 완료·보고. R1(고-K 배깅 후보화) 착수는 위 발견을 반영한 재설계(3-base
병행) 승인 대기. 데이터/캘리/블렌드/실현체선택 경로는 여전히 봉쇄 불변.

**다음 액션**: A는 등재 완료(4-32, 규칙② 탈락)·C는 사용자의 실제 제출 + ΔLB 보고 대기(B는 이미
승인, 제출 여부 미확인). S0(팀원 질의, v16_exp 파일목록 요청 포함)도 계속 대기.

### 4-34. B·C 실제 LB 등재 + S3 규칙③ 발동 — 최종파일 원본 확정 (2026-07-28)

**4건 실측 완료**: 원본(팀원 v7+CNN) **0.65184** / C(`submission_v7cnn_fixed_g2shift.csv`)
**0.6491906** / A(`submission_v7cnn_fixed_v2.csv`) **0.6490737**(4-32 기 등재) / B
(`submission_task8_g1g2ficr.csv`) **0.6482289**.

**S3 판정(4-29b 선등록 규칙 그대로, L0=0.65184·ε=0.0005 → 통과선 0.65134)**:
① L_C≥통과선? 0.6491906 < 0.65134 → 미충족. ② L_A≥통과선? 0.6490737 < 0.65134 → 미충족.
**③ A·C 둘 다 미달 → max(L0, L_B)=max(0.65184, 0.6482289)=0.65184 선택 → 원본 확정.**

**결론: 4건 중 원본(팀원 v7+CNN, LB 0.65184)이 그대로 최종파일 유지.** A/B/C 전부 원본보다 낮아
NaN버그 수정판·G2 shift 보정 모두 LB 기준으로는 원본을 넘지 못함 — 4-32의 S3⑤ 그룹별 진단(G3
실현체 변동성이 A의 FICR 붕괴 주도)과 정합적인 결과. **제출·선택·검증 축(부록 S/T) 완전 종결.**

### 4-35. ★ TASK29 R1. 3-arm(real/null/shrink) × 3-base 재설계 — ③판정, ENDGAME 재확정 (2026-07-28,
`task29_power_rescreen/TASK29_R1_result.md`)

fable 재설계 지시(R0의 "단일base 함정" 발견 반영): base∈{42,123,2024} 전부에서 real(K=20 배깅
CNN, 신규시드)·null(K=20 y셔플)·shrink(λ·base+(1−λ)·ȳ, λ는 base42 첫 cal창에서 1회 그리드서치
후 동결) 3팔 병행 측정. 결과: real 3/3 모두 base 대비 음수(0/3 양수), null도 3/3 모두 음수(0/3
양수), real>null은 2/3(평균차 +0.00058, 문턱 +0.0010 미달). **shrink 팔은 λ=1.0(무수축)으로 축퇴**
— 테스트한 λ∈[0.50,1.00] 전 구간에서 조금이라도 수축시키는 것이 항상 손해였다는 뜻, "CNN 이득=값싼
수축"이라는 가설②를 오히려 약화시키는 정황. 선등록 해석표(①/②/③) 기계적 적용 결과 **③(CNN 기대
정보가치 미확립) 성립 → ENDGAME 최종 종료** 재확정. 부수진단(팀원 원본 CNN vs 우리 CNN v2의
SD비/고출력 수축률)도 서로 비슷한 규모(0.985~1.002 vs 0.938~0.973)로 "A의 붕괴가 우리 CNN만의
과잉수축 탓"이라는 가설을 뒷받침하지 않음. **배깅 축(TASK2·R1) 영구 종결.**

### 4-36. ★ TASK30. 공간표현 재설계(GAP제거+위치조건부풀링+그룹head) — Gate-W1 FAIL, 표현 축 영구
종결 (2026-07-28, `task30_spatial_repr/TASK30_GateW1_result.md`)

재개근거: 팀원 CNN의 `GlobalAvgPool2d((1,1))`가 4×4/3×3 격자를 조기에 채널별 전역평균으로 뭉개
(a) 위치정보가 head에 전달 안 됨 (b) G1/G2/G3(격자 내 다른 좌표)가 완전히 동일한 pooled벡터+공유
head를 써 그룹을 구분할 구조적 경로가 없음 — R1의 real≈null이 "격자에 정보 없음"이 아니라 "구현이
뭉갠 것"일 가능성. `SpatialCNNPositionAware`(`task30_spatial_repr/cnn_spatial_v2.py`) 신규 구축:
LDAPS 브랜치 GAP 제거해 공간맵 보존, `turbine_geometry.py`의 17기 좌표 기반 가우시안 커널(σ=2km,
1회고정)로 그룹별 격자셀 가중치 산출(LDAPS 그룹간 상관 0.52/0.79/−0.03로 실제 구분 확인, GFS
3×3은 25km급 해상도라 부지 전체가 단일 셀에 담겨 그룹간 위치가중치 완전 동일 축퇴 — 정직하게 공유
GAP 유지) → 그룹조건부 head. 용량 확대(ch1 32→48·ch2 64→96)·에폭 확대(50→150)·patience(8→15)도
동시 적용(과소적합 의심 대응).

task29 하네스 그대로 재사용(3-base×K=20 null대조). **Gate-W1 문턱(real−null 3/3 방향일치 ∧
평균≥+0.0015 ∧ real vs base 3/3 양수, 완화 없음) 세 조건 모두 불충족 → FAIL**(real−null 평균
−0.00014, 오히려 R1의 원본 아키텍처(+0.00058)보다 나쁨). 위치정보를 쓸 구조적 경로는 이번에 실제로
뚫렸음(그룹별 가중치가 실제로 다름을 자체 검증)에도 신호가 안 잡힘 — "구현이 뭉갰다"는 가설에
불리한 증거. **지시문 규정대로 표현 축 영구 종결**(Gate-W2/W3·LB프로브 진행 안 함).

> ★★ TASK34 정정 주석(2026-07-29, `task34_grid_geometry_fix/`, fable 지시): 사용자 제공 원자료
> (`task30_spatial_repr/ldaps_grid_latlon.csv`)를 직접 대조한 결과, 위 Gate-W1은 **지리적으로
> 뒤섞인 래스터** 위에서 수행됐음이 확인됨. LDAPS 16격자는 4×4 정격자가 아니라 4행×5열 격자의
> 다이아몬드 배치(네 모서리 결측: 행0/행3은 3개 셀만, 행1/행2가 5개 셀 전부)인데, `cnn_common.py`의
> `reshape(4,4)`(TASK1~33 전체 CNN 게이트가 공유하는 코드)가 이를 무시하고 flat index를 그대로 4×4로
> 접었다. 그 결과 래스터 인접 24쌍 중 8쌍이 허위 인접이며 최악은 격자4↔8(같은 실제 행의 반대쪽 끝,
> 도메인 전폭 6.14km)이 세로로 인접 취급됨. 전역평균(GAP)은 순열에 불변이라 이 결함을 가려주지만,
> W-B의 위치조건부 풀링(터빈좌표 기반 가우시안 커널)은 이 뒤섞인 지도를 그대로 진지하게 사용했다 —
> TASK30이 원본(GAP) 아키텍처보다도 나빴던(real−null −0.00014 < R1의 +0.00058) 현상은 이 결함으로
> 설명 가능(방법론 노트). **결론 정정: "위 Gate-W1 FAIL은 지리적으로 뒤섞인 래스터 위에서 수행된
> 결과이며, 공간 모델링은 올바른 지도 위에서 아직 검정된 바 없다."** 표현 축의 "영구 종결"은 이
> 결함이 발견된 시점부로 잠정 철회하고, 올바른 4×5 래스터(+결측 마스크 채널, 원본 SpatialCNN
> 아키텍처 그대로, 변수 하나만 교체)로 동일 Gate-W1(완화 없음)을 TASK34에서 재검정한다.
> ★★후속(4-42): TASK34 재검정 완료 — **Gate-W1 FAIL**(real−null −0.00127, 뒤섞인 지도였던 이
> TASK30 결과(−0.00014)보다도 나쁨). 올바른 지도에서도 신호가 없어 **표현 축 영구 종결을
> 재확정**(잠정 철회 종료) — 상세는 4-42 및 `task34_grid_geometry_fix/TASK34_result.md` 참조.
> (GFS 9격자는 정격자 3×3 —
> `reshape(3,3)` 정확, 수정 불필요. TASK29 R1의 "배깅 축 영구종결" 판정 자체는 이 결함과 별개 질문
> (K-배깅의 한계이득)이라 유지하되, R1의 real/null CNN도 동일하게 뒤섞인 지도 위에서 학습됐다는 점은
> 참고용으로 기록.)

### 4-37. ★★ fable 통합 지시 (2026-07-28) — 이전 지시 전부 대체, TASK31(타깃품질)·TASK32(ECMWF
3차 재시도) 신규 착수 + 발표 서사 정정

TASK29(4-35)·TASK30(4-36)으로 배깅·공간표현 두 축까지 완전 종결된 직후, fable이 이전 모든 지시문을
대체하는 통합 지시를 발행. 핵심:

**고정 사실 갱신**: 최종파일 불변(팀원 v7+CNN, LB 0.65184). 제출 4건 실측 완료·규칙③으로 원본
확정(4-34). 종결축 목록 갱신: 데이터경로 4중봉쇄·GBDT피처·캘리(TASK18/20 동결)·수작업 물리피처
(부록U)·블렌드비율(v7a)·실현체선택(TASK15)·gh500(TASK24 STEP2)·배깅(TASK2·R1)·공간표현(TASK30
Gate-W1 FAIL). fable 자신의 재개가설 5연속 실패 이력(GEFS spread 근접실패, ECMWF planB, TASK26
취소, TASK29, TASK30)을 근거로 아래 두 신규 트랙도 사전 할인하고 볼 것을 명시.

**TASK31(최우선, X-1만 우선 착수, 판정 없음)**: v7의 최대 자체 레버(clean downweight +0.0021)가
이진마스크×상수 0.3인데 `clean_1/2/3`이 무엇을 표시하는지 팀이 모른다는 점에 착안(HEFTCom2024 우승팀
SVK가 발전소 가용성 문제 처리를 성공요인으로 명시한 것과 정합) — SCADA 10분 원자료(`data/
scada_vestas_train.csv`·`data/scada_unison_train.csv`, G1=vestas01-06/G2=vestas07-12/G3=
unison01-05)로 clean==0 시간대를 원인분류·연속블록길이·그룹간 동시성·시각/월/계절분포·저온고습(착빙)
비율·2024 홀드아웃 채점행 오차기여도 6항목으로 "이해"(마스크 재구성 아님 — 인접 실패 cleanmask v2와
구분). `clean_1/2/3`은 train전용(규율#5)이라 산출물은 학습가중 재설계 후보뿐, 테스트 피처 불가.
X-2(가중 재설계) 착수는 X-1 보고 후 fable 결정, 임의 진행 금지.

**TASK32(X-1과 병렬)**: ECMWF 3번째이자 마지막 재시도. 근거: TA감사가 기존 기각사유를 "전이실패"가
아니라 "검증설계 결함(2024내 학습→2024내 평가라 2025 전이를 애초에 검증한 적이 없었음)"으로 정정
(4-17 각주 참조)한 것 + HEFTCom2024 상위 2팀 모두 멀티NWP가 1번 기법. ECMWF 실질 커버리지가
2024-03-08~라 표준 연도전이 하네스 사용 불가 — 현 프로덕션 base(동결 CNN 예측 재사용, CNN 재학습
금지)에 ECMWF 멤버를 얹는 구조로 설계, 학습구간은 ECMWF 유효구간 내로 한정. 게이트: G1 D2
스트레스셋 평균≥+0.0005∧≥2/3, G2 널테스트(ECMWF 컬럼만 24h블록셔플 시 이득소멸 Δ≤+0.0003). 둘 다
통과시에만 LB프로브 1회(승인 후), ΔLB≥+0.0010일 때만 교체 상신. 미통과 시 **ECMWF 라인 영구
종결(3번째이자 마지막 시도)**.

**★발표 서사 정정(최우선, 제출결과와 무관·즉시 시행)**: "프론티어와의 0.02 격차는 우리가 접근 불가능한
입력 때문"이라는 서술을 전부 폐기 — 참가자격이 대학(원)생·취준생이고 기관전용 데이터가 규칙위반이므로
상위팀도 같은 공개 데이터를 쓰며, 이 주장은 Q&A에서 붕괴한다는 것이 사유. 대체 서술: "피처·캘리·
후처리·손실·모델클래스·공간표현(2종)·데이터경로를 선등록 게이트와 널대조로 전부 배제했다. 격차가
존재하고 같은 입력으로 달성 가능하다는 것도 안다. 우리는 그 경로를 찾지 못했고, 어디에 없는지는
정확히 안다." `발표_골격_v1.md`(1장·5장)·본 문서(4-23·J-트랙 4-25·§9)를 이 기조로 개정 완료.
`REPRODUCIBILITY_STATEMENT.md`는 이 서사를 담고 있지 않아 수정 불필요(확인 완료).

**참고자료 추가**: HEFTCom2024 GEB팀(arXiv 2505.10367, github.com/BigdogManLuo/HEFTcom24) = 멀티NWP
sister forecast 스태킹, 우승팀 SVK = 기상데이터셋 3종+발전소 가용성 문제 처리가 성공요인. 평가식이
한국 재생에너지 발전량 예측제도(오차 6%↓ 4원/kWh, 6~8% 3원/kWh)를 반영한 구조임도 §8에 등재.

**병행 유지(변경 없음)**: 팀원에게 `v16_exp` 전체 사본+전처리 스크립트+`clean_1/2/3` 판정기준 설명
요청 대기. 참가자격 증빙서류 수합. 8/13·8/14 09:59 최종파일 선택 상태 확인.

**금지(갱신)**: CNN 재학습·실현체선택·w/블렌드비율 스윕·DOWNWEIGHT 상수 단독 스윕(HPO 전멸 규율#8)·
LB로 후보선택·단일base 판정·8/8 이후 신규후보 착수·종결축 재개. GEFS spread는 "영구기각·재시도
없음" 명시 건이라 별도지시 없이는 착수 금지.

### 4-38. TASK31 X-1. clean_1/2/3 해부 — 판정 없음, 사실 보고 완료 (2026-07-28,
`task31_target_quality/TASK31_X1_cleanmask_anatomy.md`)

SCADA 10분 원자료(`data/scada_vestas_train.csv`·`data/scada_unison_train.csv`)로 clean==0 시간대를
6항목(원인분류·블록길이·그룹간동시성·시간분포·착빙비율·2024채점행 오차기여도) 측정. **판정 없음**(지시
그대로). 주요 사실: (1) 원자료 자체에 극소 비율(~0.05%) 물리불가능 센서오류값 존재(집계시 발견,
clean_1/2/3과 무관). (2) G3 "dirty율 40%"는 84%가 unison 미가동 구조적 결측(2022-01~2023-01)이지
품질열화가 아님 — episodic dirty만 보면 G1/G2와 비슷한 규모(1664 vs 2092/1714). (3) 3그룹 모두
"바람있음∧출력억제"(출력제한·정지 추정)가 최대 또는 2위 범주(25~60%) — HEFTCom2024 SVK의 "발전소
가용성 문제" 성공요인과 정합. (4) ★착빙가능조건(기온≤0°C∧습도≥90%)에서 dirty율이 3.4~5.2배 높음 —
가장 뚜렷한 신호. (5) ★clean==0 인접 2024 채점행 오차가 뚜렷이 악화(G2 NMAE 3.8배, FICR등급비율
0.11 vs 0.43) — 단 표본 작음(0.5~2.1%). X-2(가중 재설계) 착수는 이 보고 후 fable 결정 대기, 임의
진행 없음.

### 4-39. ★★ TASK32. ECMWF 3차이자 마지막 재시도 — G1/G2 문자상 PASS했으나 잡음대조로 신뢰불가 판정,
ECMWF 라인 영구종결 (2026-07-28, `task32_ecmwf_retry/TASK32_result.md`)

TA감사 정정(예전 planB 실패=전이실패 아닌 "2024내 학습→2024내 평가" 검증설계 결함) 반영해 ECMWF
유효구간(2024-03-08~) 내부에서 FIT(03~08월)/HOLDOUT(09~12월)을 시간순 분리, 학습이 못 본 미래구간만
게이트. ECMWF 전용피처(팀피처 미사용, 예전 혼합 결함 시정) + 기존 5:1 블렌드비(재탐색 없음) 승계.
CNN 재학습 없음(동결), v7 base 재학습 없음.

결과: G1(stress∩holdout n=688) diff=+0.01575(≥+0.0005·3/4월 — 문자상 PASS), G2(24h블록셔플)
diff=−0.00308(문자상 PASS). **그러나 G1의 크기가 이 프로젝트 전체 최대 실제레버(CNN +0.00258)의
6배 이상** → 방법론 규율 #3(+0.005급 이득 널테스트 필수)에 따라 순수 가우시안잡음(5:1 동일블렌드)
추가대조 실행 → **잡음대조도 거의 동일 크기(+0.00992)로 G1 문턱을 통과** — ECMWF의 실제 정보가치가
아니라 파이프라인(월별 롤링 calibrate_total)이 소표본(n=688, 월별 cal창 200~1500행)에서 ANY 2차
신호를 부풀리는 아티팩트로 판정. **G1/G2 문자상 PASS를 신뢰불가로 하향, LB 프로브 비권고**.
**ECMWF 라인 영구종결(3번째이자 마지막 시도)**, 데이터경로 재개 없음.

### 4-40. ★★ fable 판정 — TASK32 영구종결 승인(방법론자산 §7-17 등재) + TASK31 X-2 착수 승인
(2026-07-28)

**TASK32 영구종결 승인**: 4-39 판정에 완전 동의, 방법론 규율 §7-17로 승격 등재(위). 규칙을 기계적으로
따랐으면 프로브를 낭비했을 것이라는 점을 특히 강조 — 규모 상식(최대 실레버의 6배)이 잡음대조 트리거의
실질 판단기준이었음.

**X-1 재판독**: 두 발견이 정반대 성격인데 현행 DOWNWEIGHT=0.3 상수가 둘 다에 동일 적용 중이라는 점을
지적. ① unison 미가동(G3 dirty의 84%, 2022-01~2023-01)은 터빈 자체가 안 돌아간 기간이라 바람과
무관 — 0.3으로 학습에 포함시키는 것은 "G3는 바람 불어도 안 나온다"는 거짓 패턴을 부분 학습시킴,
더 버려야 함. ② 착빙 dirty(dirty율 3.4~5.2배)는 낮은 출력이 오류가 아니라 기상의 함수인 참값 —
0.3을 곱하는 것은 학습 가능한 물리를 폐기하는 것, 덜 버려야(=가중 복원해야) 함. TASK16 icing_features
기각(착빙 피처 "추가")과는 개입 지점이 다름(이번은 착빙 시간의 "학습 가중 복원").

**TASK31 X-2 착수 승인 — 변형 3개 한정, 스윕 금지**:
- V1(구조결측 제외): unison 미가동 시간의 G3 가중 0.3→0. 타 그룹·타 시간 불변.
- V2(착빙 보존): 착빙조건(t≤273.15K∧r≥90%, X-1 임계 그대로 승계·재정의 금지) clean==0의 가중
  0.3→1.0.
- V3 = V1∧V2.
- 금지: DOWNWEIGHT 상수 스윕·착빙 임계 스윕·변형 추가.

**게이트(완화 없음)**: Y1 연도전이(2022-23→2024, production-mode, 3-seed) 평균 diff≥+0.0015 ∧
≥2/3 seed, 변형 3개라 Bonferroni α/3. Y2 D2 스트레스셋 diff≥+0.0005 ∧ ≥2/3. Y3 널테스트 필수
(TASK32 교훈 직접 적용) — 동일 개수의 무작위 시간에 같은 가중 변경을 가한 대조 팔(nullV1/nullV2)이
비슷한 크기를 내면 즉시 기각, V1은 표본수 자체가 줄므로 "무작위 동수 제외" 대조로 표본효과와 분리.
전부 통과 시에만 LB 프로브 1회(승인 후), ΔLB≥+0.0010일 때만 교체 상신.

**일정**: 7/29~31 실행 → 8/1 판정 → 8/2~4 프로브 → 8/8 신규 착수 금지선. **불변**: 최종파일(원본
0.65184)·동결예측·CNN 재학습 없음.

산출물: `task31_target_quality/x2_weights.py`(V1/V2/V3/nullV1/nullV2 가중 계산)·
`x2_train_variant.py`(변형 학습, baseline과 sample_weight만 차이)·`x2_variants/`(학습 예측 산출물).

### 4-41. ★ TASK31 X-2 실행 완료 — V1/V2/V3 전원 Y1·Y2·Y3 결정적 FAIL, 라인 종결 (2026-07-28,
`task31_target_quality/TASK31_X2_result.md`)

3변형(V1 구조결측제외·V2 착빙보존·V3=V1∧V2)×3-seed(42/123/2024) 전부 학습(CNN 동결 재사용) 후
Y1(연도전이 pooled diff)·Y2(D2 스트레스셋)·Y3(널대조 nullV1/nullV2) 3중 게이트 실행.

**Y1**: 셋 다 평균이 문턱(+0.0015)에 크게 못 미침(V1 −0.00026·V2 −0.00236·V3 −0.00109, 전부 1/3만
양수) — Bonferroni α/3을 따질 필요도 없는 점추정 자체 미충족.

**Y2(D2 스트레스, n매칭 1639)**: V1/V2/V3 전부 3/3 시드 음수(0/3 양수, V1 −0.00220·V2 −0.00229·
V3 −0.00460 최악). 반대로 **nullV2(무작위 대조)가 유일하게 문턱을 기계적으로 통과**(2/3, +0.00212).

**Y3**: V1은 real과 nullV1이 두 축(Y1/Y2) 다 구분 불가한 크기 — 신호 아님. **V2는 real이 nullV2보다
오히려 나쁨**(Y2에서 null만 PASS) — 착빙 가중복원 가설(X-1 관찰)이 이 구현·이 게이트에서 재현되지
않고 역효과 방향. V3는 두 결함이 상쇄되지 않고 누적돼 Y2 최악치 기록.

**판정: 3변형 전원 Y1·Y2·Y3 결정적 FAIL(마진성 미스가 아님). LB 프로브 없음(자격 미충족).
TASK31 X-2(clean-downweight 상수 세분화) 라인 영구종결, 지시문 규정대로 변형 3개 한정 소진.**
최종파일(원본 0.65184)·동결예측 불변.

### 4-42. ★★ TASK34. LDAPS 격자 기하 결함 발견·수정 및 공간 재검정 — Gate-W1 FAIL, 표현 축 영구
종결 재확정 (2026-07-29, `task34_grid_geometry_fix/TASK34_result.md`)

fable 긴급지시(2026-07-29): 사용자 제공 원자료(`task30_spatial_repr/ldaps_grid_latlon.csv`) 직접
대조로 LDAPS 16격자가 4×4 정격자가 아니라 4행×5열 격자의 다이아몬드 배치(네 모서리 결측: 행0/행3
3셀, 행1/행2 5셀)임을 확정(`grid_fix.py` 자체검증 0 mismatch/16). `cnn_common.py`의
`reshape(4,4)`(TASK1~33 전체 CNN 게이트 공유 코드)가 flat index를 그대로 4×4로 접어 래스터 인접
24쌍 중 8쌍이 허위인접(최악 6.14km, 격자4↔8, 도메인 전폭)인 지도를 만들고 있었음 — TASK30·R1의
CNN 학습은 전부 이 뒤섞인 지도 위에서 수행됨. GFS 9격자(3×3)는 정격자로 확인, 수정 불필요.

**수정(변수 하나만 교체)**: LDAPS를 올바른 4×5로 재구성, 결측 4칸은 (a)같은 시각·행의 평균으로
채움 (b)유효성 마스크 채널 1개 추가(채널 30→31). 아키텍처는 `SpatialCNN` 원본(전역평균 포함) 그대로
— TASK30의 위치조건부 풀링·용량확대는 배제(교란 방지). task29/30과 동일 하네스(3-base×K=20
real/null, 신규 시드 13001-13020/14001-14020) 재사용, 신규 하네스 구축 없음.

**결과**: real−null 평균 **−0.00127**(문턱 +0.0015에 크게 미달, 부호도 음수), real vs base 3/3
양수 **0/3**(세 base 전부 음수), real−null 3/3 방향일치 **2/3**. **Gate-W1 FAIL** — 뒤섞인
지도였던 TASK30(−0.00014)·R1(+0.00058)보다도 나쁨. 올바른 지도로 고쳐도 신호가 나타나지 않아
"기하 결함 때문에 신호를 놓쳤다"는 재개근거를 정면으로 반증했다.

fable의 독립 분석(같은 지시문, 원자료 실측)도 동일 방향으로 수렴: 미사용 원자료 33종·집계변형 7종을
2단 게이트(S1a 상관≤0.90 ∧ S1b 부분R²≥0.5%, gh500 앵커 보정)로 스크리닝해 **0/40 통과**(신호⟺중복의
완전한 트레이드오프, 변수·집계 축 실측 종결), 기하 A/B 대조(올바른 vs 뒤섞인 3×3 이웃평균, 3-seed)에서
차이 +0.00062·페어 부호 갈림으로 **미검출**, 거리기반 셀선택 가설도 기각(최근접5셀 +0.00042 <
현행5셀 +0.00070). 세 갈래 독립 검정(TASK34 CNN Gate-W1, 기하 A/B, 변수/집계 스크리닝)이 전부 같은
결론으로 수렴.

**판정에 따른 조치**: Gate-W1 FAIL → Gate-W2/W3·LB 프로브 진행 안 함. **"올바른 지도에서도
무신호"로 표현 축 영구 종결을 재확정**(4-36의 잠정 철회를 종료). TASK30/R1의 "표현 축 영구
종결"·"원시격자 잔여정보 없음" 결론은 이제 정당한 근거(올바른 지도로도 검정 완료) 위에 서 있다.
최종파일(팀원 v7+CNN, LB 0.65184)·동결예측 불변.

### 4-43. ★ TASK35. LDAPS 셀 확장(5→16셀 전량), 프로덕션 GBDT 피처셋 단일변경 — Y1·Y2 FAIL
(2026-07-29, `task35_ldaps_cell_expansion/TASK35_result.md`)

fable 신설 지시: 프로덕션 캐시(`cache_train.parquet`)의 LDAPS 피처가 16격자 중 5개(g4/g5/g6/g7/
g12)만 사용 중임을 확인, 나머지 11셀에 동일 19개 파생변수를 추가해 16셀 전량으로 확장하는 단일
피처 변경 실험. ★TASK12(원시격자를 별도 앙상블 멤버로 만들어 기각)와 구분 — 본 건은 기존 피처셋에
셀만 추가하는 별개 실험으로 기록상 미검정이었음.

**재현 방법론**: `merged_train_v9.csv` 빌드 스크립트가 레포에 없어, 기존 g4/g5/g6/g7/g12 값과
원자료(`data/ldaps_train.csv`)를 대조해 변환식을 역산(19개 중 17개 완전정확 최대오차 9e-13,
air_density만 근사 상대오차 4e-5) — 자체검증 통과 후 11셀 209개 신규 컬럼 생성.

**실험**: 프로덕션 베이스라인 레시피(`gen_base_holdout_seed.py`)와 100% 동일(4모델 앙상블·동일
하이퍼파라미터·동일 clean-downweight·동일 TRAIN_END) — FEAT만 교체. `expand16`(실 209컬럼 추가)
vs `noise16`(§7-17 잡음대조, 동수 209 가우시안 컬럼 추가), 3-seed×2변형, CNN 동결 재사용.

**Y1(연도전이)**: expand16 평균 pooled_diff **−0.00069**(1/3 seed 양수) → FAIL(문턱 +0.0015∧2/3).
★noise16(잡음대조) 평균 **+0.00073**(2/3 양수) — 문턱은 못 넘었지만 **잡음 컬럼이 실제 LDAPS
11셀보다 평균적으로 더 나은 점수**를 냄, §7-17이 겨냥한 패턴이 정확히 재현됨.

**Y2(D2 스트레스, n=1639)**: expand16 평균 +0.00039(2/3 양수, 문턱 +0.0005 근소 미달) → FAIL.
noise16 평균 −0.00035(1/3 양수) → FAIL.

**판정**: Y1·Y2 전부 FAIL → LB 프로브 없음, 교체 상신 없음. TASK34(CNN Gate-W1)·TASK35(GBDT
Y1/Y2)·fable 변수/집계 스크리닝(0/40)·기하 A/B — **네 갈래 독립 검정이 전부 무신호로 수렴**.
최종파일(팀원 v7+CNN, LB 0.65184)·동결예측 불변.

### 4-44. ★★★ TASK36. D2 스트레스 게이트셋 전수 감사 — 게이트 자격 정지 (2026-07-29,
`task36_stress_gate_audit/TASK36_result.md`)

fable 긴급 전략전환(D3 헤드룸≈0 철회, 위 §3 참조)에 부수한 최우선 감사. D2 스트레스셋이 세션 내내
"연도전이보다 날카로운 최종 관문"으로 격상돼 M1·TASK24 STEP2·TASK29 R1·TASK30/34·TASK31 X-2·
TASK35 Y2 등 다수 레버를 기각해왔다 — 이 게이트 자체의 실적을 신규 학습 없이 기존 기록으로 전수 감사.

**① 진양성**: 스트레스 통과 후 실제 LB로 검증된 사례를 전수조사 — **0건**. 이 세션에서 스트레스
통과→실LB 확인까지 도달한 경우는 아래 ②의 S2 단 하나뿐(다른 통과 사례는 TASK32처럼 잡음대조로
LB 프로브 자체가 비권고되거나, 애초 스트레스에서 기각돼 LB에 도달하지 못함).

**② 위양성**: TASK7b/S2(`task8_g1g2ficr`) 재확인 — D2 스트레스 3-seed 평균 **+0.00146(3/3, 문턱의
3배 마진)** 통과. 실제 LB: B=0.6482289(L0 대비 ΔLB **−0.00361**) 또는 (CNN 실현체 혼입 없는 더
깨끗한 비교 C−A) **+0.00012**(예측치의 8%, 통과문턱 +0.0005에도 미달). 스트레스셋이 실제 효과를
최소 12배 과대평가(최악 부호까지 반전) — **위양성 1건 확정**.

**③ 구성편향 가설(고출력/밴드경계 시간대 과소표집) 검정 → 반증**: `stress_2025like_2024.parquet`
vs 2024 전체를 그룹별 y/cap으로 직접 대조 — 스트레스셋의 평균 출력수준이 전체보다 **1.44배 높고**,
FICR 채점대상(y≥0.1cap) 비율이 **1.51배 높다**. 가설과 정반대 — 스트레스셋은 저출력이 아니라
오히려 고출력·채점대상 구간을 과대표집하고 있다("계통적으로 FICR 레버에 불리"라는 서사는 성립 안 함).

**판정(지시문 규정, 완화 없음)**: 진양성 0 ∧ 위양성 ≥1 → **D2 스트레스 게이트 차단권한 정지**.
최종 관문을 연도전이(3-seed ≥+0.0015∧≥2/3)+널대조(§7-17)로 환원, 스트레스셋은 참고지표로만 기록.
**유보**: 표본 1건(위양성)뿐이라 "게이트가 구조적으로 고장났다"는 강한 결론은 아니다 — 지시문의
기계적 규칙에 따라 판정했을 뿐, 위양성의 진짜 원인은 미해결.

이 판정에 따라 M1의 기존 "게이트3(스트레스) 기각"은 더 이상 최종 판정으로 인정되지 않음 — TASK37
에서 연도전이+널대조 기준으로 재검토.

### 4-45. TASK37. M1 부활 검토 + 신규 분위조건부 shift 후보 (2026-07-29,
`task37_m1_revival/TASK37_result.md`)

TASK36 판정에 따라 새 게이트(연도전이 3-seed, §7-17 널대조 필수)로 두 후보 재검정.

**A. M1 부활**(`m1_holdout_gate.py` 로직 1-seed→3-seed 확장): real 평균 **+0.00121, 3/3 seed 양수**
— null(§7-17, 밴드소속 셔플) 평균 **−0.00259, 0/3** — real과 완전히 반대 방향으로 뚜렷이 갈림.

**B. 신규 분위조건부 shift(10분위, 1회적합→동결)**: fable 주장(원자료측정) FICR +0.00509. 레포에
2022-23 리키지세이프 홀드아웃이 없어 정확한 "train적합" 재현 불가 — 2024 전반부 적합/후반부 동결평가
대안 설계. 1차(단일월 적합, n≈70/분위)는 real·null 둘 다 파국적 붕괴(−0.02대, 초소표본 과적합
아티팩트로 판단·폐기). 2차(6개월 풀링 적합, n≈400/분위, 07~12 동결평가): real 평균 **−0.00035**
(1/3, 부호 자체가 seed간 갈림), null 평균 **−0.01234**(0/3, 정상 방향).

★★문턱 정정(2026-07-29, fable): 전략 리드가 이번 세션 내내 3-seed 게이트에 +0.0015를 적용했으나,
팀 규율 원문(`인수인계서.md` §4-2: "채택 게이트 = 단일 seed +0.0015 (3-seed 평균이면 +0.0010
등가)")은 단일시드 문턱이었다 — **3-seed 게이트의 올바른 문턱은 +0.0010**. 이 세션의 다른 FAIL
판정(TASK31 X-2 전부 음수, TASK34 −0.00127, TASK35 −0.00069, gh500류 −0.00105 등)은 부호반전 또는
큰 폭 미달이라 이 정정으로 결론이 바뀌지 않는다 — **영향받는 것은 M1 하나뿐**.

**A. M1 재판정**: +0.00121 ≥ +0.0010(정정된 3-seed 문턱), 3/3 양수, 널대조 −0.00259로 명확 분리
→ **PASS**. **B. 신규 분위조건부 shift**: fable이 측정치(+0.00509)의 출처를 (a)2022-23 in-sample
잔차 적합(리키지) (b)37피처 단일GBDT 약한 baseline(팀피처·MLP·quant·CNN 부재)이라 확인, 프로덕션
이식 불가로 판단해 **폐기 확정**(재시도 없음) — 클로드코드 재현치(−0.00035, 부호 비일관)를 정본으로
기록.

**조치(fable 승인)**: M1을 프로덕션 파이프라인에 적용한 제출파일
(`submission_m1_bandcorrected.csv`, 팀원 원본 v7+CNN에 밴드보정 후처리 — 밴드[0.05,0.20]cap 진입시
G1/G2/G3 shift +0.06/+0.03/+0.05×cap, cal=2024-11~12 적합, 영향행수 3030/2998/2729행/8760)를
빌드, LB 프로브 1회 대상. **선등록 판정규칙**: ΔLB≥+0.0010→최종파일 교체 상신 / |ΔLB|<0.0010→기록만
원본 유지 / ΔLB≤−0.0010→M1 영구 기각. **로컬 재판정 금지**(스트레스셋 자격정지·잡음팔 역전·문턱
오적용이 이번 세션에 전부 드러나 LB만이 유일한 신뢰 가능 심판) — 사용자 제출 대기.

### 4-46. TASK38(1/2). 블렌드 전/후 멤버별 FICR·밴드적중 분해 (2026-07-29,
`task38_band_maximization/TASK38_decomposition_result.md`)

fable 가설("앙상블 평균화가 밴드적중을 구조적으로 훼손") 검정 — 밴드최대화 점추정 착수 전 선행
확인. 가용 데이터 한계: MLP7·ms·ts는 라벨보유 2024 전체 홀드아웃이 없어(MLP7은 cal=11-12/test=
2025 raw만, ms/ts는 팀원 미제공) 분해는 GBDT 4서브모델(lgb/xgb/cat/lgb_l1, 개별 재학습·저장)·
CNN(seed42 동결)·quant(q=0.5, 유일한 라벨보유 2024 quant)·이들의 블렌드로 한정.

**① GBDT 4서브모델 평균화 → 가설 반증(오히려 개선)**: 서브모델 hit6%(비가중 밴드적중률) 범위
0.3253~0.3341 vs 평균화 후(avg4) **0.3359 — 최고 서브모델보다도 높음.** 분산축소가 band-hit에도
이득.

**② CNN 블렌드 → 가설 소폭 지지**: avg4 단독 hit6%=0.3359 → blend(avg4+CNN w=0.20) hit6%=**0.3324**
(상대 −1.0%), FICR도 근소하락(0.3858→0.3850). CNN 단독 hit6%=0.3058로 어떤 GBDT보다 낮아, 이질적
멤버 혼입이 밴드적중을 살짝 깎는 것으로 보임(단 실LB 이득 +0.00258과는 별개 지표).

**③ 부수관찰**: quant_q50(단일 분위회귀) FICR **0.39512로 전체 멤버 중 1위**(avg4 0.38585·블렌드
0.38504 모두 앞섬) — 판정 미사용이나 별도 검토 가치.

**판정**: 가설은 부분적으로만 참(GBDT 단계 반증, CNN블렌드 단계만 국소적으로 지지) — fable 조건
("가설 참이면 밴드최대화 사전확률 크게 상승")에 따르면 **전면적 밴드최대화 점추정 구축을 정당화할
만큼 강한 근거는 아님**. 밴드최대화 전면 착수 여부는 fable 보고 후 결정 — 로컬 대안(CNN 블렌드
가중치의 밴드근접 조건부 축소 등)이 비용 대비 더 합리적일 수 있음을 권고로 병기.

### 4-47. ★★★ M1 LB 프로브 채택 — 최종파일 교체 (2026-07-29,
`submission_m1_bandcorrected.csv`, LB 0.6529253)

fable 선등록 판정규칙 그대로 적용: ΔLB **+0.00109** ≥ 문턱 +0.0010 → **채택**. 신규 최종파일 =
`submission_m1_bandcorrected.csv`(LB 0.6529253 / 1-NMAE 0.8735029 / FICR 0.4323477). 순위
70위 → 약 62위. 기여 분해: FICR +0.00184(84%) / NMAE +0.00034(16%) — 리더보드 진단(FICR 축
열위, §3 D3 철회 근거)과 정합.

**빌드 계보 확인(fable 요청)**: base = **① 팀원 원본 v7+CNN**(`submission_ficr_w1_v7_cnn(0.65183).csv`
직접 로드, `task37_m1_revival/build_m1_submission.py:119-120`) — 재현판(v7cnn_fixed_v2) 아님.
`REPRODUCIBILITY_STATEMENT.md` §9에 계보 전문 등재: base는 §7-1의 재현불가 계보를 그대로 승계,
밴드보정 레이어(shift 적합·적용) 자체는 100% 결정적·재현 가능.

**D2 스트레스 게이트 최종 실적(TASK36 자격정지 → 영구 폐기로 격상)**: 진양성 0·위양성 1(TASK7b)·
**위음성 1(M1, LB로 확정)** — 이번 사건으로 스트레스셋이 M1을 잘못 기각했었다는 것이 실측으로
확정됨. 참고 지표로도 사용 금지, 완전 폐기(§7 규율 #10 정정·#18 신설, 위 참조).

**문턱 오적용 재확인**: 전략 리드가 3-seed 게이트에 단일seed 문턱(+0.0015)을 오적용해 M1을 죽일
뻔했음이 이번 LB실측(+0.00109)으로 실증 — 팀 원규율 §4-2(3-seed 등가 +0.0010)가 옳았음이 최종
확인됨.

**최종파일 갱신**: 이 시점부터 "최종파일"은 `submission_m1_bandcorrected.csv`(LB 0.6529253)를
가리킨다 — 팀원 원본(`ficr_w1_v7_cnn`, LB 0.65184)은 base 계보로만 남는다.

### 4-48. TASK41. M1 기전 평문화 + 팀 공유 패키지 (2026-07-29,
`task37_m1_revival/M1_mechanism_plain.md`)

fable 지시(최우선, 추측 금지·코드 인용 필수)에 따라 M1의 실제 차별점을 코드 대조로 특정.

**① 기존 calibrate_total과의 차이**: "채점행에서만 적합"은 기존 `calibrate_total`(shift 탐색이
`score_fn` 내부의 `y>=0.1·cap` 필터를 이미 통과)도 하던 일 — M1의 진짜 차별점은 **적용 대상**이다.
기존 shift는 그룹당 상수 1개를 **전체 예측범위에 균일 적용**하는 반면, M1은 **이미 캘리된 예측값이
[0.05,0.20]×cap 구간에 든 행에만** 별도의 2차 상수(그룹당 1개, 총 3개)를 추가 적용한다
(`m1_holdout_gate.py:60-66` vs `:40-41`) — "카운트 문턱(예측값 기준) 근방 예측만 노리는 국소
2단계 보정"이 정확한 정의.

**② "bandcorrected"의 실제 연산**: 밴드 A(예측값 [0.05,0.20]×cap, 어느 행을 고칠지)와 밴드
B(오차율 6%/8%×cap, `score_fn`의 계단함수, 무엇을 보상하는지)는 다른 개념 — 밴드 A 안에서
그리드서치(±0.10×cap, 21후보)로 `score_fn`(밴드 B를 내재)을 최대화하는 shift를 찾는다.

**③ 파라미터 3개(그룹당 1개)**, 실제 채택 스크립트(`build_m1_submission.py`) 기준 cal(09-10)→
eval(11-12) calibrate_total 후, **11-12월 자기 자신(ev_c/yev)을 cal로 삼아** band_shift를
1회 적합(자기참조 적합 — 리키지 아님, 과적합 리스크로만 별도 기재) → 고정해 2025 실제 test에 적용.

**④ 리키지 재검 — 없음 확인**: 실제 적용 연산(`build_m1_submission.py:124-128`)은 (a)행 자신의
예측값 (b)2024년 데이터로만 만든 고정 상수 3개만 사용, 행간·미래시점 정보 전혀 없음.

**⑤ FICR 84%/NMAE 16% 분해도 기전으로 설명됨**: NMAE는 8760행 평균이라 밴드영향행(약
2700~3000행)의 개선이 희석되는 반면, FICR은 6%/8% 문턱을 넘으면 점수가 비연속 도약(계단함수)해
같은 개선이 훨씬 크게 반영 — score_fn 구조상 정합적.

**패키징(TASK41-B)**: `~/m1_share.tar.gz`(팀 공유용, m1_spec.md/build_m1_submission.py(★정본)/
M1_mechanism_plain.md/record_full.md/TASK36·38 결과/common.py/신구 제출파일 2종 + FILENAME_MAP.txt)
— 서버 원본 이동·삭제·수정 없음(복사만).

### 4-49. TASK40. 중앙값 계열 추정량 블렌드가중 검정 — STEP0 confound 확정, STEP0.5 손실함수
통제비교 FAIL → 종결 (2026-07-29, `task40_median_blend_weight/TASK40_STEP0_5_result.md`)

**STEP0**: TASK38의 `quant_q50`을 생성 스크립트(`rolling_eval.py:238-247`, `tm = (tr["t"] < ev_s)
& (~cm)`)까지 추적한 결과, eval월마다 학습창이 "그 달 2개월 전까지 전체"로 매번 늘어나는
**롤링 재학습** 산출물임을 확정 — fable이 예상한 (A)2022-23 고정학습도 (B)팀원 프로덕션 quant
파일(`experiments/09_quant_member/train_quant_member.py`, α=0.80)도 아닌 제3의 경우. gbdt_avg4·
CNN(2022-23 고정학습)과 학습창이 달라 **공정비교 아님** — TASK38의 "quant FICR 0.395 > 블렌드
0.33x" 관측치는 무효화됨(경험적 근거 소멸, 이론적 근거만 남음).

**STEP0.5**(fable 신설, confound 없는 통제비교): 동일 피처·데이터·하이퍼·2022-23 고정학습·
seed{42,123,2024}, 손실함수만 3종(①mse ②l1 ③quantile α=0.5) 교체해 단일 LGB 학습, canonical
calibrate_total 후 FICR 대조.

| 대조 | FICR diff 평균(① 대비) | 양수seed | 판정 |
|---|---|---|---|
| ② l1 | **+0.00652** | 3/3 | PASS |
| ③ quantile α=0.5 | +0.00149 | 2/3(1회 부호반전) | FAIL |

fable 규칙(②③ 둘 다 +0.005∧2/3 충족 시 이론 지지) 그대로 적용 → **③ 미달로 종합 FAIL, TASK40
종결**(가중 스윕 STEP1 착수 안 함). ★단, L1과 quantile α=0.5는 이론상 둘 다 조건부 중앙값
추정량인데 실측이 크게 갈림(L1 깨끗한 통과 vs quantile 부호반전 포함 미달) — LightGBM 구현상
두 손실함수의 그래디언트/헤시안 근사 차이가 원인일 가능성(미검증, 추측으로 명시). STEP1을
quantile 대신 L1 멤버로 재설계할지는 규율(#7-18 유지) vs 사후재설계금지 원칙이 충돌하는
지점이라 클로드코드가 임의 결정하지 않고 fable에 보고.

**★fable 판정(2026-07-29, 같은 날)**: 원 판정(FAIL) 유지 전제, ③ 유효성 진단(STEP0.5-D) 선행
요구 — 먼저 ①②③ 전체 (1-NMAE, FICR, TOTAL) 3-seed 값 확인(② l1이 NMAE도 개선(+0.00082)하며
FICR도 개선(+0.00652) — 트레이드오프 아닌 순이득, STEP1 무의미 우려 기각). 이어 진단: quantile
(α=0.5)의 gradient(±0.5)가 regression_l1(±1)의 절반이라 동일 learning_rate에서 유효 학습률이
반일 수 있다는 가설 — **learning_rate만 2배(0.03→0.06)**로 quantile 재실행(그 외 전부 동일,
3-seed).

**STEP0.5-D 결과**: ③'(lr=0.06) FICR diff 평균 **+0.00211**(2/3 양수, seed123 **−0.00453**
부호반전 지속, seed2024는 +0.00902로 변동성 오히려 확대) — ②(L1, +0.00652)와 ±0.002 이내가
아님(격차 0.0044) → **최적화 아티팩트 미확인**. fable 선고정 규칙("미달이면 원 판정 FAIL 확정,
TASK40 영구 종결, 재설계·재시도 없음") 그대로 적용.

**★★최종 판정: TASK40 영구 종결.** L1로 STEP1을 재설계하는 옵션도 사용하지 않는다(위 규칙에
명시적으로 금지됨) — "중앙값 계열 레버가 §7-18(지표구조 레버) 부류에 속한다"는 가설은 이번
검정 범위에서 견고하지 않은 것으로 확정.

**조치**: 지시문 문언 그대로 적용해 TASK40 종결. 최종파일(`submission_m1_bandcorrected.csv`,
LB 0.6529253) 불변.

### 4-50. TASK42. u*(마찰속도) 후보 독립 검증 — 결정적 기각 (2026-07-29,
`task42_ustar_verify/TASK42_result.md`)

fable이 출처불명 "+0.00296" u* 측정치의 판정을 요청 — 그러나 해당 측정은 이 세션·레포 어디에도
존재하지 않아(코드·결과파일 없음) 클로드코드가 fable 요구사항(①연도전이 고정학습 ②3-seed
③§7-17 잡음대조 ④M1적용본 base ⑤1-NMAE/FICR/TOTAL 전부 ⑥u* 계산식 명시) 전부를 충족하도록
처음부터 독립 재측정.

**공식**: `u* = 0.4×(u_50m−u_10m)/ln(50/10)`, LDAPS 10m/50m(프로덕션 기존 5셀 g4/g5/g6/g7/g12,
신규 원자료 유도 없음, 안정도 보정 미적용). **하네스**: M1 적용본 base(GBDT+CNN블렌드→calibrate_total
→M1 밴드보정 전부 거친 뒤 비교, `m1_holdout_gate.py` 로직 그대로 재사용) 위에서 3-seed 연도전이.

| | 평균 diff TOTAL | 양수seed |
|---|---|---|
| ustar(real) | **−0.00058** | 2/3 |
| noise(§7-17) | **−0.00068** | 1/3 |

문턱(≥+0.0010∧≥2/3) 애초 미달(평균 음수)이자, 잡음팔이 real보다도 더 나쁜 규모(즉시기각 규칙
발동) → **결정적 기각**. §7-18 분류(피처추가=분포적합 부류, ~15건 연속실패)에 u*도 추가.
최종파일(`submission_m1_bandcorrected.csv`, LB 0.6529253) 불변.

### 4-51. 유도(induction)/NTF 과소추정 가설 — 파이프라인 실현 불가로 종결 (2026-07-29, fable)

전처리 스크립트 대기 없이 fable 판정으로 종결. **근거**: SCADA 나셀/LDAPS-50m 비율이 풍속
전 구간에서 **단조 감소**(1.041→0.772, 구간별 변화 전부 음수) — 단조 왜곡은 트리 분할의 순서를
바꾸지 않으므로 GBDT는 완전 불변이다. 따라서 `pc_pred_`/`ws_corr_`(팀 파생피처)가 나셀 축에서
적합됐더라도 계통 편향이 예측으로 전파되지 않는다 — **이 경로에서는 실현 불가능한 가설**.

**유보(2차 규모, 완전 배제는 아님)**: ①파워커브 포화로 천장(saturation ceiling) 위치가 이동할
수 있음(원시 풍속이 피처에 있어 이론상 복구 가능) ②MLP는 GBDT와 달리 단조 불변이 아님(다만
base 블렌드에서 비중 1/6로 작음).

**등재**: §2-1의 EDA 사실 3건(나셀 편향 ~16%, LDAPS 고풍속 과대예보 SCADA/LDAPS50m=0.772,
GFS 100m 지형평탄화 프록시 약화)은 이 가설 검토 과정에서 발견됐으나 독립적으로 유효 — 발표
기술문서 EDA 절에 활용. SCADA 잔차보정 실패(4-14, −0.00276)의 재해석(위 blockquote)은 **가설로만
기록, 미검증, 재개 근거 사용 금지**로 병기.

**조치**: 최종파일(`submission_m1_bandcorrected.csv`, LB 0.6529253) 불변. 신규 전처리 작업 없음.

### 4-52. TASK43. 채점자격 기반 학습가중 — V1·V2 둘 다 FAIL, 종결 (2026-07-29,
`task43_censoring_weight/TASK43_result.md`)

가설(§7-18 지표구조 레버 부류): 채점 안 되는 행(y<0.10×cap, 2022-23 기준 37~43%)에 손실을
쓰는 것은 목적함수-산식 불일치. 기존 clean-downweight에 채점자격 곱셈항만 추가 — V1(비채점행
×0.0)·V2(×0.3), 그 외 피처·하이퍼·블렌드비율·캘리·CNN 전부 불변. §7-17 잡음대조(동일개수
무작위행에 동일배수) 병행. **하네스**: M1 적용본 base(GBDT+CNN블렌드→calibrate_total→M1
밴드보정, `m1_holdout_gate.py` 로직) 위 3-seed 연도전이, 롤링 없음. (★MLP는 이 세션 전체 게이트
전례대로 재학습 생략 — GBDT 레이어 효과만 측정, 정직하게 명시.)

| variant | real 평균 diff | real 양수seed | 판정 |
|---|---|---|---|
| V1(완전제외) | **−0.00140** | 0/3 | FAIL |
| V2(0.3 축소) | **−0.00074** | 2/3 | FAIL |

문턱(≥+0.0010∧≥2/3) 둘 다 미달, 부호부터 반대 — **가설과 정반대 방향**(V1이 V2보다 더 나쁨:
완전제외가 완만축소보다 손해가 큼) → "채점 안 되는 행의 정보가 컷인 근방 경계 학습에 여전히
유용했다"는 해석과 정합. V1·V2 중 하나만 통과해도 인정하는 규칙에서도 종합 FAIL. LB 프로브
자격 미충족. 최종파일(`submission_m1_bandcorrected.csv`, LB 0.6529253) 불변.

**★fable 최종 판정 및 정밀 해석(2026-07-29, 같은 날)**: V1(−0.00140, 0/3)이 V2(−0.00074, 2/3)보다
더 나쁘다는 것은 "모델은 행별 점수의 단순 합이 아니라 **공유 함수**이므로, 표본가중 변경이 채점
구간 밖에서 일어나도 채점 구간의 트리 분할에까지 파급된다"는 뜻으로 해석 — 저출력 행을 지울수록
공유 함수 추정 품질이 깎여 채점 구간도 함께 나빠진다는 기전. 한계 재확인: MLP 재학습 생략(GBDT
레이어만 측정)이나, V1이 0/3·−0.00140으로 규모가 크고 방향이 뚜렷해 MLP 포함 시 뒤집힐 가능성은
낮음 — **재개 없음**.

**★★규율 #7-18 정밀화(이번 사이클 핵심 산출물)**: 기존 "지표 구조 레버는 전이, 분포 적합 레버는
비전이"를 정밀화 — **"지표 구조 레버는 '사후 조정'에 적용할 때만 전이된다. 학습 목적함수·표본
가중에 반영하면 실패한다."** 근거 3건: M1(검열문턱×사후조정, LB+0.00109, 전이율90%) /
v8b(FICR밴드×손실함수, LB−0.00089) / TASK43(검열문턱×표본가중, −0.00140/−0.00074). 기전: 사후
조정은 행 단위 독립이라 "채점 안 되는 행의 오차는 공짜"가 정확히 성립하지만, 학습 단계는 모델이
공유 함수라 이 전제가 깨진다 — 함수 추정 품질을 담보로 잡는 거래가 되기 때문. **향후 후보 심사
시 "무엇을 이용하는가"뿐 아니라 "어디에 적용하는가"(사후조정 vs 학습단계)를 함께 볼 것.**

**★★★전처리 축 완전 폐쇄**: fable 직접 스크린 48후보(0/48) + TASK34(격자기하)·TASK35(16셀 확장)·
TASK43(표본가중) + 과거 ~15건 누적 — 변수·집계·격자·표현·표본가중 축 전부 소진. **신규 전처리
착수 금지.**

**★전략 리드 역할 재정의(fable 자기평가, 2026-07-29)**: 이번 세션 가설 7전 7패(전부 fable 발안:
u*/전단 8후보·TASK40 중앙값계열·TASK43 표본가중 등). 유일 성공 M1은 외부 증거(리더보드) 판독에서
나옴. 반면 측정·감사 역할은 실적 보유(LDAPS 격자결함 발견, 48후보 스크린이 u* 기각을 사전예측,
D2 스트레스게이트 위음성 발견→M1 부활, 3-seed 문턱 오적용 발견, 자기 측정치(u* +0.00296 유사)
폐기 등). **fable은 신규 가설 발안을 중단하고 검증·감사·판정 역할에 한정한다 — 재개는 외부 신규
정보(리더보드 변동, 게시판, 팀원 레버, 사용자 지시)가 있을 때만.**

### 4-53. TASK44. 분포 기반 밴드최대화 점추정 — STEP2(분위 캘리브레이션 검증)에서 종결
(2026-07-29, `task44_band_max_dist/TASK44_result.md`)

발동 근거: **사용자 명시 지시**(fable 자체는 미발동 대상으로 기록, 사전확률 10~15%, TASK40을
본 트랙 사전검사로 선등록했었음). 분류: 사후 조정(점 선택) = 정밀화된 §7-18 유리한 쪽(M1과
동일 지점) — 학습 목적함수·표본가중은 변경 없음.

**STEP1**(`train_quantile_grid.py`, 2026-07-29): LGB quantile 10분위(α=0.05~0.95, 0.10간격 —
지시문 "9개"와 실제 나열이 불일치해 나열 그대로 10개로 진행, 판단 명시) × 3그룹 × 3seed,
2022-23 고정학습(롤링 없음), 하이퍼·lr 튜닝 없음. 분위교차(정렬 전) 48.6~49.1% — 지시문대로
재학습 없이 정렬로만 해소.

**STEP2**(`step2_calibration_check.py`, 2026-07-29, seed42 기준·3그룹 풀링): 경험적 커버리지
P(y≤q_α) vs 명목 α 대조 — **저분위(0.05~0.55) 전부 FAIL**(예: 명목5%인데 실제커버리지19.8%,
저분위 예측이 체계적으로 과대), 고분위(0.65~0.95)만 PASS. **통과 4/10(문턱≥7 미달).**

**판정(완화 금지, 지시문 그대로)**: 분포 추정 도구 자체가 무효 → **STEP2에서 종결**, STEP3(점
추정 산출)·STEP4(블렌드)·게이트·LB 프로브 전부 미진행. 최종파일(`submission_m1_bandcorrected.csv`,
LB 0.6529253) 불변. **8/8 신규착수 금지선에 따라 모델클래스 축의 마지막 트랙도 이로써 종결.**

### 4-54. ★★★ 규율 #7-20 신설 — 게이트 문턱의 검정력 부족 확인 + 터빈단지 EDA 등재
(2026-07-30, fable, `data_description`/`info.xlsx` 명세 확인)

fable이 대회 명세 문서를 직접 재확인해 도출. **§7-20(방법론 규율)**: 일 블록 부트스트랩 SE=0.00119
기준, 이 세션이 써온 3-seed 문턱 +0.0010은 **0.84 SE**(1 SE 미만) — 참효과 0이어도 통과확률
약 20%로 **문턱 단독은 판별력 없음**. 실제 판별은 §7-17 잡음대조가 수행했다(M1: 로컬+0.00121은
1.0 SE로 문턱만으론 불충분했으나, 잡음팔(−0.00259)과의 간격 0.0038≈3.2 SE가 채택의 진짜 근거,
LB+0.00109로 사후확인). 이 세션의 위양성 3건(ECMWF·nullV2·noise16)이 이 검정력 부족으로 설명됨.
→ **이후 모든 게이트는 §7-17 잡음대조 없이 판정 금지**로 §7에 등재(위 20번 참조).

**부수 확인**: (a) 예보구조상 리드타임(12~35h)≡시각 완전공선 — "리드타임 열화"는 이 데이터에서
원리적으로 분리불가. (b) `forecast_horizon_h` 피처는 명세상 진짜리드와 불일치(Spearman−0.50)
하지만 4개 인코딩 비교에서 기능적 결함 아님을 확인, 수정하지 않음. (c) 잔차 일단위 설계효과 9.7
이나 대응표본에서 상쇄돼 SE배율 1.17배.

**EDA 등재(§2-2)**: `info.xlsx` 확인 — G1/G2/G3 위치 상이(최대 1.9km 이격), **G3의 2~5호기만
단지명이 '태백원동'으로 나머지('태백가덕산')와 다름** — G3의 지속적 이상신호(SD·편향·FICR 최악)와
연관 가능성이나 TASK30·TASK9-A가 이미 이 방향을 실패로 확인해 **신규 착수 없음**, EDA로만 등재.
로터직경/용량도 G3(UNISON, 136m/4.2MW)만 G1/G2(VESTAS, 126m/3.6MW)와 다름.

## 5. 점수 총정리 (실제 LB)

| 버전 | 구성 | TOTAL | 1-NMAE | FICR |
|---|---|---|---|---|
| v16 ens (quantile) | 초기 라인 | ~0.6406 | | |
| v1 (팀원) | MSE + iso + shift, LGB | 0.64301 | | |
| ens_v2 | ①LGB+XGB+Cat, 3:1:1 | 0.6446 | | |
| ficr_w1 | ②(5·ens_v2+1·quant)/6 | 0.6453 | 0.8656 | 0.4250 |
| ficr_w2 | (5·ens_v2+2·quant)/7 | 0.64528 | 0.86453 | 0.42604 |
| ficr_w1_v3b | teamfeat + 4-model(L1) + 그룹별 캘리 | 0.64687 | 0.86735 | 0.42639 |
| ficr_w05_v3b | w=0.5 프로브 | 0.64670 | 0.86814 | 0.42526 |
| ficr_w1_v4 | + ECMWF planB 5:1(기각) | 0.64666 | 0.86725 | 0.42608 |
| ficr_w1_v5 | v3b + MLP 멤버 5:1 | 0.64717 | 0.86860 | 0.42574 |
| ficr_w1_v7 | v5 + clean downweight + subsample_freq 수정 | 0.64926 | 0.87021 | 0.42831 |
| ficr_w1_v7a | ms/ts 축소 프로브(플랫) | 0.64996 | 0.87068 | 0.42924 |
| ficr_w1_v8b | value-loss 멤버(전이실패 기각) | 0.64837 | 0.86962 | 0.42712 |
| v9 (딥시계열) | 게이트2 실패, 미제출 | — | — | — |
| **ficr_w1_v7_cnn(0.65183)** | **v7 + 팀원 CNN(w=0.20), base 계보(구 최종파일)** | **0.65184** | — | — |
| **v7cnn_fixed_v2 (A)** | 우리 CNN 재현·NaN버그 수정(TASK14 결정성 계보) | **0.6490737** | 0.8742969 | 0.4238505 |
| task8_g1g2ficr (B) | G2 shift+0.01 단독 | **0.6482289** | | |
| v7cnn_fixed_v2_g2shift (C) | A+G2 shift 결합 | **0.6491906** | | |
| **m1_bandcorrected** | **원본 + M1 채점밴드 보정(TASK37), ★★신규 최종파일(4-47)** | **0.6529253** | 0.8735029 | 0.4323477 |

**S3 규칙③ 발동(4-34): A·C 둘 다 통과선(L0−ε=0.65134) 미달 → max(L0,L_B)=원본 확정.** 4건 중
원본(팀원 v7+CNN)이 그대로 최종파일 유지 — 제출·선택·검증 축(부록 S/T) 완전 종결(2026-07-28
시점). ★2026-07-29 갱신: TASK37 M1 밴드보정이 별도 축(제출/선택 축이 아니라 캘리브레이션 축)에서
LB ΔLB+0.00109로 통과, m1_bandcorrected가 신규 최종파일로 교체(4-47) — S3 규칙은 "4건(A/B/C/원본)
중 선택"이라는 당시 범위 내에서는 여전히 유효, M1은 그 범위 밖의 새 레버.

랭킹 추이: 115위 → ~70위 → v3b → v7(48위, 30위컷까지 0.0030) → v7+CNN(팀원, 4건 실측 비교 후
2026-07-28 재확정) → **m1_bandcorrected(2026-07-29, 약 62위, ★현재 최종)**.

## 6. 좋았던 것 / 실패한 것

### 좋았던 것 (실제 LB로 확인)
- MSE base + isotonic + FICR-max shift 캘리브레이션 (v1) — 토대.
- LGB+XGB+Cat 앙상블(분산축소→NMAE), FICR 전용 고분위 quantile 소가중 블렌드.
- ★ v3b: L1 멤버 4-model + 그룹별 캘리 형태. rolling CV + pooled 채점 인프라 확립.
- ★★ v7: clean downweight + subsample_freq 버그수정 — 이번 대회 최대 자체개발 단일 레버(+0.0021).
- **★★★ CNN 멤버(팀원)**: +0.00258, 91% 전이 — 4개월간 못 움직인 FICR 축을 처음 건드림. 유일하게
  "원시 격자를 그대로 소비"한 접근.
- 방법론 인프라: production-mode transfer harness(CNN급 고분산 레버용), 연도전이 홀드아웃 게이트(v8b
  교훈), D2 스트레스 게이트셋(연도전이보다 더 날카로움), TASK14 CNN 결정성 복원, freeze 게이트(TASK20).

### 실패/중립 (로컬은 좋았으나 LB 중립·하락, 또는 사전 기각)
- lean 피처 앙상블·프루닝·블렌딩·공간 피처, 외부데이터(JMA/Open-Meteo 1차), 손으로 만든 물리 피처,
  5-model 다양화, FICR w≥3, spread/gap/ramp 신규 피처, seasonal 캘리, seed bagging, lead_time 피처,
  C1 결정 레이어, quant w↑(새 base에선 단조악화).
- [LB 기각] w=0.5 프로브, ECMWF planB(v4, 같은해 학습→평가 일반화 실패), ASOS(rolling −0.0038).
- [3-seed 기각] GEFS 전 계열, SCADA 실측풍속 잔차보정(팀 ws_corr_ 중복).
- [LB 반전 기각] value-loss v8b(로컬 전게이트 통과 → LB −0.00089).
- [게이트2 기각] 딥시계열 v9(게이트1 근소통과 → 연도전이 실패).
- [23연속 CNN계열 기각] TASK3(공간채널 4후보)·TASK4-A(후류/지형)·TASK9-A/B(터빈별/잔차학습)·TASK10
  (점추정 변경)·TASK11-S1(AnEn/풍속우회/ExtraTrees)·TASK12(전체격자, 단 스택가치 정량화라는 부산물)·
  TASK13-S2(그룹별 CNN가중)·TASK15(실현체 선택, 영구종결).
- [2026-07-25 세션, 전부 기각] TASK16(회전DEM 빌드는 보류 유지, 방향구조 자체는 약하게 참이나 규모
  작음)·TASK18(캘리 축소)·TASK20(캘리 그리드 확장, freeze게이트 실패)·TASK22(젠센보정, 부분R² 미달)·
  TASK23(외부 NWP, 독립소스 부재)·**TASK24(GFS 미추출 변수 gh500, STEP1 통과 후 STEP2 연도전이/
  스트레스셋에서 결정적 기각)** · 사전학습 파운데이션모델 전체(WindFM/Tyan-WP/OffWindFM/DEVINE/
  FuXi-CFD).
- [2026-07-28, ENDGAME 부분재개 2건 전부 기각] **TASK29**(원시격자 재스크린 R0→R1, 3-arm×3-base
  선등록해석표 ③판정 — 배깅 축 영구종결) · **TASK30**(공간표현 재설계, GAP제거+위치조건부풀링+그룹head,
  Gate-W1 3조건 전부 불충족 FAIL — 표현 축 영구종결). ★TASK34(2026-07-29): TASK30의 FAIL이 지리적으로
  뒤섞인 LDAPS 래스터(4×4 오귀속, 실제는 4×5 다이아몬드) 위에서 수행됐음이 확인되어 "표현 축
  영구종결"을 잠정 철회하고 올바른 지도로 재검정 → **재검정도 FAIL**(real−null −0.00127, 원래보다
  나쁨) — 표현 축 영구종결 재확정. 상세는 4-42 참조.

### 제출·선택·검증 축 완전 종결 (4-34)
- A=`submission_v7cnn_fixed_v2.csv` — LB 0.6490737, ΔLB−0.00277, 규칙② 탈락(4-32). G3 실현체
  변동성(TASK13/15/T4-V1과 정합)이 FICR 손실의 주된 메커니즘, 이상신호 아님.
- B=`submission_task8_g1g2ficr.csv` — LB 0.6482289. C=`submission_v7cnn_fixed_v2_g2shift.csv` —
  LB 0.6491906. **S3 규칙③ 발동(A·C 둘 다 통과선 미달) → max(L0,L_B)=원본 확정.** 최종 선택 파일 =
  팀원 원본(`ficr_w1_v7_cnn(0.65183)`, LB 0.65184), 4건 실측 비교 후에도 불변.

## 7. 방법론적 규율 (사실상 가장 큰 교훈)

1. 판정은 실제 LB로만. in-sample cal은 뻥튀기, 겨울 forward는 반복 오도. 로컬은 방향만, 확정은 제출로.
2. base가 바뀌면 결론도 뒤집힌다. "캘리 무용", "팀피처 제외"는 quantile base 한정 참, MSE base에선 반전.
3. 전이 인식. adversarial AUC·분포 비교로 신기루 필터.
4. 겨울-편향. 검증창이 고출력 겨울이면 FICR·고분위 가중을 과대평가 — FICR 이득은 항상 할인.
5. GBDT/CNN 비결정성. paired 실험(diff=0 확보) 또는 결정성 코드 수정(TASK14) + 실현체 자체가 신호가
   아님을 검정(TASK15)으로 대응.
6. 채점은 pooled로. 월평균은 최악월 과대평가 — "12개월 pooled TOTAL"이 로컬 판정 지표.
7. 배포 가능성 필터. eval 정답이나 미래 정보가 필요한 로직은 후보에서 제외. train-only 컬럼(clean_1/2
   등)은 `c in te.columns` 가드 — 로컬 기준선과 프로덕션 피처셋 주기적 대조.
8. 프록시 검증 금지. 선택(selection)은 판정과 같은 해상도의 검증에서만(4창 HPO가 12창에서 전멸).
9. **로컬 다중게이트 통과 ≠ 연도전이 보장**(v8b 교훈). 마진성 레버는 로컬이 아무리 좋아도 전이 실패
   기저확률이 높다 — 특히 FICR 타깃 최적화는 밴드구조가 분포이동에 취약.
10. **★ 연도전이 홀드아웃도 최종 관문이 아니다**(M1 교훈, D2 스트레스 게이트셋). 스트레스셋이 연도전이가
    놓치는 전이실패를 추가로 포착 — 이후 모든 레버의 최종 관문. ★★영구 정정(2026-07-29, TASK36·M1
    LB실측): 이 규칙 자체가 틀렸다. D2 스트레스 게이트의 실측 성적은 진양성 0·위양성 1(TASK7b)·
    위음성 1(M1 — 스트레스가 기각했으나 LB ΔLB+0.00109로 실제 채택됨) — "모든 레버의 최종 관문"이
    아니라 방향성 없는 잡음에 가까웠다. **D2 스트레스셋은 영구 폐기(참고 지표로도 사용 금지)**,
    연도전이+널대조(§7-17)가 최종 관문이다. 이 규칙(#10)이 있었기에 M1이 한 세션 내내 잘못 기각된
    채로 방치됐다는 것 자체가 "방법론 규율도 실측으로 재검정해야 한다"는 방법론 규율(#18 참조).
11. **★ freeze 게이트가 롤링/월별 재적합의 착시를 잡아낸다**(TASK20 교훈). 매달 재적합해서 얻는 이득은
    소량씩 쌓인 과적합일 수 있음 — 1회 적합 후 고정 적용해도 이득이 유지되는지가 진짜 신호의 증거.
12. **★ 문언 그대로의 선등록 규칙도 맹신 금지**(TASK16 교훈). 균일 오프셋이 섞여 있으면 기계적 규칙
    적용이 "108개 셀 다 flag"처럼 무의미해질 수 있다 — 혼란변수(균일 수준)를 제거한 뒤에야 진짜
    구조(방향별 초과편향)가 드러남. 단, 판정 자체는 여전히 선등록 규칙의 정신(3독립모델 재현성)으로.
13. **★ S1 스크리닝 임계는 양성대조로 캘리브레이션**(TASK11-PC 교훈). 이미 LB로 검증된 성공 사례
    (CNN)를 새 스크린에 통과시켜봐야, 그 스크린이 "저분산 기준을 고분산 레버에 오적용"하는 실수를
    피할 수 있다(TASK1의 실패를 TASK11이 바로잡음).
14. **★ "모델 다양성" ≠ "오차구조 다양성"**(TASK11-S1 교훈). GBM→ExtraTrees/HistGBM으로 바꿔도 입력
    피처셋이 같으면 오차상관 r=0.99로 사실상 동일 — 새 알고리즘이 아니라 새 정보원(CNN처럼 원시
    격자를 직접 보거나, 독립 NWP처럼 다른 입력)이어야 오차구조가 갈라진다.
15. herbie 등 신규 패키지는 별도 venv(프로덕션 스택 파손 실사고 1회). CNN은 `deepseq_pipeline/.venv`.
16. **★ 신규 트랙 설계 전 기존 산출물 폴더를 기계적으로 조회할 것**(TASK26 취소 교훈, 2026-07-27).
    단일 진실원(`DACON_풍력_전체기록.md`) 등재 여부만 믿지 말고, 해당 라인의 `task*_*/` 폴더가 이미
    존재·완주됐는지 `find`/`ls`로 직접 확인 후 설계할 것 — 전략 리드조차 미등재 결과 때문에 이미
    기각된 라인을 "미빌드 상태"로 오인해 재검증을 지시할 수 있음(fable의 gh500/TASK26 사례).
17. **★ 소표본(n<2000)에서 문자상 게이트 통과 크기가 이 프로젝트 최대 실레버의 수배(예: 6배)면 규모
    상식으로 의심하고 즉시 잡음-대조 팔을 병행할 것**(TASK32 교훈, 2026-07-28). n=688·월별 cal창
    200~1500행 소표본에서 블렌드+캘리(월별 롤링 calibrate_total) 파이프라인 자체가 멤버 추가로
    캘리 자유도를 늘려 ANY 2차 신호(순수 가우시안 잡음 포함)를 +0.01 규모로 부풀렸다 — 실제 ECMWF
    정보가치가 아니라 파이프라인 아티팩트였음. 규칙을 문언 그대로만 따랐으면 이 사실을 못 잡고 LB
    프로브를 낭비했을 것. **이후 모든 소표본(n<2000) 검증은 잡음-대조 팔 필수 병행.**
18. **★★ 지표 구조 레버는 전이되고, 분포 적합 레버는 전이되지 않는다**(fable, 2026-07-29, M1 LB
    실측으로 확증). M1(채점 밴드 [0.05,0.20]×cap 구조를 이용한 보정)은 로컬 +0.00121 → 실LB
    +0.00109, **전이율 90%(이 프로젝트 최고)**. 대조군: v8b(FICR 분포 자체에 적합)는 음전이,
    gh500(기상 변수 추가)는 음전이, G2 shift(base별 편향에 적합)는 전이율 8%(TASK7b, 예측치의
    8%만 실현). **메커니즘 가설**: 채점식 구조(0.06/0.08 cap 경계, 0.05~0.20 카운트 근접 밴드)는
    대회가 바뀌어도 불변인 반면, 특정 데이터/모델의 분포적 편향에 적합한 보정은 분포가 조금만
    이동해도(2024→2025) 무너진다. **이후 후보 선정 1차 필터로 사용**: "이 레버가 이용하는 것이
    채점식의 고정된 구조인가, 아니면 특정 데이터셋의 우연한 분포인가"를 먼저 분류할 것.
    ★★부수 교훈(같은 사건): 전략 리드 자신이 3-seed 게이트에 단일seed 문턱(+0.0015)을 오적용해
    M1(+0.00121)을 문턱 미달로 잘못 기각할 뻔했다(팀 원규율 §4-2의 3-seed 등가 문턱은 +0.0010)
    — **방법론 규율·게이트 문턱 자체도 정기적으로 원문 대조·재검정 대상이다.**
    ★★★정밀화(fable, 2026-07-29, TASK43 직후): 위 규율을 "지표 구조 레버는 **'사후 조정'에
    적용할 때만** 전이된다. 학습 목적함수·표본 가중에 반영하면 실패한다"로 정밀화. 근거 3건 —
    M1(검열문턱×사후조정, LB+0.00109·전이 90%) / v8b(FICR밴드×손실함수, LB−0.00089) /
    TASK43(검열문턱×표본가중, −0.00140/−0.00074, 4-52). **기전**: 사후 조정은 행 단위 독립이라
    "채점 안 되는 행의 오차는 공짜"라는 산식의 전제가 정확히 성립하지만, 학습 단계는 모델이
    **공유 함수**라 이 전제가 깨진다 — 함수 추정 품질을 담보로 잡는 거래가 되기 때문(TASK43에서
    V1(완전제외)이 V2(완만축소)보다 더 나쁜 것이 직접 증거: 저출력행 제거가 채점구간 예측 품질도
    깎았다). **향후 후보 심사 시 "무엇을 이용하는가"뿐 아니라 "어디에 적용하는가"(사후조정 vs
    학습단계)를 함께 볼 것.**
19. **★★★ 측정·감사 역할과 가설 발안 역할을 분리 인식할 것**(fable 자기평가, 2026-07-29). 이
    세션에서 fable이 발안한 가설은 7전 7패(u*/전단 8후보, TASK40 중앙값계열, TASK43 표본가중 등)
    — 유일한 성공(M1)은 가설 발안이 아니라 **외부 증거(리더보드) 판독**에서 나왔다. 반면 fable의
    측정·감사 역할은 실적이 뚜렷하다(LDAPS 격자기하 결함 발견, 48후보 스크린이 u* 기각을
    사전예측, D2 스트레스게이트 위음성 발견→M1 부활, 3-seed 문턱 오적용 자기발견, 자기 측정치
    폐기). **전략 판단 주체는 신규 가설을 계속 만들어내는 것보다 검증·감사·판정에 집중할 때 더
    신뢰할 수 있다** — 새 레버 발안은 외부 신규 정보(리더보드 변동·게시판·팀원 레버·사용자 지시)
    트리거가 있을 때만 재개할 것. **전처리 축은 이 시점부로 완전 폐쇄**(변수·집계·격자·표현·
    표본가중 전부 소진, 48+~15건 누적 실패).
20. **★★★ 게이트 문턱의 검정력 — 통과는 필요조건일 뿐 판정 근거가 아니다**(fable, 2026-07-30,
    data_description 명세 확인에서 도출). 일 블록 부트스트랩 기준 대응표본 diff의 SE=0.00119.
    이 세션이 채택한 3-seed 문턱 +0.0010은 **0.84 SE**(1 표준편차 미만) — 참효과가 0이어도 통과
    확률 약 20%. 즉 **문턱 단독으로는 판별력이 없다.** 실제 판별을 수행한 것은 §7-17 잡음대조:
    M1 로컬 +0.00121(1.0 SE, 문턱만으로는 신뢰 불가)이 채택된 진짜 근거는 잡음팔(−0.00259)과의
    간격이 0.0038 ≈ **3.2 SE**였다는 점(LB+0.00109로 사후 확인). 이 세션의 위양성 3건(ECMWF·
    nullV2·noise16)이 이 검정력 부족으로 설명된다. **이후 모든 게이트는 §7-17 잡음대조 없이
    판정 금지** — 문턱 통과 여부만으로 채택·기각을 결정하지 말 것.
    ★부수 확인(같은 명세 검토): (a) 예보 구조상 매일 09:00 KST 초기화 1회가 D+1 01:00~D+2 00:00
    24시간을 전부 덮어(명세 §6) 리드타임(12~35h)≡시각이 완전 공선 — "리드타임 예보열화"는 이
    데이터에서 원리적으로 분리 불가. (b) `forecast_horizon_h` 피처가 명세상 진짜 리드와 24/24
    불일치(Spearman −0.50)하지만 4개 인코딩 비교(진짜리드 −0.00177/0·sin/cos −0.00159/0·시각원값
    −0.00080/0, 전부 3-seed 중 0/3 개선 — 현행이 최선)에서 기능적 결함이 아님을 확인, **수정하지
    않음**(불연속을 정오에 둬 야간을 안 쪼개는 우연한 이점으로 추정). (c) 잔차 일단위 ICC=0.377,
    설계효과 9.7이나 대응표본 diff에서는 상쇄돼 SE 배율 1.17배에 그침(위 SE=0.00119가 이를 반영한
    값).
21. **★★★★ 게이트 체계 재설계 — 문턱에서 간격으로, 프로브 예산제**(fable, 2026-07-30, §7-20 측정
    기반, 즉시 발효, 이 세션 전체 게이트 관행을 대체). **측정**: 분산 분해 결과 표집 SE=0.00119 vs
    시드 SE=0.00062 → 3-seed SE=0.00124, 10-seed SE=0.00121 — **시드를 늘려도 2%밖에 안 줄어든다**
    (병목은 seed가 아니라 2024 홀드아웃 1년 자체). **→ K(시드수) 확대로 검정력을 확보하려는 시도
    폐기.**

    **신규 판정 통계**: 판정 대상을 real의 절대값(문턱 통과 여부)에서 **real−null 간격**으로
    전환. 근거: M1(간격 +0.0038=2.2SE)은 채택되어 LB로 확인됐고, 실패 4건은 간격이 음수이거나
    0.06SE에 불과했다 — **real 절대값으로는 성패가 안 갈리지만 간격으로는 전부 갈린다.**
    **S1 스크린 문턱(신규)**: real−null ≥ **+0.0020** ∧ real이 base 대비 ≥2/3 seed 양수 ∧
    잡음팔이 real의 절반 미만 재현. (기존 +0.0010 단독 문턱보다 간격 기준을 낮게 잡은 이유: 로컬은
    스크린이지 최종판정이 아니며, 위음성(M1을 죽일 뻔했던 사고)이 위양성보다 더 비싸기 때문.)

    **LB 프로브 예산제(신규)**: S1 통과분만 프로브. **잔여 대회기간 총 프로브 상한 = 5회**
    (Public 과적합 통제 — k회 중 최고를 고르면 겉보기 이득이 σ√(2·ln k)만큼 부풀려짐, k=5 →
    약 1.8σ ≈ +0.0021 부풀림). 프로브 전 판정 규칙은 그대로 고정(ΔLB≥+0.0010→교체상신 /
    |ΔLB|<0.0010→기록만·원본유지 / ≤−0.0010→영구기각), **사후 재해석 금지**. ★복수 프로브가
    밴드 안(|ΔLB|<0.0010)에 들면 **가장 높은 것을 고르지 않는다**(그게 곧 Public 선택이 되므로) —
    밴드 내에서는 **재현 가능성이 높은 쪽**을 택한다(부록S/S3 규칙 승계).

    **폐기**: "3-seed 평균 +0.0010" 단독 판정 · 시드 수 확대로 검정력 확보 시도 · D2 스트레스셋
    (TASK36에서 이미 폐기, 재확인). **격상**: **§7-17 잡음대조는 이제 선택이 아니라 판정의
    본체다 — 잡음 팔 없는 결과는 애초에 판정 대상이 아니다.** **유지**: 롤링 금지·학습창 일치·
    base 일치(현재 M1 적용본)·§7-19 출처 추적성.

### 4-55. TASK45. 기각 건 재점검(신규 게이트 §7-21 적용) — 3건 전부 FAIL (2026-07-30,
`task45_reexamine/TASK45_result.md`)

정확히 3건(R-A/B/C, 사전확정·추가금지) 재검. S1 스크린(간격≥+0.0026[Bonferroni 상향]∧real≥2/3
seed양수∧잡음<real 절반)을 M1적용본 base 위 3-seed 연도전이로 적용. **3건 전부 FAIL — 배정된
LB 프로브 2회 중 0회 사용.**

**R-A**(TASK40 ② L1을 블렌드 멤버로, w∈{1,2,3}): 전 w FAIL — real이 어느 w에서도 유의미한
양의 신호 없음(최대 +0.00016, 사실상 0). "real≥2/3양수" 조건이 독립적으로 이미 불성립.

**R-B**(GEFS spread, `gefs_ws_spread`/`_rel`): **결정적 FAIL** — real 평균 −0.00178(0/3, 전부
음수) vs noise 평균 +0.00112(3/3, 전부 양수) — 부호부터 반대(TASK35·43과 같은 패턴).

**R-C**(ECMWF, D2필터 제거로 n=688→**2928** 확대, FIT/HOLDOUT 분할은 불변 유지): real 평균
+0.00134(2/3, 크기 작고 비일관) vs null −0.01131 — gap은 크지만 "잡음이 real 절반 이상 재현
시 즉시기각" 규칙이 문자 그대로 발동, **FAIL**. real 자체도 강건하지 않아(2/3, 소규모·비일관)
잡음대조 유효성과 무관하게 채택 근거는 못 됨.

**방법론 노트(판정 미영향, 향후 참고)**: R-A·R-C 둘 다 블록셔플형 잡음대조(날짜블록 단위로 값을
바꿔치기)가 "동일스케일 무정보 잡음"이 아니라 "엉뚱한 날짜 정보를 대입하는 파괴적 조작"이 되어
잡음 규모가 비정상적으로 커지는 공통 패턴 관찰 — 판정 자체는 real의 독자적 무신호로 이미 확정
되므로 재검·재설계 없음, §7-17 잡음대조 설계 시 참고 사항으로만 기재.

**조치**: 최종파일(`submission_m1_bandcorrected.csv`, LB 0.6529253) 불변.

### 4-56. TASK48. M1 밴드보정 상수의 적합창 안정성 측정 — 불안정(FAIL), 후속 게이트 설계 대기
(2026-07-30, `task48_bandconst_stability/TASK48_result.md`)

fable 지시(측정 전용, 레버 제안 아님). 동기: 리더보드 프론티어 분석에서 우리가 NMAE 0.868~0.895
구간 아웃라이어(FICR 중앙값 대비 −0.00916)로 확인됐고, 그 격차의 국소화(6~7% 오차구간, 가중질량
4.6%)에서 유일하게 작동하는 레버가 M1인데 그 상수 3개가 2024-11~12(2개월) 단회 적합이었다는 점을
검증.

**방법**: 재학습 없음 — `m1_holdout_preds_2024.parquet`(v7 GBDT, 2022-23 학습) + 동결 CNN
seed42 블렌드(w=0.20, 프로덕션과 동일 원본)를 cal=2024-09~10 캘리(1회 고정)로 변환한 뒤,
`fit_band_correction`만 W1(11-12,2개월)/W2(09-12,4개월)/W3(07-12,6개월)/W4(2024전체,12개월)로
재적합. W5(2023 전체, 연도전이)는 동결 CNN이 2023분 없어 base-only로 대체(조건차 명시).
★sanity: W1 결과가 실제 프로덕션 값 `[+0.06,+0.03,+0.05]`·밴드내표본 `[370,455,414]`와
bit-identical 일치 — 측정 파이프라인이 프로덕션과 동일 대상 재현 확인.

**결과표(band_shift, ×cap)**:

| 창 | G1 | G2 | G3 |
|---|---|---|---|
| W1(2개월, 현행) | 0.06 | 0.03 | 0.05 |
| W2(4개월) | 0.04 | 0.01 | 0.04 |
| W3(6개월) | 0.04 | 0.01 | 0.04 |
| W4(12개월) | 0.04 | 0.01 | 0.04 |
| W5(2023, base-only) | 0.04 | 0.02 | 0.04 |

**판정(선등록 규칙 기계적 적용)**: 부호는 전 창 양수로 일관(통과). **폭은 G1·G2가 문턱(±0.01)을
초과(둘 다 0.02), G3만 경계상 통과(0.01) → 불안정 판정.** 관측 패턴: 무작위 산포가 아니라
W1→W2에서 뚜렷이 하락 후 W2~W4가 완전 평형(G2는 W1이 장기값의 3배) — 2개월 표본(밴드내
370~455개, 21개 그리드후보 탐색)이 더 긴 창과 다른 값에서 수렴하는 구조적 패턴. W5(2023)는
W1~W4 범위 안(장기값 쪽에 더 가까움)이나 CNN 미포함이라 확정 근거는 아님.

**조치**: [금지] 규정 그대로 준수 — 밴드경계/상수개수/그리드해상도 변경 없음, 제출파일
(`submission_m1_bandcorrected.csv`) 불변. 선등록 규칙상 "불안정" 분기이므로 원칙적으로는
"후속 검정 설계(더 긴 창 M1′을 §7-21 체계로 게이트, 잡음대조 필수, 프로브 1회)" 대상이나, 이는
TASK48 범위 밖 — **fable의 별도 착수 승인 대기**, 이번 세션에서 게이트 설계·실행 없음.

### 4-57. TASK49. M1′(장기창 상수) 게이트 — S1 FAIL, 현행 M1 유지 (2026-07-30,
`task49_m1prime_gate/TASK49_result.md`)

TASK48 선등록 분기(불안정 FAIL) 발동으로 fable이 승인한 후속 검정. 단일 변경(M1 밴드보정 상수만
교체, 밴드경계·상수개수·그리드해상도 불변)으로 현행(A, W1=`[0.06,0.03,0.05]`) vs M1′(B, real,
W5=2023 적합 `[0.04,0.02,0.04]`, out-of-sample) 비교. 1단계 캘리(cal=2024-09~10)는 두 후보가
공유, 2024 전체(3-seed, 동결 CNN seed42)에서 밴드보정 상수만 바꿔 채점.

**결과**: real(B−A) 평균 **+0.00027**(3/3 seed 양수) vs 잡음5팔(배포값 ±0.02 섭동, RNG
seed=4900000) 중앙값 **−0.00009** → **gap +0.00036, 문턱 +0.0020에 크게 미달 → S1 FAIL.**
잡음팔 중 하나(noise2 +0.00019)가 real과 거의 같은 크기 — 무작위 섭동도 비슷한 "이득"을 낸다는
아티팩트 패턴.

**해석**: TASK48이 찾은 상수 자체의 창간 차이(계산값)는 실재하나, 그 차이가 2024 전체 평가에서
만드는 점수 효과는 잡음 수준과 구분되지 않는다 — "상수 계산값의 차이 ≠ 채점상 유의한 차이".

**조치**: 현행 M1 상수(W1) 유지, 최종파일(`submission_m1_bandcorrected.csv`, LB 0.6529253) 불변.
LB 프로브 미사용(잔여 5회 그대로). **이 라인 종결** — 새 근거 없이 재시도 비권장.

## 8. 참고자료

- 관련 논문 5편: NWP 기반 풍력 예측, 확률·quantile 예측, 캘리브레이션, 파워커브 보정, 앙상블 다양성.
- DACON 사전 워크숍: 세션2(홍진규, 3승법칙 P∝v³) · 세션6(이금민, SCADA·파워커브·데이터>모델) ·
  세션7(김기민, 평가산식 50:50·10%이용률 제외·Public/Private 40:60).
- 부록: "1회 우승=대기과학자, 2회 우승=피처엔지니어, 정답=하이브리드".
- **HEFTCom2024**(Hybrid Energy Forecasting and Trading Competition 2024, 동형 대회):
  - GEB팀 솔루션 논문(arXiv 2505.10367, 코드 `github.com/BigdogManLuo/HEFTcom24`) — 멀티NWP
    "sister forecast" 스태킹(여러 예보센터 앙상블 멤버를 개별 피처로 스태킹)이 핵심 기법.
  - 우승팀 SVK — 성공요인으로 기상예보 데이터셋 3종(멀티NWP) + **발전소 가용성 문제(케이블 고장 등)
    처리**를 명시. TASK31(clean_1/2/3 원인진단)이 이 축과 직접 정합.
  - 평가식 맥락: 이 대회 평가식(FICR 계단형 단가)은 한국 재생에너지 발전량 예측제도(오차 6%이하
    → 4원/kWh, 6~8% → 3원/kWh 인센티브 등급)를 반영한 구조.

## 9. 현재 상태 & 다음 방향 — ★ENDGAME 부분재개 국면 (2026-07-28 갱신, 09절 헤더는 최종파일 갱신
전 시점 서술 — ★★2026-07-29 최신 상태는 4-47 참조)

★★★최신(2026-07-29): **최종파일 = `submission_m1_bandcorrected.csv`, LB 0.6529253**(순위
약 62위) — TASK37 M1 밴드보정 채택(4-47). 아래는 그 이전(2026-07-28) 시점 서술 그대로 보존.
★2026-07-30 갱신: TASK46(2차 산출물 패키지 최종화) 완료. TASK48(M1 상수 적합창 안정성 측정,
4-56)은 **불안정(FAIL)** 판정 — 후속 TASK49(M1′ 게이트, 4-57)에서 **S1 FAIL**로 종결(상수
계산값 차이는 실재하나 채점 효과는 잡음과 구분 안 됨). 현행 M1(W1 상수) 유지, 최종파일 불변.

(2026-07-28 시점 서술) **최종파일 = 팀원 `submission_ficr_w1_v7_cnn(0.65183).csv`, LB 0.65184.**
4건 실측(원본/A/B/C) 비교 후 S3 규칙③으로 재확정(4-34). 8/14 09:59 전 선택 상태 확인 필수, 추가
제출 금지(진단만) — ★이 마지막 문장(추가 제출 금지)은 TASK36-38 fable 지시로 무효화됨(스트레스셋
자격정지/폐기·M1 LB 프로브 승인으로 갱신).

**★서사 정정(2026-07-28, fable 지시, 최우선·즉시반영 완료)**: "프론티어와의 0.02 격차는 접근 불가능한
입력 때문"이라는 서술 전부 폐기(4-23·4-25·발표_골격_v1.md 1장/5장에 정정 반영 완료). 참가자격이
대학(원)생·취준생이고 기관전용 데이터가 규칙위반이므로 상위팀도 같은 공개 데이터를 쓴다 — 이 주장은
성립하지 않는다. 정정 서술: "피처·캘리·후처리·손실·모델클래스·공간표현(2종)·데이터경로를 선등록
게이트와 널대조로 전부 배제했다. 격차가 존재하고 같은 입력으로 달성 가능하다는 것도 안다. 우리는 그
경로를 찾지 못했고, 어디에 없는지는 정확히 안다."

**모델링·데이터 경로는 여전히 봉쇄, 배깅·공간표현 축도 신규 종결(4-35·4-36)** — 종결축 전체 목록:
데이터경로 4중봉쇄·GBDT피처·캘리(TASK18/20 동결)·수작업 물리피처(부록U)·블렌드비율(v7a)·실현체선택
(TASK15)·gh500(TASK24 STEP2)·배깅(TASK2·R1)·공간표현(TASK30 Gate-W1 FAIL — TASK34가 LDAPS 래스터
기하결함 수정 후 재검정, 재검정도 FAIL, 4-42 참조)·제출/선택/검증(부록S/T, 4-34).

**★현재 활성 트랙 (4-37, fable 통합지시 2026-07-28, 이전 재개가설 5연속 실패 이력 대비 사전 할인
명시)**:
1. **TASK31 X-1 완료(4-38)** — 6항목 측정 완료, 판정 없이 사실 보고
   (`task31_target_quality/TASK31_X1_cleanmask_anatomy.md`). 핵심: 착빙조건 dirty율 3.4~5.2배·
   clean==0 인접 2024채점행 오차 뚜렷악화(G2 NMAE 3.8배)·G3 구조적결측(84%) 분리 필요. X-2(가중
   재설계) 착수는 fable 결정 대기.
2. **TASK32 완료·영구종결 승인(4-39·4-40)** — G1/G2 문자상 PASS했으나 순수잡음 대조도 동일 문턱을
   통과(파이프라인이 소표본 n=688에서 ANY 신호를 부풀리는 아티팩트로 판정) → 신뢰불가로 하향, LB
   프로브 비권고. **ECMWF 라인 영구종결(3번째이자 마지막 시도).** fable이 방법론 규율 §7-17로 승격
   승인.
3. **TASK31 X-2 완료·영구종결(4-40·4-41)** — fable이 X-1 재판독(구조결측=더 버려야/착빙=덜 버려야,
   개입 지점 다름) 후 승인한 변형 3개(V1 구조결측제외·V2 착빙보존·V3=V1∧V2)를 3-seed로 전부 실행,
   Y1(연도전이)·Y2(D2 스트레스)·Y3(널테스트) 3중 게이트 결과 **전원 결정적 FAIL**(V2는 널대조보다도
   나쁨). LB 프로브 없음. clean-downweight 상수 세분화 라인 종결.

**TASK31(X-1·X-2)·TASK32 전부 완료 — 이 세션의 활성 트랙 소진.** fable 판정/다음 지시 대기.

재가동 조건(신규 트랙): 30위 진입 확정+사용자 요청 / 팀원 신규 레버 / 사용자 명시 지시.

**2차 발표 소재 후보**: 게이트 양방향 검증(가양성·가음성 둘 다 실제로 잡아냄, TASK1이 스스로의 임계
오류를 TASK11이 재교정, **★TASK26 취소 — 선등록 규율이 전략 리드 자신의 오판까지 컴퓨트 지출 전
차단**) · 재검증에서 잡힌 버그 3건(v9 캘리버그, `_nan_to_med` 축순회, subsample_freq no-op) ·
**부분R² 스크린 = 필요조건이지 충분조건 아님, 4번째 실증**(v8b·v9·TASK20에 이어 TASK24 gh500 — STEP1
통과가 STEP2 연도전이·스트레스 통과를 보장하지 않음) · 스택가치 정량화(0.735%, TASK12) · FICR-NMAE
환율(TASK7/18에서 실측한 트레이드오프 비대칭) · CNN 실현체 로또 비전이성(TASK15, ρ=0.02) · 캘리
SHIFT_GRID ±0.08=안전장치 실증(TASK20, 확장 시 freeze게이트 붕괴) · base가 물리적으로 이미 선점한
신호 영역(TASK12) · ★데이터 실사(TASK23→27) + ★배깅/공간표현 재개시도 2건도 게이트가 정직하게
닫음(TASK29/30) · ★서사 정정 자체("격차=접근불가 입력"을 스스로 폐기한 것)가 방법론 규율의 정직성을
보여주는 사례.

**잔여 옵션**: TASK31(X-1·X-2)·TASK32 전부 완료·종결. 그 외(GFS0.25° 미추출 변수 gh500·TASK26류 재시도·배깅·공간표현)
는 전부 결정적으로 봉쇄, 재검토 여지 없음.

### 4-35. ★ TASK-G3TAIL — G3 상단 tail 확장 프로브 (2026-08-09, 사용자 지시로 재가동, `opencode_TASK_G3TAIL_20260809.md`)

**성격**: 신규 학습이 아닌, 기존 최고 제출본 `submission_A_m1v2_coordBA.csv`(LB 0.65465, 67위)의
**G3 단독 단조·순서보존 후처리 변환** 프로브. 사용자 명시 지시로 엔드게임 재가동 조건 ③ 충족.
선등록(`work_a_package/PREREG_g3tail_20260809.md`)·가드 3층·널테스트 전부 완료.

**진단 근거(지시서 §0-1)**: ① "병목 100% FICR" 진단은 낡음 — 컷 상승(0.87496→0.87850)으로
NMAE −0.00457 적자, 갭 기여도 NMAE 44.2% / FICR 55.8% (사용자 제공 LB 수치). ② D3 "후처리 헤드룸≈0"
은 LB가 반증(동 NMAE 팀들이 FICR +0.015~0.021) — 헤드룸은 오차 분포 모양에 있음. ③ **핵심**: G3 예측
CF가 0.9019에서 물리적으로 막혀 있는 반면 실측 G3는 1.006까지 감 — 실제>pmax+0.08은 0점 확정(가중
3.49%), >pmax+0.06은 만점 불가(가중 6.94%) 몰수.

**변환**: `g=kpx_group_3 단독, a=0.70, λ∈{1.00(널),1.10,1.20,1.35,1.50}`, 앵커 이하 불변·상한 clip 1.0.
G1/G2 **바이트 단위 불변**(하드 요구사항). 지시서 §4 코드에 "변경 행만 재포맷, 나머지 원문 유지"
(§4 경고문의 전환 조항)를 적용 — base 원문에 4자리 소수 27행이 있어 λ=1.00 바이트 동일성 달성.

**검증 결과 (전부 PASS)**:
- **§3-1 대조**: 전 λ에서 new_max_cf·변경행(1129)·Δ평균·Δmax·전체평균Δ가 **소수 5자리까지 일치**
  (λ=1.50 Δmax 0.09911 포함) — base 파일이 지시서 분석 대상과 동일함을 확인.
- **NULL**: λ=1.00 md5 `9f792b40…` == base md5 (바이트 일치).
- **L1**: G1/G2 문자열 100% 동일 · ID/dtm 100% · 8760행 · 헤더 정합.
- **L2**: 변경행 1129(λ>1) / 0(λ=1) · Δ값 §3-1 일치 · 앵커 이하 변경 0행.
- **L3**: NaN/음수/cap초과 0 · Spearman ρ=1.0 (단조성 완전 보존) · sample_submission 정합.
- **몰수 가중(train_labels masked, 실측 max_cf 1.0062)**: base 6.94%/3.49% → λ1.20 0.54%/0.00% →
  λ1.35 0.00%/0.00% (지시서 기대 "λ1.20에서 ≈0%" 충족).
- **질량비 꼬리**: [0.85,0.95) base 0.05 → λ1.20 0.10 → λ1.35 0.12 (지시서 기대 0.06, 성공 조건 충족).

**판정**: 가드 전부 통과 → 1차 제출 후보 `submission_coordBA_g3tail_l120.csv`(md5 `722db0bb…`).
**LB 프로브 2회 예산 선등록**: 8/10 λ=1.20 → 8/11 λ=1.35 (하루 1회 초과 금지). ΔLB 기준:
≥+0.0010 채택 / |ΔLB|<0.0010 무판정·더 큰 λ 1회 재프로브 / ≤−0.0010 영구기각·G1/G2 트랙(a=0.85) 전환.
방어선 `submission_A_m1v2_coordBA.csv`(0.65465) 항상 복귀 가능 유지. 사용자 실제 제출·ΔLB 보고 필요.

**산출물**: `work_a_package/make_g3tail_probe.py` · `submission_coordBA_g3tail_l{100,110,120,135,150}.csv` ·
`g3tail_report.md` · `PREREG_g3tail_20260809.md`.

### 4-36. ★ TASK-G12TAIL — 누적 프로브 매트릭스 (G2 → G1) (2026-08-09, 사용자 지시, `opencode_TASK_G12TAIL_20260810.md`)

**성격**: G3TAIL의 검증된 단조 변환을 G2/G1로 확장한 **누적 프로브 매트릭스**. 신규 학습 없음.
**전제(지시서 §0)**: 그룹별 효과의 정확한 가법성 ΔTOTAL=(1/6)Σ_g(ΔFICR_g−ΔNMAE_g) — G3를 바꿔도
G1/G2 항은 변하지 않음(근사 아님, 정확히). 1/6 희석으로 G3 단독으로는 필요량 +0.00518 불가 →
세 그룹 전부 필요. **선행 조건(G3 λ1.20 8/10 ΔLB) 미확보 → G3W 미확정. ΔLB와 무관한 전 준비 작업 완료.**

**파라미터 (탐색 금지)**: G3 a=0.70 / G2 a=0.80 / G1 a=0.80, λ∈{1.20,1.35} (a=0.85는 전체평균Δ
0.00053 cap으로 노이즈밴드 아래라 배제). 프로브 순서: P2=G3W+G2(λ1.20) → P3=+G1(λ1.20) → P4=양(+)판정
그룹만 λ1.35 승급. G2를 G1보다 먼저: `[0.80,1.10)` FICR 가중 36.77% 최대, `[0.85,0.95)` 단일 21.89%.

**검증 결과 (전부 PASS)**:
- **§2-1 assertion**: G1(852행, Δmax λ1.20=0.03322) / G2(1193행, Δmax=0.03317) 전 λ·전 항목
  소수 5자리 일치. G1 변경행 852<859는 cap(1.0000) 도달 7행 clip 불변 — 지시서 설명과 정합.
- **NULL**: 전 λ=1.00 md5 `9f792b40…` == base (바이트 일치).
- **L1/L2/L3**: --spec 미지정 그룹 문자열 100% · 변경량 정합 · 앵커 이하 변경 0 · NaN/음수/cap초과 0 ·
  그룹별 Spearman ρ=1.0 · 결정성(2회 실행 md5 동일 `9b214aa5…`).
- **질량비(masked: 예측·실측 각각 자체 CF≥0.10cap, 분포 점유율 대조)**: G2 `[0.95,1.10)` 0.13→0.40(λ1.20)→
  0.62(λ1.35), G1 0.14→0.27→0.38 — G2 상단 결손이 전 그룹 최대(21.89% 근거)와 정합.

**가드 검증 중 발견·수정 (파일 결함 아님)**: 검증 스크립트 자체 버그 2건 — ① Python bool이 int의
서브클래스라 `isinstance(True,int)` 참 → L1 판정 오판(`isinstance(v,bool)` 제외로 수정) ② 그룹 컬럼
인덱스 오차(`r[1+g]`로 수정).

**판정**: 전 가드 PASS → P2 후보 준비 완료. G3W 시나리오 3종(미채택/λ1.20/λ1.35)×G2(λ1.20/λ1.35)×G1
(λ1.20/λ1.35) 11개 파일 생성. **제출은 8/10 G3 ΔLB 판독 후 사용자가 수행** (자동 제출 금지).
선등록 `work_a_package/PREREG_g12tail_20260810.md`: 판정 ΔLB≥+0.0010 채택 / +0.0003~+0.0010 방향채택(λ1.35
승급 후보) / |ΔLB|<0.0003 무판정 / ≤−0.0010 영구기각. 하루 2회 상한(2회차는 1회차 LB 확인 후). 동시 제출 금지.

**산출물**: `work_a_package/make_tail_probe.py`(일반화 CLI, `--spec col:anchor:λ`) ·
`submission_tail_*.csv` 11종 · `g12tail_report.md` · `PREREG_g12tail_20260810.md`.

### 4-36b. ★ TASK-G12TAIL 전량 매트릭스 — λ∈{1.00,1.20,1.35}×3그룹 = 27종 (2026-08-09, 사용자 지시)

G12TAIL 지시에 따라 `make_tail_probe.py`로 **27종 전량**(3³)을 생성·가드 전수 재검증.
앵커 고정: G3=0.70 · G2=0.80 · G1=0.80. 파일명 규칙: `submission_tail_g3l{λ}_g2l{λ}_g1l{λ}.csv`
(λ=1.00 그룹은 이름에서 생략, 전부 1.00이면 `submission_tail.csv`).

**결과 (전부 PASS)**:
- **NULL**: `submission_tail.csv`(전 λ=1.00) md5 `9f792b40…` == base — 바이트 일치.
- **가드 L1/L2/L3**: λ=1.00 그룹 원문 100% 미접촉 · 변경행 (G3 1129/G2 1193/G1 852, λ=1.00 0) ·
  앵커 이하 변경 0 · NaN/음수/cap초과 0 · 그룹별 ρ=1.0 · 결정성(기존 G12TAIL 11종 파일과 md5 재현 확인).
- **변경행 수 (그룹별, λ에만 의존)**: G3 λ≥1.20→1129 · G2→1193 · G1→852 (G1 852<859는 cap 도달 7행 clip 불변).

**산출물**: `work_a_package/build_tail_matrix.py`(재현 빌더) · `submission_tail_*.csv` **27종** ·
`tail_matrix_manifest.md`(27종 md5 표 + 조합별 변경행 수) · `tail_matrix_results.json`.
P2/P3/P4 후보 선정은 기존 선등록(`PREREG_g12tail_20260810.md`) 유지, 제출은 사용자.

### 4-36c. TASK-G12TAIL 쇼링크 방향 (λ<1) — shrink085/090/080 (2026-08-09, 사용자 지시)

`make_tail_probe.py`의 파라미터 가드를 `λ<1.0 → 차단`에서 **`λ≤0 → 차단`**으로 완화
(λ>0이면 단조성 보존 — ρ=1.0 실측 확인). 동일 앵커(G3 0.70·G2/G1 0.80)로 λ∈{0.85,0.90,0.80}
쇼링크 3종 생성·가드 전수 PASS:
- 변경행: G3 1129 / G2 1193 / **G1 859** — λ<1에선 cap 도달 7행도 감소해 변경되므로 cf>a 전부
  (λ>1 때 852와의 차이는 이 때문, 정상)
- NULL(전 λ=1.00 == base `9f792b40…`) ✅ · L1/L2/L3 ✅ (3그룹 ρ=1.0 단조성 보존)
- 산출물: `submission_tail_shrink085/090/080.csv` · md5 `ea96a9e6…`/`110d1d7a…`/`77fc754f…`
- 용도: 상단 tail 축소 방향 예비 프로브 (선등록 판정 체계 미적용 — 명시 지시 시에만 제출 고려)

### 4-37. ★ TASK-METRICW — 평가지표 정합 표본가중 재학습 (2026-08-09, 사용자 지시, `opencode_TASK_METRICW_20260809.md`)

**문제 정의**: 평가식은 actual≥0.10cap 행에서만 계산되고 FICR은 실측 발전량 가중인데, 표준
회귀 학습은 전체 분포(마스크 밖 39~46%)의 조건부 중앙값을 추정 → 점수가 걸린 부분집합 위에서
구조적 하향편향(마스크 −0.18~0.21cap, FICR 가중 −0.32~0.36cap). 인수인계서의 "G3 균일 음편향
−6.6~7.0%cap"은 모델 결함이 아니라 이 마스킹 아티팩트로 설명됨 (지시서 §0).

**변형 3종**: W0(균등, 컨트롤) / W1(마스크만) / W2(정식: 마스크 + 0.5/n + 0.5y/Σy) × seed
{42,1337,2024} + W2S(널 셔플). v7 GBDT 계보(`m1_gen_holdout_preds.py` 구조, 4-GBDT 앙상블)
에서 **표본가중만 교체** — 피처·하이퍼파라미터·시드 불변. pipeline_v2 계보 미사용.
파이프라인: 2022-23 학습 → 2024 전체 예측(연도전이 홀드아웃), 캘리 없음(가중 효과 순수 측정).

**구현 이슈 2건 (모두 해결)**:
1. **W2 가중 스케일 붕괴**: 원수식 가중 합=1.0(~1e-4 절대값)은 lightgbm subsample bagging에서
   손상 → 스모크에서 W2 TOTAL −0.07 붕괴 확인. **합=n 정규화**(상대 비율 2.6:1 보존, 수식적 동일)로 해결.
2. **결정성**: LGB(MSE)가 비균등 가중치에서 병렬 합산 잡음(~7e-12)으로 md5 불일치 → `deterministic=True`
   추가로 해결. XGB/Cat/LGB-L1은 원래 결정적. 검증: W2 seed42 2회 md5 `6f1e43f3…` 일치.

**결과 (연도전이 홀드아웃, 대회 평가식 원본 — competition_metric)**:
| 가중 | TOTAL | Δ vs W0 | seed 양수 |
|---|---|---|---|
| W0 | 0.61145 | — | — |
| W1 | 0.62493 | **+0.01348** | 3/3 |
| **W2** | 0.62913 | **+0.01768** | **3/3** |
| W2S(널) | 0.60810 | −0.00335 | 0/1 |

- **W2 vs W2S(널) +0.02122** — 널 유의 초과. 선등록 판정(≥+0.0010 ∧ 2/3 ∧ 널 초과) **통과**.
- FICR이 +0.03184로 가장 크게 개선 (0.34957→0.38141) — 마스크 가중이 FICR 축을 직접 겨냥.
- **질량비(2024 홀드아웃)**: 고출력 구간 상승 확인(G3 [0.70,0.85) 1.23→1.56, G2 [0.85,0.95)
  0.77→0.86)이나 [0.95,1.10) 극단 꼬리는 여전히 0.00(해당 구간 실측 부재로 미포착), [0.10,0.30)
  과잉은 악화(1.2→3.1, TOTAL에는 무해). **지시서 §1-4 "분포 결손 해소"는 부분 성공으로 기록.**
- **의의**: 8연속 실패(2023/24 적합 상수 이동)와 달리 **연도 적합 상수 0개** — 목적함수 구조 변경만으로
  전이 성공. CNN 편입과 동일한 "순수 구조 변경" 패턴 (지시서 §0-3 논거 실측 지지).

**환경**: lgaimers conda env (numpy 1.26.4 / catboost 1.2.10 / lgb 4.7.0 / xgb 3.2.0 / pandas 2.0.3) —
지시서 명시(VERSIONS.txt 기준)와 부분 차이, 상대 비교라 유효. base(/opt/anaconda3)에 GBDT 스택 부재.

**판정**: 원리적 개선 **확인 (통과)**. 제출 여부는 헤드/사용자 판단 대기 — 프로브 예산은 G3TAIL 우선.
`submission_metricw_W2.csv` 생성 가능하나 **즉시 제출 금지**. G3TAIL λ와 결합([0.10,0.30) 과잉 상쇄) 여지는 후속 지시 대기.

**산출물**: `work_a_package/metricw/` — `train_metricw.py`·`eval_metricw.py`·`METRICW_report.md`·
`pred_{W0,W1,W2}_seed{42,1337,2024}.parquet`(9)·`pred_W2S_seed42.parquet`(널)·`wstats_*.json`·
`metricw_results.json` · `PREREG_metricw_20260809.md`(선등록).

### 4-38. ★ TASK-W2CHAIN — 동결 체인 스왑 결정실험 (2026-08-09, 사용자 지시, `opencode_TASK_W2CHAIN_20260809.md`)

**질문**: METRICW W2의 체인 전 이득(+0.01768)이 기존 제출 체인(iso/shift/M1/coordBA/CNN)에
이미 흡수됐는가? → **답: 흡수됨. Δ_체인후 음수, 중복률 149.7%.** 이 숫자 하나가 8/10 제출
여부를 결정 — **결과: 제출 금지.**

**설계**: `chain(X)=coordBA(M1(shift(iso(blend(GBDT=X, CNN=동결)))))`에서 X만 W0↔W1↔W2 스왑.
체인 상수는 원본 v7 GBDT로 적합 후 **동결** (재적합 0회 — 연도 적합 상수 재도입 방지).
coordBA 좌표는 `submission_A_m1v2.csv` vs `_coordBA.csv` 차이로 실측 복원 (G1 CF[0.7,1.0]∧비JJA
1572행 +216kW). 평가: 2024 연도전이 홀드아웃(2022-23 학습) 전체, 대회 평가식 원본, 3seed.

**체인 추출 검증**: 캘리만 0.62906 / M1 0.63093 (run.py와 정확히 일치), submission_A.md5
`6d5e4214…` 재현. 동결 상수: shifts=[0.03,0.07,0.04], M1=[0.045,0.04,0.055].

**결과 (2024 홀드아웃 TOTAL, 3seed 평균)**:
| 가중 | 체인 전 | 체인 후 | Δ_체인후 | 중복률 |
|---|---|---|---|---|
| W0 | 0.61145 | **0.63402** | — | — |
| W1 | 0.62493 | 0.62610 | **−0.00793** (0/3) | 158.8% |
| **W2** | 0.62913 | 0.62524 | **−0.00878** (0/3) | **149.7%** |

- **단계별 역전 국소화**: blend +0.01725 → iso +0.01095 → **shift −0.00671(역전)** → M1 −0.00931.
  iso가 일부 흡수, **shift 캘리가 과잉 보정하여 부호 반전** — 지시서 §0-1 우려 실증.
- **그룹별**: G2가 역전 핵심 (ΔFICR −0.02036 — W2 마스크 가중과 shift +0.07 캘리 정면 충돌).
  G1/G3는 FICR 개선이나 NMAE 손실로 상쇄.
- **캘리 창 × 2024 겹침**: 있음 (cal 09-10 + M1 11-12 self-ref 모두 2024 내부 — in-sample,
  W0 대비 상대 비교라 공통 오염으로 유효).

**판정: 제출 금지** (<0 밴드). `submission_metricw_W2chain_frozen.csv` 생성됨 (md5
`eef91a61…`, 8760행·NaN/음수/cap초과 0) — **2차 평가 소명 자산으로만 보존, 즉시 제출 금지.**
**소명 관점**: METRICW는 "체인 전 맨몸 GBDT"에서 원리적 개선(+0.01768)을 소명하고, W2CHAIN은
"기존 캘리가 이미 그 편향을 흡수 → W2 얹으면 악화"의 중복 정량화(149.7%)로 '왜 제출 안 했는지'
근거. W2를 쓰려면 shift 캘리 제거가 필요하나 연도 적합 상수 재설계 = 8연속 실패 구조 회귀
위험이라 본 실험 범위 밖 (후속 지시 대기).

**산출물**: `work_a_package/w2chain/` — `chain_frozen.py`·`stage_trace.py`·`build_submission.py`·
`W2CHAIN_report.md`·`frozen_constants.pkl(+.isos)`·`w2chain_raw.json` · 루트
`submission_metricw_W2chain_frozen.csv` · `PREREG_w2chain_20260809.md`(선등록) ·
`work_a_package/metricw/pred_test_W2_seed{42,1337,2024}.parquet`(W2 2025 test 예측).

### 4-39. TASK-W2NOSHIFT — W2 × shift 캘리 계수 스케일링 (2026-08-09, 사용자 지시, `opencode_TASK_W2NOSHIFT_20260809.md`)

**목적**: W2CHAIN에서 역전 원인으로 국소화된 **shift 캘리**(G2 +0.07)의 계수를 축소하면
W2의 마스크 교정 이득이 드러나는지 측정. chain_frozen 재사용(재학습 0회), iso/M1/coordBA/
블렌드/CNN 불변. 변형 3종(shift 계수만 변경): V1=[0,0,0] / V2=[0.5,0.5,0.5] / V3=[1,0,1]
(G2만 제거). **★ 비겹침 부분구간(01-08월, cal 09-10 미포함) 별도 산출** — in-sample 오염이
덜한 유일한 숫자.

**결과 (2024 연도전이 홀드아웃, 대회식, 3seed 평균)**:
| 변형 | 전구간 TOTAL | Δ vs W0(0.63402) | seg01-08 | seg Δ vs W0(0.62902) |
|---|---|---|---|---|
| V1 | 0.62820 | **−0.00583** (0/3) | 0.62427 | −0.00475 |
| V2 | 0.62954 | **−0.00449** (0/3) | 0.62456 | −0.00446 |
| V3 | 0.63056 | **−0.00347** (0/3) | 0.63163 | **+0.00260** |

- shift 축소 방향은 단조 개선(W2원본 0.62524→V3 0.63056)이나 **chain(W0)을 못 넘음**.
- V3만 01-08월에서 유일하게 W0 초과(+0.00260, 3/3) — G2 shift 제거의 여름 유효 신호.
  그러나 겨울(11-12월, cal 창 포함) 손실로 전구간 기각.
- **판정: 전 변형 기각** (선등록: 전구간 ≥+0.005 ∧ seg ≥+0.002 ∧ 2/3 양수 — 셋 다 전구간·seed
  미달). 변형 추가 생성 금지. 제출 후보 생성 안 함.
- 계절 분리 가중(겨울만 shift 유지 등) 가능성은 선등록 범위 밖 — 후속 지시 대기로 기록.

**산출물**: `work_a_package/w2chain/` — `run_w2noshift.py`·`w2noshift_results.json`(판정 포함)·
`W2NOSHIFT_report.md` · `PREREG_w2noshift_20260809.md`(선등록).

### 4-40. ★ TASK-RETROHARNESS — 검증 하네스 역검정 (2026-08-09, 사용자 지시, `opencode_TASK_RETROHARNESS_20260809.md`)

**문제**: 10전 10패(최고 RWS v4 +0.00343 전층가드 PASS → LB −0.00861)는 후보가 아니라
**측정기(하네스)가 고장**임을 시사. LB 확인 제출 파일을 정답지로 삼아 "어느 로컬 검증
방식이 LB 부호·순위를 맞히는가"를 역측정하는 메타 실험.

**정답지 (n=8, md5 무결성)**: 기준 = `submission_A_m1v2_coordBA.csv`(LB 0.65466).
g3tail_l120(−0.00050) · m1v2(−0.00063) · m1v3(−0.00089) · bandcorrected(−0.00173) ·
v7_cnn(−0.00282) · g3shift03(−0.00352) · avgblend(−0.00613) · dm(−0.02045).
**재현 가능 n=4로 축소**: v7_cnn(모델 상이)·avgblend(블렌드 대상 부재)·dm(코드 부재)·
m1v3(M1 shift 미확정) 제외. **또한 지시서 표의 RWS v4·signcorr·m1noise·v8itrans·c2·
avg_smooth·curtailP2는 저장소 부재(실측)로 포함 불가** — 이것이 n=4의 근본 원인.

**결과 (2024 대응, 대회식, 3seed)**:
| 방식 | 부호정확 | Spearman(p) | 치명오탐 | 널초과 |
|---|---|---|---|---|
| V_A rolling | 0.750 | +0.800 (0.200) | 0.000 | ❌ |
| V_B 연도전이 | 0.750 | +0.400 (0.600) | 0.000 | ❌ |
| V_C 적대가중 | 0.750 | +0.400 (0.600) | 0.000 | ❌ |
| V_D 풍속정합 | 0.750 | +0.400 (0.600) | 0.000 | ❌ |
| V_E 다년풀 | 0.750 | +0.400 (0.600) | 0.000 | ❌ |
| 널(상수 −0.005) | **1.000** | — | — | — |

- **널(상수) 부호정확도 1.000**: 정답지 전부 음수 → "무조건 음수"가 100% 정확. 부호 정확도
  지표가 이 표본에서 정보 가치 0임을 널이 실증 (지시서 §3 경고).
- **널(셔플 1000회) Spearman 95% CI = [−1.0, +1.0]**: n=4에서는 셔플 널이 전 범위를 덮어
  통계적 검정 자체가 불가능.
- V_C 분류기 AUC **0.9973** (판별 피처: gfs_temp_700/850·air_density·stability — 온도/안정도
  계열이 2025를 구분). V_D 풍속 분위: 2025는 중간풍속(q25~q90)↑ 상단꼬리(q99)↓ — 팀원 문서와 정합.

**판정: 전원 불합격 → §5 경로 (검증 천장 실증)**:
- 5종 검증 방식이 실측 라벨로 역검정 불가능함(표본 n=4 + 셔플 널 전 범위)을 **정량적으로
  실증** — "후반 개선 정체의 구조적 원인" 2차 평가 최상위 소명 자산.
- **이후 신규 실험 전면 중단.** 남은 자원 2차 평가 산출물로 전량 이전.
- 8/13까지 `coordBA` 방어, 8/14 09:30 선택 상태 확인.
- V_A(rolling)가 ρ=+0.800로 최상 방향이나 p=0.20 — 방향성 단서일 뿐 합격 근거 불가
  (지시서 §7: 불합격 상태에서 "제일 나으니까" 채택 금지).

**산출물**: `work_a_package/retroharness/` — `build_truthset.py`·`harness_variants.py`·
`retro_eval.py`·`RETROHARNESS_report.md`·`retro_results.json`·`truthset.json`·`cands_2024.npz` ·
`PREREG_retroharness_20260809.md`(선등록).

### 4-41. ★ 4-프로브 계획 — coordBA 스케일 수술적 단일요인 프로브 (2026-08-09, 사용자 지시)

**근거**: RETROHARNESS(2024 검증 무효) + W2CHAIN(체인 중복) + g3tail 실측(−0.00049) →
"크고 광범위한 변경 반전, 작고 수술적인 변경만 전이" (coordBA +0.00063·M1 +0.00109 유일 성공).
선등록 `work_a_package/PREREG_plan_20260809.md` — 1일 1제출, 최대 4회, 방어선 coordBA.

| # | 제출일 | 후보 | 좌표 | md5 |
|---|---|---|---|---|
| **P1** | 8/10 | `submission_p1_g3band010.csv` | G3 CF[0.70,1.0]∩비JJA 955행 +210kW (coordBA 미러) | `6eb82f4b…` |
| **P2** | 8/11 | `submission_P2_maskprob.csv` | 마스크 확률 가중 재학습 → 동결 체인 | `2c99c2e4…` |
| **P3** | 8/12 | `submission_p3_g2_jja_noshift.csv` | G2 shift 여름(6~8월) 제거 2208행 (V3 신호 계절화) | `3c4d1d16…` |
| **P4** | 8/13 | (스태킹 규칙) | P1~P3 중 ΔLB≥+0.0003분 조합 (G1/G2/G3 그룹 분리 → 가법적) | — |

- **P1**: coordBA와 동일 메커니즘(+0.010 cap 상수, JJA 차단)을 G3에 — G3TAIL(λ 변형)과
  다른 부류. 만점불가 6.94%→5.23%, 0점확정 3.49%→2.03% (train 분포 기준).
- **P2**: METRICW W2(하드 마스크 0/1)의 전이 실패 원인 = 마스크 구조의 연도 차이 →
  **NWP 추정 P(y≥0.1C) 확률 가중**(연도전이 AUC 0.943~0.963 실증)으로 재학습. 2025 마스크
  확률 양극화(P<0.3 26~33% / P>0.85 51~57%)·월별(2월 0.91/9-10월 0.40~0.51) 확인.
  2024 홀드아웃 0.63155 (참고 — 하네스 무효라 LB만 판정).
- **P3**: W2NOSHIFT V3(01-08월 +0.00260) 신호를 계절 구조화 — coordBA JJA 게이트 아이디어를
  G2 shift(+0.07)에 적용. 겨울 shift 유지로 전구간 기각 원인 회피.
- 가드: P1/P2/P3 전부 L1(G1/G2/G3 무접촉)·L2(좌표 정합)·L3(NaN/음수/cap초과 0, ρ=1.0, sample
  정합) PASS · P1/P3 결정성(md5 재현) PASS.
- **판정 (선등록)**: ΔLB ≥+0.0010 채택 / +0.0003~0.0010 기록(스태킹 풀) / |Δ|<0.0003 폐기 /
  ≤−0.0010 영구기각. 제출은 사용자. 8/14 09:30 최종 선택 재확인.

**산출물**: `submission_p1_g3band010.csv` · `submission_P2_maskprob.csv` ·
`submission_p3_g2_jja_noshift.csv` · `work_a_package/P1_report.md`·`P3_P4_design.md`·
`PREREG_plan_20260809.md` · `work_a_package/metricw_p2/` (train_p2_maskprob.py·pred_P2_2024·
pred_test_P2_2025·run_p2.log).

### 4-42. ★ TASK-P2CHAIN — P2 마스크확률 가중의 체인 중복 검정 (2026-08-09, 사용자 지시, `opencode_TASK_P2CHAIN_20260809.md`)

**질문**: P2(마스크 확률 가중, W2의 완화판)가 W2처럼 동결 체인에서 이중 보정에 걸리는가?
→ **답: 걸림. Δ_체인후(P2) = −0.00242 (0/3), 중복률 122.3% → 제출 금지.**

**설계**: chain_frozen 재사용(체인 상수·CNN·블렌드 동결, 재적합 0회), W0↔W2↔P2 스왑,
2024 연도전이 홀드아웃·대회식·3seed. P2 3-seed 확보(train_p2_maskprob.py --seed 파라미터화).

**결과 (3seed 평균)**: chain(W0)=0.63402·chain(W2)=0.62524·**chain(P2)=0.63161**.
Δ_체인후(P2) = **−0.00242** (seed별 −0.00153/−0.00220/−0.00353, **0/3 양수**).
Δ_체인전(P2) = **+0.01086** → **중복률 122.3%** (W2 참조 149.7%). 사전검증(W0/W2 기준선 재현) PASS.

**단계별 trace (W2와 역학 상이 — 핵심 발견)**:
| 단계 | P2 | W2 |
|---|---|---|
| blend | +0.00989 | +0.01725 |
| iso | +0.00810 | +0.01095 |
| **shift** | **+0.00077 (반전 없음)** | −0.00671 (반전) |
| **M1** | **−0.00171 (여기서 반전)** | −0.00931 |
| coordBA | −0.00153 | −0.00916 |

- W2는 shift 단계에서 반전(shift 캘리와의 이중 보정) → **P2는 shift에서 반전 없음**
  (마스크 확률 가중이 하드 마스크보다 shift +0.07과 충돌 적음) → **그러나 M1 밴드 단계에서
  반전**되어 체인 후 음수. W2와 메커니즘은 다르나 결론은 동일.

**그룹별 분해 (1/6 희석)**: G1 +0.00152 (개선 유지) / **G2 −0.00033 (여전히 역전 핵심,
ΔFICR −0.00665)** / G3 +0.00037. 캘리 창(09-10)·M1(11-12) × 2024 겹침 in-sample 고지.

**판정: 제출 금지 (Δ≤0, 0/3). P2 계열 종료, 2차 평가 자산으로만 보존.**
1회 측정으로 종결 (재시도·체인 변형 재측정 금지).
**★ 지시서 §6에 따라 P1(g3band010)·P3(g2_jja_noshift) 사용자 판단으로 폐기** — 제출 준비
금지 (P1=g3tail 재시도 부류, P3=E1/C3 계절상수 사망 패턴). 4-프로브 계획은 P2 하나로
사실상 종료.

**산출물**: `work_a_package/p2chain/` — `run_p2chain.py`·`P2CHAIN_report.md`·
`p2chain_results.json` · 루트 `work_a_package/submission_P2_chain_frozen.csv`(제출 금지,
md5 `ac1936ed…`) · `PREREG_p2chain_20260809.md` · `metricw_p2/pred_P2_2024_seed{42,1337,2024}`
·`pred_test_P2_2025_seed{42,1337,2024}`.

## 10. 파일/스크립트 인벤토리

| 파일/폴더 | 역할 |
|---|---|
| train_lgbm_only.py | v1 승리레시피(팀원) |
| train_msecalib_ensemble*.py | ①②GBDT 앙상블 계열, teamfeat/v3~v8b 변형 |
| train_quant_member.py | FICR 전용 quantile 멤버 |
| reblend_*.py / build_v3b~v9.py | 각 버전 재블렌드/빌드 스크립트 |
| rolling_eval.py | ★전계절 rolling forward CV 채점기(12창) |
| calib_sweep*.py / calib_p0p1p2.py | 캘리 스윕(형태선택/pooled 재판정) |
| gefs_pipeline/ | GEFS 수집·피처·2단계 판정(종결, 데이터 보존) |
| scada_correction/ · scada_cleanmask_v2/ | SCADA 잔차보정(기각) · 클린마스크 v2(기각) |
| value_loss/ | value-oriented FICR 손실(v8b, LB 반전 기각) |
| deepseq_pipeline/ | 딥시계열 v9(게이트2 실패 기각), CNN 학습용 `.venv`(torch cuda) |
| weight_interaction/ · group_joint_model/ · icing_features/ | 상호작용항·그룹joint·착빙피처(전부 기각) |
| **cnn_gate/** | CNN 게이트①②③, `cnn_common.py`(공용, TASK14 결정성 패치 반영), TASK2/3 결과 |
| **task4_wake_terrain/** | TASK4-A(후류/지형, 기각) · TASK4-B(ts편향, 측정불가) |
| **task5c_repro/** | CNN 학습/추론 완전분리 재현(`train_cnn_production.py`+`infer_cnn_production.py`) |
| **task7_ficr_recal/** | TASK7/7b/8(블렌드후 FICR재캘리, G1/G2 채택·G3 제외) |
| **task9_turbine/** · **task9b_residual/** | TASK9-A(터빈별, 기각) · TASK9-B(잔차학습, 기각) |
| **task10_pointest/** · **task11_screening/** · **task12_full_grid/** | 점추정변경·S1스크리닝·전체격자(전부 기각, TASK12는 스택가치 정량화) |
| **task14_determinism/** | CNN 결정성 복원(성공), `cnn_common_backup_pre_task14.py`(수정전 원본) |
| **task15_realization/** | CNN 실현체 로또 검정(영구종결) |
| **task16_wind_residual/** | 회전DEM 방향 재검토→풍향잔차구조 진단(약하게참, 빌드보류) |
| **task18_calib_shift/** · **task20_shift_cap/** | 캘리 shift 축소(기각)·상한확장(freeze게이트 기각) — calibrate_total 동결 확정 |
| **task22_intra_var/** | 시간내변동성/젠센보정(STEP0 기각) |
| a_prime_probe/ | A'(멀티NWP) 스코프 축소 이력, TASK23으로 최종 종료 |
| `jtrack_probe/` · `Jtrack_J0_recon.md` | J-트랙 J0(JMA GSM 커버리지·리키지·변수 실사) — 허브밴드 기압면 구조적 획득불가 확인 |
| `jtrack_JC_surface_pressure/` · `Jtrack_JC_surface_pressure.md` | J-트랙 JC(선등록 S1 스크린, JMA surface_pressure) — F1/F2 둘 다 기각, J-트랙 영구종결 |
| `외부데이터_정리.md` | ★외부데이터 규정·leakage 논증(ECMWF/ASOS/GEFS/사전학습FM/TASK23 NWP실사 전부 기록) |
| `REPRODUCIBILITY_STATEMENT.md` · `ENDGAME2_package_manifest.md` | 2차 소명 문서(CNN 비결정성 소명, 산출물 매니페스트) |
| `인수인계서.md`(v2) | 새 세션 인수인계, 2026-07-25 엔드게임 상태 반영(TASK29~32는 본 문서가 최신) |
| **task29_power_rescreen/** | TASK29 R0(검정력교정, base간 부호역전 발견)·R1(3-arm×3-base, ③판정) — 배깅축 영구종결 |
| **task30_spatial_repr/** | TASK30 공간표현 재설계(`cnn_spatial_v2.py` GAP제거+위치조건부풀링, `position_weights.py` 터빈좌표 커널) — Gate-W1 FAIL, 표현축 영구종결 |
| **task31_target_quality/** | TASK31 X-1 타깃품질(clean_1/2/3 원인) 진단 — 진행 중, 판정 없음 |
| **ecmwf_pipeline/**(신규 재사용) | TASK32 ECMWF 3차 재시도 — 기존 수집물(`fetch_ecmwf.py`·`ecmwf_train/test.parquet`·`leakage_justification.md`) 재사용, G1/G2 게이트 신규 설계 |
| `발표_골격_v1.md` | 2차 발표 5장 골격(TASK28-P2) — 2026-07-28 서사정정 반영(1장/5장/부록후보) |

제출본 계보: submission_lgbm_ens.csv → ens_v2(0.6446) → ficr_w1/w2(0.6453/0.64528) → ficr_w1_v3b
(0.64687) → v4(0.64666,기각) → v5(0.64717) → v7(0.64926) → v7a(0.64996,플랫) → v8b(0.64837,기각) →
**팀원 ficr_w1_v7_cnn(0.65183) = LB 0.65184**. A=v7cnn_fixed_v2(0.6490737)·B=task8_g1g2ficr
(0.6482289)·C=v7cnn_fixed_v2_g2shift(0.6491906) 4건 실측 완료 → **S3 규칙③으로 원본 최종 재확정
(4-34)**.

## 11. 서버 운영 노트

- 접속: cs.dongguk.edu(port 102, user student), 작업폴더 `~/DACON_baram2026`, GPU=RTX A6000 49GB.
- GPU 지정: 초기 GPU6 → v9부터 `CUDA_VISIBLE_DEVICES=4`(2026-07-22 변경). **CNN 학습은
  `deepseq_pipeline/.venv`(Python 3.11, torch==2.5.1+cu121, CUDA 12.1) 전용 환경 사용.**
- 실행: `CUDA_VISIBLE_DEVICES=4 nohup python3 -u <script> > log 2>&1 &` + `tail -f log`. tmux는
  cgroup 에러 → nohup 사용. 로그는 `python3 -u`로 unbuffered.
- 크래시 복구: nohup 잡은 SSH 세션 종료와 무관하게 계속 실행 — 재접속 후 `tail -80 <로그>` +
  `ps aux|grep python`으로 상태 확인, 결과는 명명된 파일(csv/log)로 저장(stdout만 의존 금지).
- 모든 CSV UTF-8, 제출은 `sample_submission.csv` 포맷.
- **★ 신규: 한글 파일명 인코딩 주의**. 일부 루트 .md 파일(인수인계서.md 등)이 macOS 방식 NFD(유니코드
  분해형) 파일명으로 존재해 표준 도구(Read/grep/cat)가 "파일 없음"으로 실패하는 사례 발생(2026-07-25
  발견·전체 정규화 완료). 향후 한글 파일명 생성 시 `unicodedata.normalize('NFC', name)`으로 저장할 것.
- **herbie-data 등 신규 패키지는 반드시 별도 venv**(프로덕션 numpy1.26/pandas1.5 스택 파손 실사고 1회,
  `gefs_pipeline/.venv` 참조).
