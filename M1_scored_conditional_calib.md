# M1. 메트릭-구조 레버: 채점조건부 캘리브레이션 — 결과

작성: 2026-07-22 (클로드코드 실행) · 판정: **기각**

## 가설
채점은 y≥0.1·C_g인 시간만 카운트 → 최적예측은 E[y|x]가 아니라 E[y|x,y≥0.1·C_g]. 경계 근방(0.05~0.20·C_g)
예측에 조건부-기대값 방향 보정을 적용하면 메트릭 구조 자체의 성질이라 2025 이동에 강건할 것이라는 가설.

## 구현
- 베이스: v7 진짜 레시피(clean-downweight 0.3 + subsample_freq=1, LGB+XGB+Cat+LGB-L1)로 3-seed(42/123/2024)
  12개월 rolling cal/eval 원시예측을 새로 생성(`m1_gen_base_preds.py` → `m1_base_preds_s{seed}_2024-{MM}.parquet`).
  기존 `preds_team1_nf0_ens1_s42_2024-*.parquet`는 버그수정 전 구버전 레시피라 폐기.
  seed42 base pooled = **0.63477** 재현 확인(canonical과 일치, 5자리까지 정확).
- 캘리: canonical `calibrate_total`(3그룹 전부 iso+shift) — build_v9.py 등 프로덕션 최종블렌드용
  `FORCE_FORMS`(G1/G2=shift_only)와는 다른 함수. 이 세션에서 실제로 섞어써서 발생한 버그(v9 1차 판정)를
  교훈삼아 처음부터 올바른 함수 사용.
- 보정: 캘리된 cal 예측이 [0.05,0.20]·cap 밴드에 들 때만, 그 구간 TOTAL을 최대화하는 상수 shift를
  그리드서치(±0.10·cap, 0.01 간격)로 cal에서 적합 → eval에 적용. base 모델·기존 캘리는 전혀 재학습/변경
  안 함(하단 base-불변 assert 참조).

## 게이트별 결과

| # | 게이트 | 기준 | 실측 | 판정 |
|---|---|---|---|---|
| 1 | 3-seed rolling pooled diff | ≥ +0.0005 | **+0.00139** (seed42 +0.00122, 123 +0.00116, 2024 +0.00180, 3/3 양수) | **통과** |
| 2 | 연도전이 홀드아웃(2022-23 학습→2024) diff | ≥ +0.0004 | **+0.00267** (7/11 양수) | **통과** |
| 3 | D2 스트레스 게이트셋(`stress_2025like_2024.parquet`, n=1756) diff | ≥ +0.0003 | **-0.0000078** (사실상 0) | **기각** |
| 4 | 널테스트 | 셀프블렌드 diff≈0 ∧ 시간셔플 이득소멸 | 셀프블렌드 diff=+0.00122(항등, 정상) / 시간셔플 diff=**-0.00528**(이득 소멸+역전) | 통과 |
| 5 | base 불변 assert | base 예측·캘리 미변경 | seed42 base pooled=0.63477(canonical과 소수 5자리까지 일치), 보정은 별도 후처리 레이어로만 적용, base 모델 재학습 없음 | 통과 |

## 최종 판정: **기각** (게이트3 미달, "하나라도 미달 → 영구 기각" 규칙)

게이트1·2·4·5는 전부 여유있게 통과했으나(특히 게이트2는 기준의 6.7배), **D2 스트레스 게이트셋(2025-유사
2024 시간대)에서는 이득이 완전히 소멸**(diff ≈ -0.0000078, 노이즈 수준). 일반 2024 rolling과 연도전이
홀드아웃 둘 다에서 신호가 살아있었지만, "실제로 2025를 가장 닮은" 부분집합에서는 통하지 않는다는 뜻 —
이 실험이 정확히 스트레스 게이트셋을 만든 이유(일반적 검증이 놓치는 실패를 잡아내는 것)를 입증한 사례.

## 기준 외 관찰 (참고용, 판정에 미사용)
- 밴드 shift 크기가 그리드 경계(±0.10·cap)에 가깝게 자주 붙음 — 밴드 내 표본수(월/그룹당 ~120~530)가
  작아 과적합 여지가 있음을 시사. 그리드를 넓히면 게이트1/2 숫자가 더 커질 수 있으나, 게이트3(스트레스셋)
  결과가 이미 기각을 결정지어 추가 튜닝 의미 없음(사후 튜닝 금지 규율과도 부합).
- 밴드 내 mean(pred)와 mean(y)가 거의 동일하게 나타난 최초 진단은 isotonic 회귀가 cal 데이터에 in-sample로
  평가되어 과적합된 아티팩트(사실상 정답을 암기)로, 조건부 기대값 방향성 자체를 판단하는 데는 부적절한
  진단이었음 — 게이트 판정(모두 held-out eval 기준)에는 영향 없음.

## 다음 액션
게이트3 실패로 영구 기각, 재시도 없음. LB 프로브 요청 불필요.
