# D1 — ECMWF planB 전이 실패 부검

작성: 2026-07-22 (클로드코드 실행) · 판정 기준은 fable_실행지침_진단트랙.md에 선등록, 결과 확인 후 변경 없음.

## 판정: 주 원인 = **(a) 커버리지/NaN 희석**

### 근거 1 — 커버리지 (a), 가장 깨끗한 신호
- `cache_train_ecmwf.parquet`: has_ecmwf=1 비율 2022-01~2024-02 **0%**, 2024-03 77.0%, 2024-04+ 100%.
- `cache_test_ecmwf.parquet`(2025 전체): has_ecmwf=1 비율 **전 구간 100%**.
- planB(B안)의 실제 학습창(train_start=2024-01-01) 안에서도 has_ecmwf=0인 행이 **1,611/8,784 = 18.3%**
  (2024-01~02 전체 + 2024-03 일부가 median으로 채워짐) 존재 — **train은 18.3% 결측, test는 0% 결측**이라는
  명확한 비대칭. 로컬 검증(rolling)이 이 결측 패턴을 그대로 반복 학습해 "결측일 때의 median 채움" 자체를
  일부 신호로 흡수했을 가능성이 있고, test(2025)에는 이 결측 패턴이 전혀 없어 그 신호가 전이되지 않음.

### 근거 2 — 레짐 편중 (c), 방향은 부합하나 계절 교란 있음
- 페어드 rolling 예측(`preds_*_ecmwf_ts2024-01-01_2024-{07..12}.parquet`, 6개월분만 존재 — 01~06월 파일 없음, 재현 한계로 명시)로
  캘리브레이션 후 풍속 4분위 분해:

  | regime(풍속 분위) | n | base TOTAL | ecmwf TOTAL | gain | gain 비중 |
  |---|---|---|---|---|---|
  | 0(최저) | 1103 | 0.6558 | 0.6745 | +0.0186 | 61.6% |
  | 1 | 1102 | 0.6448 | 0.6553 | +0.0105 | 34.7% |
  | 2 | 1102 | 0.6126 | 0.6160 | +0.0034 | 11.2% |
  | 3(최고) | 1103 | 0.6491 | 0.6469 | -0.0023 | -7.4% |

  상위 2개 레짐(0+1)이 **96.3%** — 기준 70%를 크게 상회. 2025 전체 데이터에서 같은 절대풍속 컷을
  적용하면 regime0+1 비중이 36.7%(2024 6개월 eval의 절대적 균등 25%+25%=50% 대비 하락) —
  방향은 (c)에 부합.
  **단, 이 비교는 2024 6개월(7~12월, 겨울 포함) 분위 대 2025 전체 12개월 분위라 계절 구성이 달라
  교란 가능성이 큼 — 순수 "레짐 자체의 희소화"인지 "계절 구성 차이"인지 이 데이터만으로는 분리 불가.**

### 근거 3 — 분포 이동 (b), 통계적으로 유의하나 효과크기 작음
- `ecmwf_gap_ldaps`(ECMWF-LDAPS 풍속 괴리) 2024(has_ecmwf=1, 2024-03~12) vs 2025(전체):
  KS stat=0.069, p<1e-16(표본이 커서 유의하나) — 중앙값 0.71→1.01(약 0.3m/s), std 거의 동일(1.88 vs 1.89).
  **통계적으로 유의하지만 효과크기는 작음.** 이 역시 train기간이 3~12월(겨울 일부 결측)인 반면
  test는 전체 12개월이라 계절 교란 가능.

### 근거 4 — staleness/통합버그 (d), 증거 없음(데이터 공백)
- `fetch_ecmwf.py`는 Open-Meteo의 `_previous_day2` 파생필드를 그대로 받아쓰며, 실제 발표시각을
  별도로 기록하지 않음 — **발표시각 직접 감사 로그 없음(데이터 공백, 있다/없다 증거 아님)**.
- `build_ecmwf_merge.py`에는 `has_ecmwf`만 fillna(0), 실제 피처값에 ffill/bfill 등 stale-fill 로직 없음 →
  결측을 과거값으로 채워 넣는 방식의 버그는 **코드상 발견되지 않음**. (d)를 적극 지지하는 근거 없음.

### 근거 5 — 피처중요도 × 이동크기 (b 확인용), 약한 상관
- ecmwf_* 11개 피처의 LGB importance와 KS stat 간 상관 = **0.237**(약한 양의 상관, 강한 확증 아님).
  가장 중요한 피처(`ecmwf_t2m_c`, 기온)가 KS stat도 상대적으로 큼(0.158)이 유일하게 시사적인 점.

## 기준 외 관찰 (참고용, 판정에 미사용)
- regime 3(최고풍속)에서는 오히려 ECMWF가 base보다 **나쁨**(-0.0023) — 가장 안전해야 할 구간에서 이미 손해.
- 재현에 쓴 페어드 예측 파일이 7~12월만 존재해 근거2/3의 "2024 대표성"이 반쪽짜리임 — 향후 1~6월분도
  재생성해 재검증 권고(단, 이번 판정을 바꾸진 않음).

## 결론
5개 근거 중 **(a) 커버리지/NaN 희석이 가장 깨끗하고 계절 교란에서 자유로운 유일한 신호**(18.3% vs 0%,
정량적·명확). (c)/(b)는 방향은 부합하나 계절 구성 차이로 교란돼 확정적 근거로 쓰기 어려움. (d)는
근거 없음. **주 원인 = (a).**

**A' 착수 권고: GO.** (a)/(b)/(d) 중 하나면 착수 규칙 충족. A'의 설계(커버리지 공백 시 NaN 대신
LDAPS/GFS-only 컨센서스 폴백 + 폴백 지시자)가 이번에 발견된 (a) 문제를 구조적으로 우회하도록
이미 설계돼 있어 정합적.
