[선등록] TASK-METRICW — 평가지표 정합 표본가중 재학습

가설: 평가식은 actual≥0.10cap 행에서만 계산되고 FICR은 실측 발전량으로 가중되나,
      표준 회귀 학습은 전체 분포(마스크 밖 39~46%)의 조건부 중앙값을 추정 → 점수가
      걸린 부분집합 위에서 구조적 하향편향(마스크 기준 −0.18~0.21cap, FICR 가중 −0.32~0.36cap).
      학습 표본가중을 w_i = 1(y_i≥0.1C)·(0.5/n + 0.5·y_i/Σy) 로 정합시키면 제거된다.
      본 레버는 연도 적합 상수가 0개 — 8연속 실패(상수 이동)와 구조적으로 다름.

변형 3종 (그 이상 탐색 금지):
      W0 = 전 행 균등 (컨트롤)
      W1 = 마스크만: 1(y≥0.1C)
      W2 = §0-2 정식: 마스크 + 0.5 균등 / 0.5 발전량
      W2-shuffle = W2 가중을 행 셔플 (널대조)

고정: 피처·하이퍼파라미터·시드 일절 변경 금지 (v7 GBDT 4-앙상블, seed 42/1337/2024)
      파이프라인: 2022-23 학습 → 2024 전체 예측 (연도전이 홀드아웃)
      평가: 대회 평가식 원본 (competition_metric — 10% 마스크 + 6%/8% 밴드 + 발전량가중)

판정 문턱 (3-seed 평균 ΔTOTAL vs W0, 연도전이 홀드아웃):
      ΔTOTAL ≥ +0.0010 ∧ 2/3 seed 양수 → 통과 (W2가 W0 대비)
      ΔTOTAL < +0.0010 또는 양수 seed 1/3 이하 → 무판정/기각
      W2 이득이 W2-shuffle(널) 평균을 유의하게 초과해야 유효 (초과 못 하면 무판정)

성공 신호 (1차): 스코어가 아니라 CF 질량비 분포 결손이 메워지는가
      G3 [0.85,0.95) 0.06 → ↑ / G3 [0.95,1.10) 0.00 → ↑
      G1 [0.95,1.10) 0.26 → ↑ / G2 [0.95,1.10) 0.24 → ↑ / [0.10,0.30) 1.35~1.45 → ↓

금지: CNN 재학습 · 피처/전처리 변경 · pipeline_v2 계보 · W0/W1/W2 외 가중 탐색 ·
      2차 튜닝 · 무단 LB 제출 (프로브 예산은 G3TAIL이 우선)

판정: METRICW_report.md + 연도전이 게이트 + 질량비 결과로 헤드/사용자 판단. 자동 제출 금지.
