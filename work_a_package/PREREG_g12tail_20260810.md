[선등록] TASK-G12TAIL
가설: 그룹별 효과의 정확한 가법성(ΔTOTAL = (1/6)Σ_g(ΔFICR_g − ΔNMAE_g))에 따라,
      G3 단독으로는 필요량 +0.00518을 채울 수 없다(1/6 희석).
      G2·G1의 CF≥0.80 상단 질량 결손을 동일 단조 변환으로 보완해 누적한다.
프로브 순서: P2 = P1승자 + G2(a0.80,λ1.20) → P3 = P2승자 + G1(a0.80,λ1.20)
            → P4 = 양(+)판정 그룹만 λ1.35 승급
판정 (희석 계수 1/6 반영):
      ΔLB ≥ +0.0010          → 채택, 다음 그룹 진행
      +0.0003 ≤ ΔLB < 0.0010 → 방향 채택, 해당 그룹 λ1.35 승급 후보로 등록
      |ΔLB| < 0.0003         → 무판정 (1129~1193행 × 1/6 희석으로 묻힘)
      ΔLB ≤ −0.0010          → 해당 그룹 영구기각, 다음 그룹으로 이동
      ※ 중간 밴드(+0.0003~+0.0010)를 무판정 폐기하지 않는 근거는 위 희석 수식이다.
         이는 사후 문턱 완화가 아니라 선등록 시점의 지표구조 반영이다.
예산: P2~P4 최대 4회. 하루 2회 상한(2회차는 1회차 LB 확인 후 발행). 동시 제출 금지.
방어선: submission_A_m1v2_coordBA.csv (0.65465) 상시 복귀 가능.
마감: 8/13까지 최종 확정. 8/14 09:30 선택 상태 재확인.
