[선등록] TASK-W2CHAIN
가설: METRICW W2의 체인 전 이득(+0.01768)은 기존 iso/shift 캘리와 중복될 수 있다.
      동결 체인 통과 후의 순증 Δ_체인후만이 LB를 예측한다.
설계: chain(X)=coordBA(M1(shift(iso(blend(GBDT=X, CNN=동결))))) 에서 X만 W0↔W1↔W2 스왑.
      체인 상수·블렌드 비율·CNN 전부 동결. 재적합 0회.
측정: 2024 연도전이 홀드아웃(학습 2022-23), 대회 평가식 원본, 3seed 평균.
판정: ≥+0.008 즉시제출 / +0.003~+0.008 제출 / 0~+0.003 보류 / <0 제출금지.
      3seed 중 2/3 미만 양수 시 한 단계 강등. 1회 측정으로 종결, 재측정 금지.
사전검증: chain_frozen(W0) == run.py 원본 산출 md5 일치. 불일치 시 실험 중단.
