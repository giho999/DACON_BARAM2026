# J-트랙 J0. 커버리지·리키지·변수 실사 — 결과

작성: 2026-07-25 (클로드코드 실행) · 판정: **조건부 GO — 단, 원안(허브밴드 기압면 브래킷) 전제가
API 구조적 한계로 성립 불가함을 확인. 지표10m-only로 축소된 안이 아니면 J1 착수 보류, 전략 리드
확인 필요(아래 판정 근거 참조).**

## 1. 사이트 좌표·허브 ASL 고도
`task4_wake_terrain/turbine_geometry.py`(17기 평균) 재사용: **37.2819°N, 128.9624°E**. 지표 ASL
고도는 LDAPS 16격자 지형고도(`surface_0_h`) 사이트 평균 **≈944m**(범위 869~1001m, 능선 지형이라
격자별 편차 큼). 허브고도(AGL 117m) 가산 시 허브 ASL **≈1061m**(±60m 격자편차). Open-Meteo가
반환한 해당 좌표의 모델 격자고도는 **1074m**(GSM 0.5° 격자, 우리 LDAPS 세분 추정과 오차 13m로 근접
— 사이트가 고지대라는 물리적 특성을 GSM 격자도 대체로 포착).

## 2. 커버리지 실측 (2022-01·2023-07·2024-06·2025-06, 각 1주)
| 모델 | 2022-01 | 2023-07 | 2024-06 | 2025-06 |
|---|---|---|---|---|
| **JMA GSM** | **100%** | **100%** | **100%** | **100%** |
| JMA MSM | **0%**(미가동) | 100% | 100% | 100% |

**GSM은 2022-2025 전구간 완전 커버 — ECMWF/ICON류의 부분공백 함정이 재현되지 않음.** MSM은 2022년
상반기 미가동, 2022-06~09 사이 어딘가에서 개시(추가 이분탐색으로 2022-06=0%, 2022-09=100% 확인,
정밀 개시일은 이 범위 내). **판단: MSM은 부분공백이 있어 그 자체로 ECMWF 함정 재현 위험 — primary로
채택하지 않고, GSM 단독을 primary로 확정.**(MSM을 secondary로도 쓰지 않기로 — 아래 4번과 결합해
가치가 낮아짐)

## 3. 리드타임 정렬 (오프셋 선택)
Open-Meteo Previous Runs API는 런 발표시각을 직접 노출하지 않고 "밸리드타임 기준 N일 전 예측값"
(previous_dayN, 상대 리드타임)만 제공 — 그래서 원문(JMA 공식 배포시각)에서 역산.

**원문 확인**(JMA 気象庁, "配信資料に関する技術情報": 全球モデル GPV는 00/06/12/18 UTC 4회 실행,
**전송완료 = 실행시각+4시간15분**).

계산: 예측기준시점 = D-1일 13:00 KST = D-1일 04:00 UTC. 안전조건: 실행시각+4h15m ≤ D-1 04:00 UTC
→ 실행시각 ≤ D-2일 23:45 UTC → 가용 실행시각 중 가장 늦은 것 = **D-2일 18:00 UTC 런**.
이 런에서 대상일 D의 24개 타깃시각(D-1 15:00 UTC ~ D 14:00 UTC, KST 00~23시)까지의 리드타임 범위
= **21시간(D 00시 KST) ~ 44시간(D 23시 KST)**.

previous_day1(≈24h 리드)은 이 범위의 하한(21h)에 근접해 늦은 시각대(리드 44h 필요)에는 **리키지
위험**(더 최신 런을 참조할 수 있음). **previous_day2(≈48h 리드)는 요구범위(21~44h) 전체를 항상
초과 충족 — 채택.** 실측 검증: 동일 시각의 previous_day1 vs previous_day2 값이 98.6% 서로 다름
(1.4%만 우연 일치) — 오프셋 메커니즘이 실제로 다른 런을 가리킴을 확인(no-op 아님).

**채택 오프셋: previous_day2, 전 변수 공통.**

## 4. ★핵심 발견 — 허브밴드 기압면/고도면 풍속은 리키지-안전 경로로 획득 불가 (구조적 API 한계)
- 기압면(hPa) 변수(예: `wind_speed_925hPa`)는 **`_previous_dayN` 접미사 자체를 API가 거부**
  (`SurfacePressureAndHeightVariable<VariableAndPreviousDay,...>` 타입 초기화 실패, GSM·MSM·900~
  1000hPa 등 여러 조합에서 재현) — 구문 오류가 아니라 Previous Runs API의 타입 설계 자체가 리드타임
  오프셋을 지표면 변수 세트에만 허용하고 기압면 변수에는 제공하지 않음(공식 문서 미고지, 실측으로 확인).
- 고도면(80/120/180m) 변수도 동일하게 파라미터명은 수리되나(200 OK) **GSM·MSM 전 기간(2022~2025)
  전부 100% 결측** — 애초에 이 두 JMA 모델에는 이 고도면 자체가 산출되지 않는 것으로 판단(Open-Meteo
  공용 문서의 "80/120/180m" 항목은 다른 모델(ICON 등)용 항목이 JMA 페이지에 잘못 병기된 것으로 추정).
- **리키지-안전하게 얻을 수 있는 건 지표 세트뿐**: `wind_speed_10m`/`wind_direction_10m`(100% 유효)·
  `temperature_2m`(100%)·`surface_pressure`(100%). `wind_gusts_10m`은 GSM에서 0% 결측(미제공).

이는 지시문 4번 "지표 10m만 유효하면 전단외삽 품질저하 명시(감점요인)"이 상정한 상황보다 **구조적으로
더 근본적**이다 — "10m 데이터가 결측이 많아 화질이 낮다"가 아니라 **"허브밴드 기압면 자체가 이 API의
리키지-안전 경로에 애초에 존재하지 않는다"**는 것. 전단외삽의 입력이 지표 1개 층뿐이라 v³ 스케일에서
품질 저하가 클 것으로 예상됨(우리 base가 이미 80/100/850hPa 다층 전단을 갖고 있는 것과 대조적).

## 5. GSM/MSM 결정
**GSM 단독 채택(primary), MSM 제외**(2022년 부분공백 + 어차피 원안의 기압면 이점이 없어 5km 고해상도의
상대적 가치도 낮음 — 커버리지 리스크만 지고 얻는 게 적음).

## 판정
### ① 선등록 기준 대비 판정
- 커버리지(2022-2025 전구간, 사이트 격자 포함): **GSM 통과**(100%), MSM 부분실패 → MSM 제외로 대응.
- 리드타임 정렬(24개 타깃시각 전부 안전): **통과**(previous_day2, 원문 배포시각 기반 계산 + 실측
  검증 병행).
- 허브밴드 기압면 풍속 결측없음: **실패**(결측 문제가 아니라 API가 리키지-안전 경로 자체를 미제공).
- **종합: 코드화된 abort 조건("공백월 있으면 사실상 사용 불가") 자체는 트리거되지 않음(GSM 완전
  커버, 정렬 가능) → 기계적으로는 GO.** 그러나 J-트랙의 핵심 가치제안("허브밴드 기압면을 갖는 독립
  제3센터")의 절반(기압면)이 성립하지 않아, 원안 그대로의 J1 진행은 부적절.

### ② 기준 외 관찰
- GSM의 격자고도(1074m)가 우리 LDAPS 세분 추정(≈944~1061m)과 근접해, 0.5° 저해상도에도 이 산지
  지형을 대체로 인지하고 있음은 긍정적 신호(지표풍이라도 지형효과 일부 반영 기대).
- MSM(5km, 한국 커버)은 커버리지만 됐다면 원안 대비 훨씬 유망한 후보였을 것(고해상도) — 이번엔
  기각이지만, MSM의 정확한 개시일(2022-06~09 사이)과 기압면 제공여부(동일하게 안될 것으로 추정되나
  미확인)는 향후 재검토 시 참고.
- surface_pressure_previous_day2가 100% 유효 — 해면기압류 종관 정보(TASK24에서 신규성 부분R² 유일
  통과했던 500hPa gh와 유사한 "종관 규모" 정보 계열)로 대체 활용 가능성 있음(참고, 미검증).

### ③ 다음 액션 권고
**J1을 원안(기압면 브래킷) 그대로 착수하지 말 것.** 대신 두 선택지 중 전략 리드 판단 필요:
(A) **축소 스코프**로 진행 — GSM 지표 세트(10m 풍속/풍향, 2m기온, 지면기압)만으로 sister-forecast
멤버 구성(전단외삽 품질저하 감수, J2/J3에서 그 저하가 실제 게이트 통과를 막는지로 판별), 또는
(B) 원안 가치제안(기압면 독립정보)이 이 경로로는 원천 불가하므로 **J-트랙 자체를 여기서 종료**.
클로드코드 판단으로는 (A)든 (B)든 J1(전구간 수집, 비용 발생)은 이 스코프 변경을 전략 리드가 확인한
뒤 시작하는 것이 안전 — 원안과 다른 산출물에 수집 비용을 쓰는 결정이라 임의 진행하지 않음.
