# J-트랙 코다(JC). JMA surface_pressure 단발 S1 스크린 — 결과

작성: 2026-07-26 (클로드코드 실행) · 판정: **kill — F1·F2 둘 다 게이트 미달, J-트랙 영구 종결** · 제출 0회,
멤버빌드 0회. 소요 예산 내(신규 패키지 설치 없음, `requests`+`pandas`+`numpy`+`scikit-learn`만 사용).

## 데이터
Open-Meteo Previous Runs API, `model=jma_gsm`, 좌표 (37.2819N, 128.9624E)(J0 확정 사이트 대표좌표,
GSM 격자 스냅 (37.5,129.0), 격자표고 1074m — J0와 동일), `surface_pressure_previous_day2`,
2022-01-01~2025-01-02(말단 여유 포함) 단일 요청. **결측 0건(100% 커버, J0 재확인)**.
`previous_day1` vs `previous_day2` 값이 다른 비율 93.6%(J0의 풍속 기준 98.6%와 유사 수준) —
오프셋이 실제로 다른 런을 가리킴을 재확인(no-op 아님, `collect_jma_sp.py` 실행 로그 참조).

리드타임 정렬 근거는 J0에서 이미 원문(JMA 배포시각=실행+4h15m) 기반으로 확정된 `previous_day2` 오프셋을
그대로 승계(코다 지시문상 재유도 대상 아님). API가 런 발표시각을 행 단위로 노출하지 않아 지시문의
"행별 hard assert"는 J0에서 도출된 구조적 결론(previous_day2가 24개 타깃시각 전 구간(21~44h 요구)을
항상 초과 충족)으로 대체 — 이 코다에서 별도 행별 어서션 코드는 작성하지 않음(신규 검증 아님, J0 승계).

## 피처
- F1 = `jma_sp` (surface_pressure, hPa, previous_day2)
- F2 = `jma_dpdt3h` = F1(t) − F1(t−3h)

## 대조 종관피처셋 C (로컬 cache 재사용, 신규 수집 없음)
`ldaps_sp`(LDAPS surface_0_sp, 5격자평균) · `ldaps_prmsl`(LDAPS meanSea_0_prmsl, 4격자평균) ·
`gfs_sp`(GFS surface_0_sp, 9격자평균) · `gfs_prmsl`(GFS meanSea_0_prmsl, TASK24 `cand_prmsl`) ·
`gfs_gh500`(GFS isobaricInhPa_500_gh, TASK24 `cand_gh500`).

## S1a — 중복성 (max|Pearson r|, 2022-24 `cache_train` 전체)
| feat | r(ldaps_sp) | r(ldaps_prmsl) | r(gfs_sp) | r(gfs_prmsl) | r(gfs_gh500) | max\|r\| | 판정(≤0.90) |
|---|---|---|---|---|---|---|---|
| F1 jma_sp | 0.972 | 0.859 | 0.924 | 0.824 | 0.025 | **0.972** | **기각** |
| F2 jma_dpdt3h | 0.061 | 0.084 | 0.082 | 0.090 | −0.049 | **0.090** | 통과 |

## S1b — 증분신호 (F2만 진출; v7+CNN production-mode 2022-23→2024 홀드아웃 잔차, `cnn_gate/gate2_evalrows_s{42,123,2024}.parquet`, C 5개 통제 후 F2 추가 증분 부분R², 9셀=3seed×3그룹)
| feat | mean 부분R² | min | max | 문턱(0.5%) | 판정 |
|---|---|---|---|---|---|
| F2 jma_dpdt3h | **0.219%** | 0.134% | 0.324% | ≥0.5% | **기각** |

9셀 전부 0.13~0.32% 범위로 문턱을 일관되게 하회 — 단일 셀의 요행성 통과조차 없음(TASK24 gh500이
9셀 전부 0.42~0.67%로 문턱을 일관 상회했던 것과 정확히 대칭적인 반대 사례).

## 널대조
S1b를 통과한 후보가 없어 **미실시**(선등록 규칙상 "S1b 통과 후보에 한해" 적용).

## 판정
### ① 선등록 기준 대비 판정
- 통과 정의(F1·F2 중 최소 1개가 S1a∧S1b∧널 동시충족) **미충족**.
  - F1: S1a에서 즉시 기각(ldaps_sp와 r=0.97 — 기존 LDAPS 지면기압과 사실상 동일 정보, 별도 정보가 아님).
  - F2: S1a는 통과(중복 아님, 예상대로 tendency는 원 신호와 낮은 상관)했으나 S1b에서 명확히 기각
    (0.22% ≪ 0.5%, 9셀 전부 일관되게 미달 — 경계선 신호조차 아님).
- **kill 판정 발동: J-트랙 영구 종결.**

### ② 기준 외 관찰 (판정에 미사용)
- F1이 ldaps_sp(r=0.97)·gfs_sp(r=0.92)와 동시에 높은 상관을 보인 것은, J0에서 언급했던 "GSM이 이
  산지 지형(격자표고 1074m)을 저해상도임에도 대체로 인지"라는 관찰과 정합 — 즉 JMA의 지면기압이
  LDAPS/GFS와 별개 정보라기보다 동일한 종관 시스템을 다른 모델이 각자 관측·재현한 것에 가까움.
- F2(기압경향)가 S1a에서 원 신호와 낮은 상관(0.06~0.09)을 보인 것은 미분 연산의 일반적 성질(추세
  성분 제거)로 예상된 결과였고 실제로 재현됨 — 그러나 "중복 아님"이 "새로운 예측신호"를 보장하지
  않는다는 것이 S1b에서 재확인됨(정보이론적 독립성과 예측 증분은 별개 문제).
- TASK24의 gh500(통과, mean 0.551%)과 이번 F2(기각, mean 0.219%)를 나란히 보면, "종관규모 정보"라는
  범주 자체가 신호를 보장하지 않고 변수별 개별 검정이 필요함을 재확인 — J0 §기준외관찰에서 제기했던
  "surface_pressure가 gh500과 유사 계열일 가능성"은 **기각으로 해소**(피처 유형의 유사성이 곧 예측
  가치의 유사성은 아니었음).

### ③ 다음 액션
J-트랙(J0→JC) 완전 종결, 후속 착수 없음. `외부데이터_정리.md`에 1줄 반영. **엔드게임 동결 유지** —
2026-07-25 ENDGAME 선언 상태(최종 파일 = 팀원 v7+CNN, LB 0.65184) 변경 없음.
